# Bug Log

Owns accepted bugs, triage, routing, fix evidence, and closure.

Humans can report bugs in any format. The agent should extract the useful details, choose a route, and ask only for blocking reproduction, environment, or priority details.

| ID | Symptom | Severity | Route | Status | Owning task | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | Selected-path backlog rows pass graph checks while still bundling independent implementation outcomes. | High | Spike / planning bug | Closed | `FND-V1-017` | `artifacts/fnd-v1-017-selected-backlog-granularity.md`; planning commit `481cb44`. |
| BUG-002 | `test:unit` opportunistically runs real tmux processes when local binaries exist, causing load-dependent failures in the deterministic unit gate. | Medium | Small bugfix | Closed | Validation harness | Real tmux suites are opt-in through `pnpm test:tmux`; default unit and explicit smoke commands pass. |
| BUG-003 | A matched static wildcard reports a missing GET file as 405 because method discovery sees its automatic HEAD route. | Medium | Small bugfix | Closed | `IFC-V1-024` / app factory | Current-method route detection plus pinned static missing-file regression; `artifacts/ifc-v1-024-fastify-static-boundary.md`. |
| BUG-004 | The deadline fixture intermittently rejects a valid 5-second monotonic duration because it requires integer timestamp subtraction. | Low | Small bugfix | Closed | Validation harness | Fractional monotonic duration schema plus close-to assertion; canonical 408-test unit gate passes. |
| BUG-005 | A finite Readable SSE source completes on a real listener but the pinned plugin leaves the HTTP response and handler open. | High | Backlog bugfix | Closed | `IFC-V1-023` / `IFC-V1-025` | Readable-end raw-response termination plus real finite-response and active-shutdown regressions; `artifacts/ifc-v1-025-fastify-host-lifecycle.md`. |
| BUG-006 | Exact Codex emits notifications after the initialize response but before `initialized`; the connection treats them as pre-initialize violations and terminates. | High | Small bugfix | Closed | `INT-V1-004` / `INT-V1-006` | Bounded ordered response/ack queue, hostile-window tests, exact private-socket smokes, and semantic capture. |
| BUG-007 | The goal-based legacy thread materialization path sets an active goal, which autonomously starts model turns despite its no-model contract. | Critical | Backlog bugfix | Closed | `INT-V1-005` / `INT-V1-006` | Paused internal goal, active-marker recovery, idle/zero-turn/token/history real smoke, corrected evidence, `DEC-022`. |
| BUG-008 | Private Serve is classified as public because the observer treats a nonempty `funnel status --json` result as a distinct Funnel projection. | High | Backlog bugfix | Closed | `IFC-V1-071` / `IFC-V1-072` | Exact 1.98.8 source/live semantics, duplicate-read equality regression, corrected active observer smoke, and private enable/read-back/path-off smoke. |
| BUG-009 | Proxy-decision invariants reject truthful combined hostile-header assessments unless lower-priority forwarding and identity evidence is falsely normalized. | High | Backlog bugfix | Closed | `FND-V1-018` / `IFC-V1-073` | Precedence-aware schema plus combined lookalike/unknown/identity/forwarding contract regressions. |
| BUG-010 | The exact Codex thread lifecycle smoke can fail cleanup when the native app-server outlives its npm launcher while settling its temporary plugin cache. | Low | Small bugfix | Closed | Validation harness / `IFC-V1-061` | Owned-socket shutdown wait, bounded recursive-remove retries, and consecutive exact 0.144.0 lifecycle smokes. |
| BUG-011 | The exact HostDeck/TUI coexistence smoke can leave its marker command unfinished or pause TUI B before the product view, despite healthy completed runtime state. | Medium | Small bugfix | Closed | Validation harness / `INT-V1-031` / `INT-V1-032` | Bounded prompt/tool timing, direct second-TUI identity proof, isolated update-check suppression, sanitized diagnostics, and clean exact standalone/aggregate passes. |
| BUG-012 | The strict Android runner fails Fastify readiness because its fixed authenticated driver routes omit required API response schemas. | High | Small bugfix | Closed | Validation harness / `IFC-V1-079` | Commit `3528c6c`; route-schema inventory/static gates plus clean physical Fastify/start/pair/reload progression pass. |
| BUG-013 | Post-pairing Chrome foreground inspection reads ActivityManager intent state containing the protected QR fragment and correctly aborts on its own privacy guard. | High | Small bugfix | Closed | Validation harness / `IFC-V1-079` | Commit `b4078b6`; bounded WindowManager-only regression plus the clean no-retry physical run pass all 12 phone rows without retaining the fragment. |
| BUG-014 | A current production audit reports two high-severity `fast-uri` host-confusion advisories through Fastify's AJV and JSON serializer paths. | High | Release blocker | Closed | Supply chain / `FE-V1-010` validation | Exact patched overrides 3.1.4/4.1.1, dependency-tree proof, zero-vulnerability audit, full workspace/runtime/package gates, implementation `9b095ad`. |
| BUG-015 | The selected mobile access fixture requires browser state that the selected APIs cannot produce and permits implicit loopback writes. | High | Backlog bugfix | Closed | `FE-V1-025` | Route-backed coordinator contract and regressions; implementation `888abf1`. |
| BUG-016 | Newly published production advisories affect the selected router, static plugin, browser router, and transitive glob graph. | High | Release blocker | Closed | Supply chain / `FE-V1-020` validation | Exact patched versions, zero-vulnerability audit, and full workspace/package/browser gates. |
| BUG-017 | Production substitutes the reviewed Codex version for a real probe and exits before it can serve an incompatible-runtime diagnostic UI. | Critical | Release blocker | Closed | `IFC-V1-087` | Real probe, diagnostic-ready listener, durable/public truth, aggregate and clean exact/drift process evidence; implementation `ceb339e`. |
| BUG-018 | Physical dashboard acceptance treats the intentionally below-fold quiet session as a failed initial Mission Control load after all protected reads return 200. | High | Release blocker | Closed | `FE-V1-090` | The next clean physical run passed pairing, protected route reads, the attention-first viewport, and fragment-free reload entry; exact cleanup passed. Implementation `9bc73ec`. |
| BUG-019 | Physical dashboard reload scrolls for a quiet session without first expanding its intentionally collapsed `QUIET` disclosure. | High | Release blocker | In progress | `FE-V1-090` | The clean `9bc73ec` run reached the fragment-free reload and exposed the inaccessible target. Accessible disclosure naming and an explicit verified expand interaction pass focused component/driver and Chromium checks; corrected physical acceptance remains pending. |
| BUG-020 | The supported-browser oracle can inspect or classify a routed POST failure without complete intercepted-response proof, making a valid synthetic response pass or fail according to scheduler timing. | High | Release blocker | Closed | `FE-V1-040` / `FE-V1-090` | Exact same-request `200`, one-request, schema, origin, and header proof gates synthetic CSRF-bootstrap aborts; the corrected 76/76 matrix passes with zero unexpected diagnostics. |
| BUG-021 | Android Chrome drops the stateful `aria-label` from the native quiet-queue `summary`, exposing only concatenated visible text to the physical accessibility hierarchy. | High | Release blocker | In progress | `FE-V1-090` | The clean pushed `4b1316b` phone run proved the mismatch. The explicit stateful button passes focused/broad gates, ten inspected captures, deterministic package identity, and the exact 76-case matrix; corrected physical acceptance remains pending. |
| BUG-022 | Android exposes an unnamed whole-session link only through its 21 CSS-pixel child text node, despite the DOM link's valid 96 CSS-pixel box. | High | Release blocker | In progress | `FE-V1-090` | The clean pushed `23c8fc3` phone run passed the corrected quiet disclosure and failed the exact 44 CSS-pixel target gate at `178.1x21`; cleanup passed. Explicit row naming and full-target device acquisition pass focused, broad, and 76/76 package gates; corrected physical acceptance remains pending. |
| BUG-023 | A Firefox CONNECT reset can terminate the supported-browser HTTPS proxy through an unhandled client-socket `ECONNRESET`. | High | Release blocker | Closed | `FE-V1-040` / `FE-V1-090` | Persistent socket handling and a mandatory 12-reset plus real-TLS guard pass; the corrected 76/76 package matrix publishes complete evidence and cleans all owned state. |
| BUG-024 | The physical dashboard replay fixture violates the production boundary-handoff contract, so Session Detail receives a fail-closed `500` instead of a live SSE subscriber. | High | Release blocker | Closed | `FE-V1-090` | The clean pushed `bcdcd1e` run established one live subscriber, drained the corrected replay, and opened boundary plus complete-event diagnostics exactly once. |
| BUG-025 | Physical failure cleanup can issue lifecycle-owned remote disable before a fresh idle observation after restoring the dedicated profile. | High | Release blocker | Closed | `FE-V1-090` | Fresh-idle cleanup passed in the pushed `824fbec` failure run; Serve, phone settings, Chrome, tunnels, processes, temporary/evidence state, and worktree all restored exactly. |
| BUG-026 | The physical detail sequence assumes top context and below-fold replay truth are already present in the current Android hierarchy. | High | Release blocker | Closed | `FE-V1-090` | The clean pushed `bcdcd1e` run bounded-backward-revealed fully visible `Current`, then bounded-forward-revealed and opened the first two event rows exactly once. |
| BUG-027 | Physical event diagnostics can cross a row boundary when associating a body label with the geometrically nearest details action. | High | Release blocker | Closed | `FE-V1-090` | The clean pushed `c5925c2` run used the bounded preceding owner, opened exact `Turn event`, verified `Content redacted`, and reached screenshot capture. |
| BUG-028 | Physical screenshot cropping rejects Chrome's valid collapsed-toolbar accessibility state. | High | Release blocker | Closed | `FE-V1-090` | The clean pushed `ffd5a1d` run captured all three event dialogs through the collapsed state and advanced into approval control. |
| BUG-029 | Inline approval review can be hierarchy-present beneath the fixed session-control dock. | High | Release blocker | Closed | `FE-V1-090` | The clean pushed `30f55f4` run safely revealed review, opened confirmation, verified its reason, and captured it. |
| BUG-030 | Approval confirmation action is not admitted by exact semantic, clickable, fully visible reveal on the real hierarchy. | High | Release blocker | Closed | `FE-V1-090` | The clean pushed `e59e1c4` phone run reacquired the exact fully visible semantic action, dispatched one approval request, and reached responding plus terminal approved truth. |
| BUG-031 | Failure cleanup can leave a background Android Chrome process after returning Home. | High | Release blocker | In progress | `FE-V1-090` | Forty-seven product-sequence post-mutation failed executions plus two bounded diagnostic stops ended with Serve absent, Chrome stopped, dedicated profile selected, and clean worktree; one cellular preflight failure also restored exact state. The harness also proved launcher foreground before one unrelated app later interrupted the device. Four separate auto-lock preflight refusals made no mutation. Successful-run absence proof remains. |
| BUG-032 | A production pairing-confirmation timeout reports no bounded claim/CSRF response or recognized UI-state evidence. | High | Release blocker | Closed | `FE-V1-090` | Implementation `de5b22b` adds private-free diagnostics; its clean no-retry run crossed pairing and reached approval confirmation. |
| BUG-033 | Approval sequencing can cross a stream reconnect and mistake prompt-composer geometry for the closed confirmation footer. | High | Release blocker | Closed | `FE-V1-090` | The clean pushed `e59e1c4` phone run retained stable write authority through approval and used only the exact semantic action; no anonymous geometry fallback or duplicate request was admitted. |
| BUG-034 | The finite server request deadline terminates a committed long-lived SSE response and removes phone write authority during an active workflow. | High | Release blocker | Closed | `FE-V1-090` / Fastify SSE transport | Pushed `e59e1c4` passes real-listener deadline/pre-commit-abort regressions, disconnect/cleanup matrices, and a clean phone run that retained one stream through exact approval terminal truth beyond the former deadline. |
| BUG-035 | After approval moved before event diagnostics, the first replay-boundary row is above the viewport but its reveal path swipes only toward later activity. | High | Release blocker | Closed | `FE-V1-090` | The clean pushed `33cc627` phone run searched backward for the first boundary, then forward for both later events, opened each exact details action once, verified all three limitations, and continued through prompt completion. |
| BUG-036 | The physical recovery fixture injects a terminal storage error while requiring the browser to reconnect automatically. | High | Release blocker | Closed | `FE-V1-090` | Pushed `0fb3586` removes the invalid injection. The clean `f51ff84` run proved one `source_failed`, exact `holding/1`, visible/captured write block, same-request release, one restored subscriber, and no extra request or failure. |
| BUG-037 | Accepted prompt progress masks a later composer availability block, leaving disabled writes without a visible cause. | High | Release blocker | Closed | `FE-V1-090` | Pushed `7858b61` prioritizes non-turn availability failures while preserving accepted-turn progress. The clean `b6c31f6` run rendered/captured reconnect truth, restored one subscriber, and then observed/captured restored fixed-composer accepted progress. |
| BUG-038 | The post-recovery phone oracle requires top-page `Current` while Session Detail remains scrolled at the fixed composer. | High | Release blocker | Closed | `FE-V1-090` | Pushed `9b3d2c5` requires unique restored `Turn completed`, no reconnect/unavailable marker, and one subscriber. The clean `b6c31f6` run crossed that capture and advanced to stale-detail validation. |
| BUG-039 | Full Chrome refresh retains deep Session Detail scroll while the stale-state oracle requires a top notice. | High | Release blocker | Closed | `FE-V1-090` | Pushed `d0d31f7` reuses bounded dock-aware backward reveal. The clean `c3fd9a4` run revealed/captured stale truth, restored current detail, and captured not-found before advancing. |
| BUG-040 | Direct Android-intent navigation can confuse retained-tab authority with same-tab history recovery. | High | Release blocker | Closed | `FE-V1-090` | The clean `c2f834b` run passed exact missing-tab authority, external-tab closure, normal Chrome relaunch, one BFCache detail read, one stream/opened subscriber, exact one active subscriber, and restored UI-ready truth before advancing into `/plan`. |
| BUG-041 | Physical `/plan` acceptance waits for generic lifecycle status below the approved phone viewport. | High | Release blocker | Closed | `FE-V1-090` | The clean `b9797bc` run passed initial Default with one read, one submitting Plan write, staged Plan, applied Plan, and reopened current Plan through approved visible hierarchy truth without scrolling or resend, then advanced to session utilities. |
| BUG-042 | Android Chrome omits exact child slash text from visible composite session-utility rows. | High | Release blocker | Closed | `FE-V1-090` | The clean `888fe19` run acquired exact full-row `Open /usage`, `Open /compact`, and `Open /skills` semantics, drove each once, completed Usage plus the full Compact lifecycle, and advanced inside Skills. |
| BUG-043 | Chrome's Android UIAutomator bridge leaves the HTML Skills EditText unnamed even with a direct ARIA label. | High | Release blocker | Closed | `FE-V1-090` | The clean `8f8b10c` run admitted only the unique page-contained editor under exact current Skills landmarks, entered the local filter, required `1 matching`, captured Skills, and advanced to laptop Resume. |
| BUG-044 | Android omits child labels from Session Actions composite rows that have no explicit whole-button name. | High | Release blocker | Closed | `FE-V1-090` | The clean `633a7c7` run acquired the exact clickable `Open Resume on laptop` row, rendered `Laptop terminal only`, captured it, and reached the successful copy outcome. Exact whole-button names remain on all four rows with described-by detail. |
| BUG-045 | Clipboard cleanup opens a second external Session Detail tab while the retained selected-session tab remains live. | High | Release blocker | Closed | `FE-V1-090` | The clean `cff9f75` run cleared the copied command, proved unchanged retained authority in the external clipboard tab, zero active subscribers after Back, and exactly one incremented detail read, stream request, opened subscriber, and active subscriber after normal Chrome relaunch, then advanced into Interrupt. |
| BUG-046 | The physical fixture marks the session active without publishing the exact active-turn event required by Interrupt. | High | Release blocker | Closed | `FE-V1-090` | The clean `ab7d685` run correlated one monotonic active event to the exact fixture turn, exposed the clickable whole-row Interrupt action, opened exact confirmation, sent one request, rendered `Turn interrupted`, and published the ordered terminal event. |
| BUG-047 | The physical Interrupt oracle expects `Done` to retain Session Actions although the owned UI contract closes every dialog. | High | Release blocker | Closed | `FE-V1-090` | The clean `8cd62f5` run completed Interrupt, tapped `Done`, proved the Session Detail actions trigger and writable composer returned with neither actions nor result dialog retained, and advanced into Host access. |
| BUG-048 | The physical Session Detail Host step still expects a direct trigger removed by the approved Session Actions consolidation. | High | Release blocker | Closed | `FE-V1-090` | The clean `2b9680a` run opened and measured the exact fourth Session Actions row, entered the embedded Host page with its back affordance, proved current remote-ready and read/write truth, and completed one read-only remote check before evidence capture. |
| BUG-049 | Host evidence capture rejects the intentionally rendered private origin as if it were retained Chrome material. | High | Release blocker | Closed | `FE-V1-090` | The clean `ac9b5d5` run wrote the redacted Host screenshot, opened and captured Office-device revocation confirmation, revoked exactly one device, and captured terminal revocation before advancing. Browser/private rejection and pixel-boundary tests remain green. |
| BUG-050 | Android does not open Host-lock confirmation after post-revocation taps on the exact enabled control. | High | Release blocker | Closed | `FE-V1-090` | Pushed `8754a63` used a strict unobscured Host content target and physically crossed lock confirmation, terminal lock, local-only unlock, and profile-away recovery with exact cleanup. |
| BUG-051 | Physical archive acceptance selects the lowest matching label without proving that the destructive action belongs to the open confirmation. | High | Release blocker | In progress | `FE-V1-090` | A strict dialog-owned selector and hostile fixture matrix pass; one clean physical archive crossing remains required. |
| BUG-052 | Physical TalkBack acceptance inventories the entire UI tree and uses keyboard/direct-tap fallbacks instead of proving accessibility focus and TalkBack activation. | High | Release blocker | In progress | `FE-V1-090` | Audit disproves the current transcript; a non-suppressing physical focus-observation spike and strict gesture path are required. |
| BUG-053 | Recovered-profile Host evidence rejects its global-sheet origin because redaction recognizes only the Session Actions Host context. | High | Release blocker | Closed | `FE-V1-090` | Fresh candidate `03103cd` retained the redacted recovered-profile Host checkpoint and advanced into runtime compatibility with exact cleanup. |
| BUG-054 | The deliberate incompatible-runtime transition still requires the pre-transition remote-ready label. | High | Release blocker | Closed | `FE-V1-090` | Fresh candidate `03103cd` completed the incompatible status-then-refresh branch and closed the Host sheet before the later route oracle failed. |
| BUG-055 | Xiaomi reports the active USB-C power source as plug bit `1`, while the long-run harness hard-codes stay-awake bit `2`. | High | Release blocker | Closed | `FE-V1-090` | Fresh candidate `03103cd` held the phone awake through 874 seconds of physical work and restored the exact original setting during cleanup. |
| BUG-056 | Runtime acceptance treats request start as completion, does not restore the Mission Control viewport, and expects a supported detail title that the route intentionally hides. | High | Release blocker | In progress | `FE-V1-090` | Exact successful-response settlement, sheet-owned detail truth, bounded route reveal, and stable incompatible/current predicates pass focused tests; one fresh phone crossing remains required. |
| BUG-057 | A bounded Session Detail reveal timeout reports no target, dock, region, or swipe-state evidence. | High | Release blocker | Closed | `FE-V1-090` | Pushed `68d17ff` and fresh candidate `7e6b06d` produced four bounded private-safe absent-target states with changing scroll witnesses and exact cleanup, isolating `BUG-058` without weakening admission. |
| BUG-058 | The physical approval fixture expires at a hard-coded calendar instant, making its required action disappear as time advances. | High | Release blocker | Closed | `FE-V1-090` | Fresh candidate `9bc7679` crossed pending approval, confirmation, responding, and terminal approved truth before reaching late Host-access controls; exact cleanup passed. |
| BUG-059 | Local-unlock Session Detail reopening can pass its tap oracle from a pre-existing subscriber and then fail without current route evidence. | High | Release blocker | Closed | `FE-V1-090` | Fresh candidate `03103cd` held unlocked Mission Control stable, opened one exact new detail/stream generation, restored writable Session Detail, and advanced through profile recovery. |
| BUG-060 | The dashboard aggregate invariant expects two browser remote checks although the frozen sequence performs four. | High | Release blocker | In progress | `FE-V1-090` | The final invariant now requires all four named read-only checks and the runtime branch requires exactly two; focused tests pass and one full phone crossing remains required. |
| BUG-061 | Archive entry assumes the session remains visible after TalkBack and accepts any active subscriber as successful navigation. | High | Release blocker | In progress | `FE-V1-090` | Archive now requires zero retained transport, bounded current-row reveal, one exact detail/stream generation, and stable writable detail; one phone crossing remains required. |
| BUG-062 | The physical claiming-state capture can exhaust the 10-second remote admission lease before Mission Control bootstrap. | High | Release blocker | In progress | `FE-V1-090` | The gated claim now requires one same-generation read-only admission refresh on each side of capture; hostile drift tests pass and one fresh phone crossing remains required. |
| BUG-063 | A late Tailscale relay outage is reported as a `/skills` failure and can strand owned Serve state during physical cleanup. | High | Release blocker | In progress | `FE-V1-090` | Exact Skills route counters now separate pre-server transport loss from handler failure; cleanup drains the selected lifecycle before bounded ownership-safe reconciliation with at most one actual Serve mutation. One fresh phone run remains required. |
| BUG-064 | A one-attempt physical remote check reacquires its action while its exact request is still in flight. | High | Release blocker | In progress | `FE-V1-090` | Pushed `bde6968` owns the first tap through an exact 45-second response boundary; one later phone crossing remains required. |
| BUG-065 | Android cleanup can report success without complete settled Chrome package-process absence. | High | Release blocker | Closed | `FE-V1-090` | Pushed `bde6968` requires one user-0 force-stop and 30 continuous seconds of main-plus-child absence; failed candidate `38c2a48` completed that exact cleanup and direct post-run inspection. |
| BUG-066 | Returning from an external Chrome page depends on retained launcher activity state instead of a canonical HostDeck route. | High | Release blocker | Closed | `FE-V1-090` | Pushed `2017215` uses one Back, one canonical root VIEW, exact Mission Control bootstrap, and one selected-row open; failed candidate `38c2a48` crossed both not-found and clipboard returns before an unrelated later failure. |
| BUG-067 | The Interrupt-to-Host handoff accepts one transient Session Detail sample, then loses all actionable Session Actions diagnosis. | High | Release blocker | In progress | `FE-V1-091` | Independent review accepts `e2201fb`'s monotonic shared-baseline/fresh-budget correction and two-use waiter test. It remains open only for the later no-retry phone crossing; no failed candidate was rerun. |
| BUG-068 | Physical aggregate actions use selectors whose ownership, geometry, or mutation oracle can be ambiguous, impossible, or swallowed. | High | Release blocker | Closed | `FE-V1-092`, `FE-V1-095` to `FE-V1-097` | Independent `FE-V1-100` review found and corrected remaining destructive/result, Session Actions, Utility/Resume, and clipboard ownership defects in `1d678f8`. Exact owner and hostile ambiguity tests pass; no phone or environment execution occurred. |
| BUG-069 | Physical route and authority transitions accept incomplete destination or one-sample truth. | High | Release blocker | Closed | `FE-V1-093`, `FE-V1-098` | Independent `FE-V1-100` review accepts complete stable Mission/detail/result transition and reload oracles through `1d678f8`. Final hostile driver and broad static/unit gates pass; no phone execution occurred. |
| BUG-070 | Profile-away settlement expects host-received route work instead of proving closed-admission zero-delta failure. | High | Release blocker | Closed | `FE-V1-094` | Independent `FE-V1-100` review accepts continuous foreign Serve/manager state and exact away/return deltas, and `1d678f8` routes profile observations through the full registry oracle. Hostile tests pass; no profile/Tailscale/Serve execution occurred. |
| BUG-071 | The aggregate physical-action ledger is metadata disconnected from the code it claims to guard. | High | Release blocker | Closed | `FE-V1-099` | Independent `FE-V1-100` review found TalkBack and zero-tap observation bypasses. Pushed `1d678f8` gives them typed registry ownership and executes selector/counter/diagnostic/transition oracles; exact-set, source-guard, call-site, and invalid-counter hostile tests pass. |
| BUG-072 | Aggregate evidence publication binds a historical package identity that cannot match the current supported-browser package. | High | Release blocker | Closed | `FE-V1-101` | Pushed `7b67563` binds a freshly built and independently verified complete package/web identity to the republished exact 76-case browser matrix before device mutation, and passes that identity directly to publication. Hostile stale/partial/malformed/drift cases and broad gates pass without phone or environment mutation. |
| BUG-073 | Physical approval responding truth is searched under an owner that production intentionally blocks with the still-open confirmation dialog. | High | Release blocker | Closed | `FE-V1-102` | Pushed `e82873e` selects the exact disabled pending dialog, unchanged navigation, and one pending call before release, then requires dialog absence and one exact terminal owner. Hostile and broad host gates pass without another phone run. |
| BUG-074 | The aggregate process inherits a bounded API command session that sends SIGTERM before physical acceptance can finish. | High | Release blocker | Closed | `FE-V1-090` | Candidate `87a6216` ran to its own 439-second harness result under a detached mode-600 monitor with no profile id in arguments or retained output; its later assertion failure is `BUG-075`. |
| BUG-075 | The nested prompt helper asserts standalone action history after the aggregate dashboard has already consumed valid earlier actions. | High | Release blocker | Closed | `FE-V1-103` | Pushed `01983b9` adds exact typed standalone and dashboard-embedded histories, composes the final dashboard prefix from the cumulative prompt prefix, and passes cross-scope plus missing/extra/duplicate/reordered hostile tests and broad host gates. |
| BUG-076 | The first aggregate event-diagnostic target can disappear after exact acquisition without an outcome-specific terminal diagnostic. | High | Release blocker | Closed | `FE-V1-104` | Pushed `440b0bf` requires continuous target/authority stability, a final registry-owned guard, one tap, exact current-sheet completion, and bounded target/request/sheet diagnostics; hostile and broad host gates pass. |
| BUG-077 | Async ADB failures discard operation identity and abort bounded preflight polling on expected nonzero probe outcomes. | High | Release blocker | Closed | `FE-V1-105` | Pushed `c8bf276` gives ping, HTTPS, and UI reads exact typed private-free outcomes; only bounded network preflight interprets completed nonzero exits as false, while UI/transport/process failures remain immediate and exact. |
| BUG-078 | Physical stream recovery records no semantic observation when every Android hierarchy read fails inside the stable-state waiter. | High | Release blocker | Closed | `FE-V1-106` | Typed bounded observations replace `states=none`; hostile host tests pass and later candidates crossed the real recovery checkpoint. |
| BUG-079 | Production route ports detach class methods from their owning service, so managed session start fails before dispatch with `internal_error`. | Critical | Release blocker | Closed | `IFC-V1-082` / `IFC-V1-040` | Pushed `793e69b` and `08c210f`; focused 94 tests, typecheck, planning/runtime gates, deterministic build, standalone package verification, live upgrade, one successful managed session, zero recovery rows, and paired-phone `Current`/`Ready to send` inspection pass. |
| BUG-080 | An accepted paired-phone prompt reports failure and delegated child completion stops parent projection. | Critical | Release blocker | Closed | `IFC-V1-041` / `INT-V1-017` / `FE-V1-020` | `0.0.3` paired-phone send produced one accepted/succeeded dashboard operation, live user/agent output, and terminal current/quiet truth without retry. |
| BUG-081 | Browser SSE rejects a valid forward replay boundary when retained history begins after its requested cursor. | Critical | Release blocker | Closed | `FE-V1-023` / `FE-V1-012` | Pushed `8230f5b`; focused 40 tests, complete 909-test web suite, clean package verification, preserved-state upgrade, and paired-phone live-stream evidence pass. |
| BUG-082 | A newly adopted native session makes Mission Control unavailable because its valid initial replay boundary is rejected as corrupt retained state. | Critical | Backlog bugfix | Closed | `INT-V1-109` | Fixed in `76f5016`; public list/detail regressions, full suites, and the exact 0.144.0 phone-read lifecycle pass. |

## Routing

| Route | Use when | Backlog interaction |
| --- | --- | --- |
| Small | Local root cause, clear expected behavior, no planning change | Fix directly; link existing task if relevant |
| Backlog | Multi-step fix or affects planned/completed work | Create or update leaf task(s), add `BUG-*` refs, update blockers if needed |
| Spike | Root cause or expected behavior is unclear | Create triage/spike task before implementation |
| Release blocker | Blocks acceptance, data integrity, security/privacy, install/run, deployment, or critical flow | Mark blocker in status/release tracking and prioritize blocking task(s) |

## Bug Template

```md
### BUG-000 Name

- Symptom:
- Impact:
- Route:
- Related requirements:
- Affected / owning task:
- Blocks:
- Root cause:
- Fix:
- Validation:
- Closed by:
```

### BUG-001 Selected Backlog Granularity

- Symptom: unfinished selected-path rows such as `DAT-V1-020`, `INT-V1-006`, and `IFC-V1-017` to `IFC-V1-021` contain independent outcomes that cannot be handed off without architecture decisions during implementation.
- Impact: dependency readiness and V1 completion can look stronger than the executable leaf backlog really is.
- Route: planning bug; implementation leaves are gated while the remaining selected backlog is audited and decomposed.
- Related requirements: all active V1 requirements through their existing owners; no product scope change.
- Affected / owning task: `FND-V1-017`.
- Blocks: resolved; affected execution now uses handoff-sized leaves, with deliberate spikes/acceptance/hardening gates classified explicitly.
- Root cause: `check:planning` validates graph/trace/status integrity but cannot determine semantic task breadth.
- Fix: classify every unfinished row, split independent outcomes, update dependencies/traces/block maps/queue, and record intentional module-hardening/release/human-gate rollups explicitly.
- Validation: planning check, manual junior-handoff audit, before/after inventory artifact, clean diff/commit/push.
- Closed by: `FND-V1-017`; planning commit `481cb44` pushed to `origin/main`.

### BUG-002 Real Tmux Leaks Into Unit Gate

- Symptom: two consecutive `pnpm test:unit` runs failed in different real-tmux tests under suite-wide load, while the same smoke passed in isolation.
- Impact: the deterministic unit gate depended on installed binaries, process scheduling, and tmux timing; failures could obscure regressions in unrelated work.
- Route: small bugfix; expected test-layer behavior was already clear from the dedicated `pnpm test:tmux` command.
- Affected / owning task: validation harness; discovered while validating `IFC-V1-016`.
- Root cause: real tmux suites selected `describe` whenever tmux/Codex happened to exist instead of requiring the repository's explicit smoke environment flag.
- Fix: gate both real-tmux suites on `HOSTDECK_REQUIRE_TMUX_SMOKE=1`, make an explicitly requested missing tmux binary fail loudly, and expand `test:tmux` to run adapter plus server real-process coverage.
- Validation: `pnpm test:unit`, `pnpm test:tmux`, lint, typecheck, and the normal aggregate checks.
- Closed by: current `IFC-V1-016` validation unit.

### BUG-003 Static Missing File Misclassified As Method Error

- Symptom: `GET /assets/missing.*` enters the pinned static wildcard, calls the global not-found handler, and returns `method_not_allowed`/405 instead of `route_not_found`/404.
- Impact: clients receive false method guidance and static not-found behavior violates the stable API/error contract.
- Route: small bugfix; expected behavior is explicit in `IFC-V1-024` and no product or architecture choice changed.
- Affected / owning task: `IFC-V1-024`; root cause was in the completed `IFC-V1-022` app-factory method resolver.
- Root cause: `allowedMethodsForUrl` skipped the current GET method, then treated the matched wildcard's generated HEAD route as evidence that GET was unsupported. It did not distinguish a router miss from a matched handler deliberately calling not-found.
- Fix: return no alternate-method result when `findRoute` confirms the current method already matches the URL; preserve normal 405 behavior when the current method has no route.
- Validation: pinned static valid/missing GET regression, explicit browser POST 405, focused factory/static tests, and aggregate unit/contract/integration gates.
- Closed by: `IFC-V1-024`; evidence in `artifacts/ifc-v1-024-fastify-static-boundary.md`.

### BUG-004 Fractional Monotonic Duration Fixture

- Symptom: under parallel test loading, the app-factory deadline fixture serializes a duration infinitesimally different from integer `5000` and response validation converts the otherwise valid probe into a 500.
- Impact: the deterministic unit gate can fail based on floating-point representation of `performance.now()`, obscuring unrelated regressions.
- Route: small bugfix; the monotonic deadline contract already permits numeric timestamps and integer timeout inputs.
- Affected / owning task: validation harness; discovered during `IFC-V1-024` aggregate validation.
- Root cause: the fixture required `expiresAtMs - startedAtMs` to satisfy `z.number().int()` and exact equality even though both timestamps are fractional monotonic values.
- Fix: keep the duration positive, require it to be close to 5,000 ms, and retain exact same-signal plus bounded-positive remaining-time assertions.
- Validation: repeated focused parallel factory/static run and canonical `pnpm test:unit` with 408 passed/18 explicit external skips.
- Closed by: current `IFC-V1-024` validation unit.

### BUG-005 Finite SSE Leaves Real Response Open

- Symptom: a finite selected-event source reaches generator `finally`, but a real HTTP client never receives response end and Fastify listener shutdown times out; injection had appeared to settle.
- Impact: finite streams retain the handler/request slot and can hang otherwise cooperative listener shutdown and restart.
- Route: backlog bugfix discovered by the planned `IFC-V1-025` real-listener lifecycle matrix; expected finite-source behavior was already owned by `IFC-V1-023`.
- Affected / owning task: transport fix in `IFC-V1-023`; real shutdown evidence in `IFC-V1-025`.
- Root cause: `@fastify/sse` 0.5.0 does not end the raw response when the Readable source ends, and `await reply.sse.send(readable)` cannot reach post-send cleanup until that response closes.
- Fix: attach one Readable `end` listener before send and explicitly end a still-writable raw response; retain plugin close after send and remove the listener during final cleanup. Listener shutdown reaps sockets that become idle after close initiation without force-closing active requests.
- Validation: direct real HTTP finite-source response end, zero final in-flight accounting, active finite-SSE lifecycle close, exact cleanup order, and immediate same-port restart.
- Closed by: `IFC-V1-025`; evidence in `artifacts/ifc-v1-025-fastify-host-lifecycle.md`.

### BUG-006 Initialize Response/Acknowledgement Notification Race

- Symptom: an isolated exact 0.144.0 app-server emits `configWarning` and `remoteControl/status/changed` after the successful initialize response but before HostDeck can send `initialized`; HostDeck reports three fatal protocol issues and closes the private socket.
- Impact: valid authenticated startup can fail based on app-server configuration notifications, blocking all structured runtime operations despite a compatible version and schema.
- Route: small bugfix; the real trace establishes expected ordering and no product or architecture choice changes.
- Affected / owning task: connection handshake from completed `INT-V1-004`; discovered by the pre-model phase of `INT-V1-006`.
- Root cause: the connection used one boolean for both "initialize response not observed" and "initialized acknowledgement not sent," so it could not distinguish a truly premature message from the legal response/ack race window.
- Fix: broker reports the correlated initialize response synchronously; connection accepts only the resulting narrow handshaking window, queues server-originated messages in order under the existing pending-server-request bound, sends `initialized`, then flushes. Messages before the response and queue overflow still terminate.
- Validation: deterministic notification/server-request ordering and overflow tests, retained pre-response rejection matrix, recorder tests, and exact isolated 0.144.0 no-model/live probe rerun.
- Closed by: `INT-V1-006`; evidence in `artifacts/int-v1-006-codex-operation-semantics.md`.

### BUG-007 Agentic Internal Goal Materialization

- Symptom: each internal `thread/goal/set` used to materialize a zero-turn legacy thread returns `active`, then app-server emits `thread/status/changed: active`, `turn/started`, reasoning/message items, token usage, and potentially approval requests after the marker is cleared.
- Impact: session creation can spend model usage, execute agent work, pollute event attribution/history, and invalidate the `INT-V1-005` no-model evidence and the semantic spike's cost bound.
- Route: backlog bugfix against the completed lifecycle behavior; expected behavior remains an isolated, persisted, empty thread with no model work.
- Affected / owning task: `INT-V1-005` materialization and `INT-V1-006` semantic evidence.
- Root cause: `thread/goal/set` defaults a new objective to `active`; active goals are execution controls, not passive metadata. Immediate clear does not cancel the already scheduled turn.
- Fix: create the version-scoped internal marker with explicit `status: paused`; if recovery finds a prior active marker, pause it before clear and reject unsupported terminal marker states.
- Validation: unit request/status/recovery assertions plus exact isolated 0.144.0 lifecycle smoke requiring idle state, empty stored turns, no `turn/started`, no token-usage update, and no agent-message delta before TUI resume.
- Closed by: corrected `INT-V1-005` lifecycle plus `INT-V1-006`; evidence in `artifacts/int-v1-005-managed-thread-lifecycle.md` and `artifacts/int-v1-006-codex-operation-semantics.md`.

### BUG-008 Private Serve Misclassified As Funnel

- Symptom: after the ownership-safe manager created one private HTTPS root proxy from an empty profile, configured observation reported `public` and correctly refused cleanup.
- Impact: every valid nonempty private Serve mapping would remain unavailable; explicit enable could end incomplete and leave an owned mapping requiring manual path-scoped cleanup.
- Route: backlog bugfix against completed `IFC-V1-071`, discovered by `IFC-V1-072` live mutation evidence; expected private/public ownership remains unchanged.
- Related requirements: `FR-018`, `NFR-005`, `NFR-010`, `NFR-013`, `PR-003`, `PR-007`, `SFR-015`, `DEC-027`.
- Affected / owning task: observer behavior in `IFC-V1-071`; manager validation in `IFC-V1-072`.
- Blocks: resolved before `IFC-V1-072` closure.
- Root cause: fixtures modeled `tailscale funnel status --json` as a separate Funnel-only projection. Exact 1.98.8 source and redacted live inspection proved both Serve and Funnel status commands call the same implementation and serialize the same ServeConfig; public exposure is represented by `AllowFunnel`.
- Fix: require the two parsed ServeConfig reads to be deeply equal, fail disagreement as `schema_invalid`, and classify public state only when `AllowFunnel` is present. Preserve the second bounded read as a race/consistency check.
- Validation: 23 focused observer regressions, corrected real active-profile observer smoke, exact-source review, normalized live equality/cleanup inspection, and real manager private enable/exact read-back/HTTPS proxy/path-off/repeat smoke with final empty state.
- Closed by: `IFC-V1-071` corrective implementation and `IFC-V1-072` live validation; evidence in `artifacts/ifc-v1-070-tailscale-remote-ingress-spike.md` and `artifacts/ifc-v1-071-tailscale-observer.md`.

### BUG-009 Combined Proxy Rejections Require False Assessments

- Symptom: a request carrying an untrusted `X-Tailscale-*` lookalike plus missing forwarding or malformed standard identity could not produce a schema-valid rejection while retaining those actual assessments.
- Impact: the proxy evaluator would have to invent `forwarding: exact`, hide a simultaneous identity defect, or fail contract parsing on hostile input. That makes diagnostics misleading and can turn an intended fail-closed path into an internal error.
- Route: backlog bugfix against completed `FND-V1-018`, resolved before the dependent `IFC-V1-073` evaluator implementation.
- Related requirements: `NFR-005`, `SFR-002`, `SFR-012`.
- Affected / owning task: normalized proxy-decision contract in `FND-V1-018`; executable precedence and regression evidence in `IFC-V1-073`.
- Blocks: resolved before the `IFC-V1-073` evaluator consumes the contract.
- Root cause: schema refinements equated each lookalike or identity assessment with one exclusive reason and required exact forwarding for lookalike, identity, and unknown-context reasons. Hostile signals are independent and can coexist; only the highest-priority reason is singular.
- Fix: require lookalike precedence, permit unknown reserved context to precede malformed standard identity, constrain forwarding only for reasons that logically determine its assessment, and allow missing forwarding to be wholly absent or partial/invalid.
- Validation: all rejection reasons retain coherent representative evidence; strict reason-specific forwarding contradictions reject; combined lookalike plus malformed identity, unknown reserved plus malformed identity, and malformed identity plus missing forwarding preserve truthful assessments; incorrect lower-priority reasons reject.
- Closed by: `IFC-V1-073` contract correction and focused contract suite.

### BUG-010 Codex Lifecycle Smoke Cleanup Race

- Symptom: the exact Codex 0.144.0 thread lifecycle assertions complete, but temporary-home teardown can fail with `ENOTEMPTY` under `plugins/cache/openai-curated-remote`.
- Impact: valid real archive evidence can report a cleanup-only failure based on Codex background filesystem settling.
- Route: small bugfix; temp-resource ownership and eventual complete removal are already required, with no product or planning change.
- Affected / owning task: validation harness; discovered while closing `IFC-V1-061`.
- Root cause: the exact native app-server can outlive and be reparented from its npm launcher after the client disconnects, while recursive `rm` also used Node's zero-retry default. Teardown could therefore race the real socket owner and its plugin-cache writes.
- Fix: wait up to 10 seconds for the owned Unix socket to disappear after client/launcher shutdown, then retain fail-loud removal with five bounded native retries at 100 ms intervals.
- Validation: two consecutive exact Codex 0.144.0 thread lifecycle smokes, type/lint checks, and absence of retained `hostdeck-thread-smoke-*` roots.
- Closed by: current `IFC-V1-061` validation unit.

### BUG-011 Nondeterministic Exact TUI Coexistence Probe

- Symptom: aggregate lifecycle acceptance reached `exact_tui_coexistence` and failed without publishing evidence. Isolated diagnostics twice observed one authoritative completed turn with a stable HostDeck connection/runtime but a marker still at `started`; later runs completed that direction but intermittently left TUI B alive on a small pre-product startup view.
- Impact: valid runtime lifecycle behavior could fail based on minimal-model shell wait choices or an interactive update check, preventing repeatable `INT-V1-031` and `INT-V1-032` evidence.
- Route: small bugfix; expected multi-client identity, lifecycle, and cleanup behavior remains unchanged, while the local exact-runtime harness removes nondeterministic validation inputs.
- Affected / owning task: validation harness from completed `INT-V1-031`; discovered while validating `INT-V1-032`.
- Blocks: resolved before `INT-V1-032` closure.
- Root cause: the 20-second marker interval exceeded the shell tool's common initial yield, but the prompt prohibited a second wait call; TUI B identity depended on model sentinel replay instead of the exact resume target; and the isolated `CODEX_HOME` omitted Codex's supported startup-update suppression.
- Fix: use an eight-second marker interval with an explicit 15-second initial tool wait, prove TUI B by exact resume thread id plus managed cwd and HostDeck read-back, set only `check_for_update_on_startup = false` in the private test home, allow a bounded 30-second history-view readiness window, and emit classifications rather than terminal content on readiness failure.
- Validation: dirty-worktree diagnostics complete both teardown directions and stop only at the intentional clean-commit publication guard. The clean exact coexistence smoke then passed in 24.29 seconds, and the no-retry four-scenario aggregate passed in 91.01 seconds with zero resource residue; full workspace and supply-chain gates also pass.
- Closed by: corrective harness commit `7584321`; aggregate evidence in `artifacts/int-v1-032-runtime-lifecycle-acceptance-evidence.json`.

### BUG-012 Physical Driver Routes Omit Response Schemas

- Symptom: `pnpm smoke:remote-android` fails during Fastify readiness before Serve mutation, QR generation, or phone interaction because `physical-phone-driver` registers API routes without response schemas.
- Impact: the selected physical Android acceptance cannot start, while cleanup correctly leaves the dedicated Serve state absent and the phone unchanged.
- Route: small bugfix; the production app boundary and frozen `IFC-V1-079` composition contract already require normal API plugin registration.
- Related requirements: `NFR-005`, `PR-007`, `SFR-005`, `SFR-018`.
- Affected / owning task: validation harness in `IFC-V1-079`.
- Blocks: clean committed physical Android acceptance rerun.
- Root cause: the phone-driver routes were added without the strict Zod response maps required by the API surface hook, while ordinary tests covered driver state and browser bundle behavior but never enumerated route registration.
- Fix: declare exact empty-checkpoint, bounded pre-revocation, and command/revision schemas for all ten fixed routes; add direct pinned Zod ownership to the CLI package; and enumerate every route/schema in the ordinary test gate.
- Validation: focused driver suite passes 5 with the physical case explicitly skipped; CLI/root typecheck, lint/exports, planning, scaffold, and frozen offline install pass. The full unit gate had two unrelated load failures; both exact files pass in isolation. The clean committed run passed Fastify registration, remote enable, private QR claim, paired checkpoint, and fragment-free reload before stopping at the independent `BUG-013` privacy guard.
- Closed by: commit `3528c6c`; physical progression evidence from the subsequent clean run.

### BUG-013 Activity Inspection Exposes Pairing Fragment

- Symptom: the phone successfully pairs and reloads fragment-free, then `requireChromeForeground` reads `dumpsys activity activities`; ActivityManager includes Chrome's original fragment-bearing launch intent, and the harness's protected-value guard aborts before retaining or logging it.
- Impact: the strict run cannot continue after a valid scan, and repeated runs unnecessarily require another human pairing action.
- Route: small bugfix; the frozen privacy contract already forbids the fragment in ADB output and evidence.
- Related requirements: `NFR-005`, `NFR-013`, `SFR-006`, `SFR-007`.
- Affected / owning task: validation harness in `IFC-V1-079`.
- Blocks: clean committed physical Android acceptance rerun.
- Root cause: ActivityManager task inspection was chosen to prove Chrome foreground state without accounting for retained launch intents. Scrubbing browser history prevents network/history leakage but does not rewrite Android's task intent record.
- Fix: replace ActivityManager inspection with the fixed `dumpsys window displays` argv, accept exactly one bounded Chrome `mCurrentFocus` component, and reject URI-bearing, oversized, null, duplicate, or non-Chrome output.
- Validation: the ordinary driver suite passes 6 with the physical case explicitly skipped; root typecheck, lint/exports, planning, scaffold, live bounded URI-free WindowManager output, and the full unit gate (1,858 passed, 27 explicit skips) pass. The strict no-retry Android run then passed pair/reload, lock/local unlock, profile-away/return, SSE recovery, self-revoke, evidence publication, and exact cleanup with all 12 phone rows terminal.
- Closed by: commit `b4078b6`; `artifacts/ifc-v1-079-device/evidence.json` and the inspected four-screen physical evidence.

### BUG-014 Vulnerable Fastify URI Parser Transitively Locked

- Symptom: `pnpm audit --prod` reports `GHSA-v2hh-gcrm-f6hx` twice for `fast-uri` 3.1.3 and 4.1.0 through Fastify's AJV compiler and `fast-json-stringify` paths.
- Impact: the committed production dependency graph contains high-severity host-confusion vulnerabilities and cannot meet release supply-chain acceptance.
- Route: release blocker discovered during `FE-V1-010` dependency validation; the expected patched versions and affected paths are explicit.
- Related requirements: `NFR-005`, `NFR-010`, `PR-007`.
- Affected / owning task: shared production dependency graph; closure evidence is recorded with `FE-V1-010` because that audit discovered and fixed it.
- Blocks: resolved in the same implementation unit before task closure; no release or frontend task remains blocked by this advisory.
- Root cause: the frozen lock predated the advisory and retained vulnerable patch releases permitted by Fastify's current dependency ranges.
- Fix: exact workspace overrides replace only `fast-uri` 3.1.3 with 3.1.4 and 4.1.0 with 4.1.1; no Fastify API, source fallback, or broad dependency upgrade was introduced.
- Validation: dependency tree shows only 3.1.4/4.1.1; `pnpm audit --prod` reports no known vulnerabilities; offline frozen install, 1,870 unit, 240 contract, 27 integration, runtime-boundary, deterministic package acceptance, browser, typecheck, lint, scaffold, and planning gates pass. Both patched releases are BSD-3-Clause.
- Closed by: implementation commit `9b095ad`; evidence in `artifacts/fe-v1-010-phone-shell.md`.

### BUG-015 Selected Mobile Access Contract Is Not Browser-Producible

- Symptom: `selectedHostAccessSchema` permits implicit `loopback_local` browser writes and requires admitted request provenance/source key, device label, full runtime compatibility, and a connected session stream for Mission Control.
- Impact: the selected browser API cannot produce that state. `DEC-024` makes safe loopback browser GETs unpaired/read-only; access/host/session routes intentionally omit proxy source keys, access bootstrap omits device labels and full compatibility, and Mission Control owns no session stream. A screen consuming the old fixture would have to fabricate authority or private/unavailable fields.
- Route: closed inside `FE-V1-025`; the coordinator is the first live consumer and establishes route-backed state before screens consume it.
- Related requirements: `IR-005`, `IR-006`, `IR-008`, `UX-009`, `DEC-024`, `DEC-027`.
- Affected / owning task: selected-mobile fixture contract from completed foundation work; live replacement and regression evidence in `FE-V1-025`.
- Blocks: resolved; `FE-V1-011`, `FE-V1-012`, and `FE-V1-013` are ready on the route-backed contract.
- Root cause: the normalized visual fixture predated the final browser authentication/status composition and was not re-audited against exact route outputs after local-admin safe-GET authority was removed.
- Fix: `selectedHostAccessSchema` now uses `loopback_read`, device id only, route-derivable access/lock/read/write/error fields, optional current remote ingress, and no provenance/source key, label, detailed compatibility, or global stream fabrication. Session Detail owns its stream and compatibility remains with its exact runtime source.
- Validation: selected-mobile regressions, 33 direct coordinator cases, three real loopback/admitted-Serve Fastify/SQLite cases, 243 contract tests, 35 integration tests, full workspace/static/package/install/audit gates, and manual privacy/residue review pass.
- Closed by: `FE-V1-025` implementation `888abf1`; evidence in `artifacts/fe-v1-025-shell-connection-state-coordinator.md`.

### BUG-016 Newly Published Production Dependency Advisories

- Symptom: the final `FE-V1-020` `pnpm audit --prod` gate newly reported four high and one moderate advisory across `find-my-way` 9.6.0, `react-router` 8.2.0, `@fastify/static` 9.3.0, and transitive `brace-expansion` 5.0.7.
- Impact: the committed production graph failed the zero-known-vulnerability release gate; the Fastify findings affect request routing/static boundaries and cannot be waived as unrelated UI risk.
- Route: release blocker discovered and resolved in the active `FE-V1-020` hardening unit; requirements and architecture remain unchanged.
- Related requirements: `NFR-005`, `NFR-010`, `PR-007`.
- Affected / owning task: shared server/web production dependency graph; closure evidence is recorded with `FE-V1-020` because its required supply-chain gate discovered the advisories.
- Blocks: resolved before physical Android acceptance and task closure; no downstream task remains blocked by these advisories.
- Root cause: exact direct versions and the frozen lock predated `GHSA-c96f-x56v-gq3h`, `GHSA-qwww-vcr4-c8h2`, `GHSA-83w8-p2f5-377r`, `GHSA-mh99-v99m-4gvg`, and `GHSA-8pvw-jcv7-9cmj`.
- Fix: upgrade exact `@fastify/static` to 10.1.2 and `react-router` to 8.3.0; pin only vulnerable permitted transitives to `find-my-way` 9.7.0 and `brace-expansion` 5.0.8 through workspace overrides; adapt the static header callback to the patched plugin's `FastifyReply.header` contract. No route, CSRF, static-path, cache, or fallback policy changed.
- Validation: dependency enumeration contains only the four patched versions; all are MIT licensed; static-boundary/Fastify 11, web 214, unit 2,064 with 28 explicit skips, contract 243, integration 36, Chromium 18, typecheck, lint/exports, scaffold, runtime boundary, deterministic package acceptance with 6,445 entries, frozen offline install, and zero-known-vulnerability production audit pass.
- Closed by: the current dependency-hardening unit; final `FE-V1-020` evidence records its pushed commit.

### BUG-017 Production Compatibility State Is Not Observable Or Reachable

- Symptom: production gives the reconnect controller `codexBindingDescriptor.codex_version` as the observed version, and initial incompatibility rejects application startup before Fastify/static routes listen.
- Impact: a protocol-compatible drifted binary can be recorded as the reviewed 0.144.0 value, while a proven incompatible runtime leaves no dashboard from which the phone can see the version or update-required state. `FE-V1-035` cannot truthfully implement its required production behavior.
- Route: critical release blocker and backlog architecture fix. The defect crosses process observation, runtime admission, durable compatibility/session truth, application/listener startup, selected host status, and later UI recovery.
- Related requirements: `FR-017`, `IR-006`, `IR-008`, `NFR-005`, `NFR-010`, `NFR-012`.
- Affected / owning task: completed production composition/lifecycle and health-route behavior from `IFC-V1-039`, `IFC-V1-082`, `IFC-V1-083`, and `IFC-V1-086`; corrective leaf `IFC-V1-087`.
- Blocks: resolved for `FE-V1-035` and `IFC-V1-091`; release acceptance remains downstream of those tasks.
- Root cause: smoke setup is the only real `codex --version` owner; the initialize user agent reflects HostDeck's client identity and is not independent server-version evidence; compatibility persistence occurs only after successful reconciliation; and listener startup requires runtime-ready application phase.
- Fix: boundedly probe the configured binary, skip runtime start/attachment for a valid version mismatch, persist current incompatibility, seal durable projections disconnected, permit only the proven diagnostic-ready listener with mutation admission closed, and expose a strict sanitized compatibility projection through the existing protected host-status response.
- Validation: `PCD-01` to `PCD-24` pass through hostile fake ports, 2,393 unit/28 skips, 245 contract, 36 integration, 516 web, full static/install/audit/package gates, clean committed exact/mismatched real process/listener smokes, durable boundary/privacy inspection, and zero residue.
- Closed by: `IFC-V1-087`; criteria `0fa8b18`, implementation `ceb339e`, and evidence in `artifacts/ifc-v1-087-production-compatibility-diagnostics.md`.

### BUG-018 Offscreen Quiet Session Misclassified As Load Failure

- Symptom: the first clean `FE-V1-090` physical run paired successfully and received one `200` response each from access, host status, and session list, but the harness reported that Mission Control did not load.
- Impact: valid production UI/network/auth behavior fails before the 39-interaction sequence because the physical oracle requires a quiet session to be visible in the first viewport, contradicting the required two-item `ACT NOW` hierarchy.
- Route: release blocker and small validation-harness fix inside the active physical hardening leaf; no product, UX, security, or architecture contract changes.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-06`, `MDH-21`, `MDH-24`.
- Affected / owning task: `FE-V1-090`.
- Blocks: the corrected clean no-retry physical acceptance and all downstream release tasks.
- Root cause: the dashboard fixture deliberately orders `release-approval` and `migration-input` in `ACT NOW`, while `physical-pairing-review` has no attention and appears below the fold. `openProductionMissionControl` used the quiet session as its universal first-viewport readiness node instead of the visible attention heading, and later target acquisition did not scroll.
- Fix: require every caller to declare either `dashboard_attention` or `single_session` initial-viewport truth; validate `ACT NOW` for the dashboard; retain the exact session check for single-session fixtures; and use bounded forward reveal before dashboard reload and prompt target acquisition.
- Validation: focused physical ledger/driver 29 passed with one device-gated case, typecheck, lint/exports, exact cleanup after the first failed attempt, and a regression for both initial-viewport modes pass. The next clean physical run reached paired Mission Control, rendered `ACT NOW`, issued the fragment-free reload, and failed only at the distinct collapsed-disclosure acquisition now owned by `BUG-019`.
- Closed by: implementation `9bc73ec`; final aggregate evidence remains owned by `FE-V1-090`.

### BUG-019 Collapsed Quiet Queue Cannot Be Reached By Scrolling

- Symptom: the clean `9bc73ec` physical run paired, rendered the required attention-first Mission Control viewport, issued a fragment-free reload, and reread sessions, then timed out trying to reveal `physical-pairing-review`.
- Impact: the aggregate phone run cannot open the selected session or execute the remaining dashboard interactions even though paired authority and protected reads remain healthy.
- Route: high release blocker and small production-accessibility plus validation-harness fix inside the active physical hardening leaf; no product scope, architecture, remote-ingress, or authorization contract changes.
- Related requirements: `NFR-004`, `PR-005`, `MDH-05`, `MDH-06`, `MDH-20`, `MDH-21`, `MDH-24`.
- Affected / owning task: `FE-V1-090`.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: with attention rows present, the production dashboard intentionally renders quiet sessions inside a closed native `details` element. The first `BUG-018` correction added bounded scrolling after reload, but collapsed content is absent from the Android accessibility tree and therefore cannot be revealed by any scroll count. The summary also lacked one stable stateful accessible name for deterministic device acquisition.
- Fix: give the production summary a count-preserving `Expand quiet sessions (n)` / `Collapse quiet sessions (n)` accessible name; after reload, boundedly reveal that unique control, verify one state-changing tap, measure its physical target, then boundedly reveal the now-expanded session. Browser and driver regressions freeze the labels and native keyboard transition.
- Validation: focused production component plus physical driver 56 passed with one device-gated case; the real Chromium reflow/keyboard/reduced-motion/contrast scenario passes the collapsed and expanded accessible names. Unit 2,910/29 intentional skips, contract 245, integration 36, web 932, typecheck, lint/exports (828 files/eight packages), scaffold (eight packages/22 scripts), planning (220 tasks/84 requirements/683 dependencies/one queued), and runtime boundary (619 production modules/22 externals) pass. The deterministic 619-source/1,245-output/6,231-entry package and exact four-project supported-browser matrix pass 76/76 after `BUG-020`. The next clean pushed `4b1316b` phone run reached reload and exposed the distinct Android native-summary mapping defect now owned by `BUG-021`; exact failure cleanup passed.
- Closed by: pending the corrected `FE-V1-090` physical artifact and implementation commit.

### BUG-020 Supported-Browser Mutation Diagnostics Race Request Settlement

- Symptom: the full package matrix reached valid terminal UI after an intercepted `202` mutation, then Chromium reported `net::ERR_ABORTED`; a focused rerun could pass the same scenario and later report the same signal on a different valid mutation.
- Impact: release-package validation was scheduler-dependent and could either miss or reject an intercepted-route diagnostic after the application parsed the valid response.
- Route: high release blocker and small validation-harness fix in the already required supported-browser gate; no product, API, security, or browser-support contract changes.
- Related requirements: `NFR-004`, `PR-005`, `PR-011`, `BRM-03`, `BRM-10`, `BRM-16`, `BRM-21`, `BRM-24`, `MDH-02`, `MDH-24`.
- Affected / owning task: completed matrix owner `FE-V1-040`; active package consumer `FE-V1-090`.
- Blocks: resolved for the corrected `FE-V1-090` package and physical acceptance.
- Root cause: `expectCleanBrowser` inspected captured HTTP and request-failure diagnostics before polling non-stream requests to settlement. Playwright-fulfilled routes can expose a valid response to the application while later recording its synthetic request as aborted, so the old ordering made inspection timing determine the result.
- Fix: settle all non-stream requests before inspecting diagnostics; admit an intercepted mutation abort only for the exact captured same-origin `/api/v1/` POST path, and only after each scenario has proved terminal UI, one request, request schema, and CSRF protection. Other mutation aborts and all unexpected diagnostics still fail closed.
- Validation: Biome and diff checks pass; focused Chromium phone passes 19/19; the deterministic package identity is `920ce6b7389f212acfc1a981b9c1e7e4dad7da5d2bdd8552fcdc2cff0111acba`; and the complete exact Chromium/Firefox phone/desktop matrix passes 76/76 with 316 bounded requests, 52 exact mutations, and zero unexpected diagnostics.
- Reopened by: the `FE-V1-090` matrix rerun exposed the same Playwright synthetic-response dual signal on `/api/v1/access/csrf` after seven valid Chromium-phone scenarios. The correction admits it only for one exact same-origin POST whose request body passes `selectedCsrfBootstrapRequestSchema`, whose security headers are exact, and whose same Playwright request object already emitted `200`; a real abort or ambiguous request still fails.
- Final validation: the mandatory proxy-reset guard passes, and the corrected exact Chromium/Firefox phone/desktop matrix passes 76/76 over content `55ee52d7cd834250e90532dfa6aee5583a5fe6626af2923e95ebb3cd997a82c3` with 316 bounded requests, 52 exact mutations, zero unexpected diagnostics, four hash-bound reports, and exact server/root/output cleanup.
- Closed by: the republished `artifacts/fe-v1-040-supported-browser-interaction-matrix/` evidence and this `FE-V1-090` implementation commit.

### BUG-021 Android Native Summary Drops The Stateful Quiet-Queue Name

- Symptom: the clean pushed `4b1316b` physical run paired and reloaded successfully, but Android UIAutomator could not find `Expand quiet sessions (1)` even after four bounded forward reveals.
- Impact: the phone cannot deterministically acquire and measure the collapsed quiet-queue control, so the aggregate interaction sequence cannot open its selected session.
- Route: high release blocker and small cross-device production-accessibility fix inside `FE-V1-090`; no product scope, visual direction, API, remote-ingress, or authorization change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-05`, `MDH-06`, `MDH-15`, `MDH-20`, `MDH-21`, `MDH-24`.
- Affected / owning task: `FE-V1-090` and the quiet-queue production surface from `FE-V1-011` / `FE-V1-039`.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: Chromium's desktop accessibility tree honors `aria-label` on native `summary`, but Android Chrome's physical hierarchy exposes the same clickable element as `text="QUIET1"`, `hint="QUIET 1"`, and an empty `content-desc`. The physical driver correctly refuses to infer state or tap a text fragment with sub-target geometry.
- Fix: retain the approved `QUIET`, count, chevron, 44 CSS-pixel target, collapsed behavior, and default-open rule behind an explicit full-width button with exact stateful `aria-label`, `aria-expanded`, and `aria-controls`; keep the controlled list present but explicitly hidden from both layout and accessibility while collapsed.
- Validation: an isolated non-private phone probe maps the explicit button to `android.widget.Button` with exact `content-desc="Expand quiet sessions (1)"`; component plus Android-driver tests pass 56 with one intentional device skip; all three Mission Control Chromium scenarios pass across five viewports, keyboard Enter, reduced motion, reflow, contrast, exact names/states, and controlled-list visibility; and ten refreshed screenshots have no unresolved visual drift. Typecheck, Biome/exports (828 files/eight packages), scaffold (eight packages/22 scripts), planning (220 tasks/84 requirements/683 dependencies/one queued), runtime boundary (619 modules/22 externals), web 932, unit 2,911/29 intentional skips, contract 245, and integration 36 pass. Two builds produce identical 619-source/1,245-output/6,231-entry package `f1d2f387dcc2de4bc66f72bae399aec0d7a64a15efb1ba1a18f739b6b03d1c1f` with web `67d9295b4b8cd0ed57b65165ced86f8914d9c0785f87a2687cb0b39d2162444a`; the exact four-project matrix passes 76/76 with 316 requests and 52 mutations. Corrected physical acceptance remains pending.
- Closed by: pending the corrected `FE-V1-090` package, physical artifact, and implementation commit.

### BUG-022 Android Session Link Exposes Only Child Text Geometry

- Symptom: the clean pushed `23c8fc3` physical run paired, reloaded, found and expanded the corrected quiet queue, then measured `physical-pairing-review` as only `178.1x21` CSS pixels and failed before opening Session Detail.
- Impact: Android can tap only a text-fragment accessibility node instead of one deterministic whole-row target, so the aggregate phone run cannot prove the required 44 CSS-pixel session target or continue through the remaining dashboard interactions.
- Route: high release blocker and small cross-device production-accessibility fix inside `FE-V1-090`; no product scope, visual direction, API, remote-ingress, or authorization change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-05`, `MDH-06`, `MDH-07`, `MDH-15`, `MDH-20`, `MDH-21`, `MDH-24`.
- Affected / owning task: `FE-V1-090` and the whole-row Mission Control navigation surface from `FE-V1-011` / `FE-V1-039`.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: the production anchor has a valid 96 CSS-pixel box and passes browser geometry gates, but without an explicit accessible name Android Chrome exposes the session name as a separate glyph-bounded text node. The physical driver correctly measures the semantic node it can acquire instead of assuming the parent anchor's DOM geometry.
- Fix: give both Mission Control and retained-navigation row links the canonical session name as an explicit accessible name, preserve native link semantics and the approved full-row visual target, and require every physical pairing, prompt, recovery, TalkBack, archive, and dashboard path to acquire that exact Android content description. A realistic hierarchy regression distinguishes the full named target from its child text fragment.
- Validation: production component plus Android-driver checks pass 57 with one intentional device skip. Typecheck, static/scaffold/planning/runtime gates, web 932, unit 2,912/29 intentional skips, contract 245, integration 36, and all 175 Chromium shell scenarios pass. The three Mission Control scenarios prove exact names, whole-row and disclosure geometry, responsive layout across five viewports, keyboard transitions, reduced motion, reflow, contrast, and unchanged captures. Two deterministic builds match; the repinned exact Chromium/Firefox phone/desktop matrix passes 76/76 with 316 bounded requests, 52 exact mutations, zero unexpected diagnostics, and exact cleanup. Corrected physical acceptance remains pending.
- Closed by: pending the corrected `FE-V1-090` package, physical artifact, and implementation commit.

### BUG-023 Supported-Browser CONNECT Reset Terminates HTTPS Proxy

- Symptom: the repinned package matrix passed 75 cases across Chromium phone/desktop and Firefox phone, then the HTTPS proxy emitted an unhandled client-socket `read ECONNRESET`, exited, and made the final Firefox desktop cookie navigation fail with `NS_ERROR_PROXY_CONNECTION_REFUSED`.
- Impact: a normal browser tunnel reset can terminate task-owned validation infrastructure and falsely fail otherwise valid package behavior. The exact 76-case package identity cannot be published or consumed by the physical gate.
- Route: high release blocker and small validation-infrastructure fix in the existing supported-browser matrix; no product, browser-support, HTTPS-cookie, API, or remote-ingress contract change.
- Related requirements: `NFR-004`, `PR-005`, `PR-011`, `BRM-02`, `BRM-03`, `BRM-15`, `BRM-21`, `BRM-23`, `BRM-24`, `MDH-02`, `MDH-24`.
- Affected / owning task: completed matrix owner `FE-V1-040`; active package consumer `FE-V1-090`.
- Blocks: exact package evidence, corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: the CONNECT bridge handled only the upstream socket's first error. Once piping began, a Firefox-side reset emitted `ECONNRESET` on the accepted client socket with no listener, so Node applied its default fatal `error` behavior and terminated the shared proxy process.
- Fix: persistently handle errors on every accepted HTTPS/tunnel socket and every outbound tunnel socket, destroy each peer when the other closes, refuse to establish a tunnel after the client is gone, and retain bounded pre-connect `502` behavior. Run a focused guard before every matrix that establishes and resets twelve CONNECT tunnels with TCP RST, proves the process remains alive, then completes a real tunneled TLS request.
- Validation: the focused Node regression passes before the matrix by establishing and forcibly resetting twelve CONNECT tunnels, proving the proxy remains alive, and completing one real tunneled TLS request. Static checks and deterministic package acceptance pass. The exact Chromium/Firefox phone/desktop matrix then passes 76/76 with 316 bounded requests, 52 exact mutations, zero unexpected diagnostics, and four hash-bound reports; ports 4175 to 4177, Playwright output, and the task-owned temporary root are absent afterward.
- Closed by: the republished `artifacts/fe-v1-040-supported-browser-interaction-matrix/` evidence and this `FE-V1-090` implementation commit.

### BUG-024 Physical Session Detail Does Not Establish Its Live Stream

- Symptom: after the earlier ambiguous timeouts, the clean pushed `74a6203` run reported one detail GET, one stream request, stream status `500`, zero opened/active/aborted/explicit subscribers, one source-open/source-failed outcome, and rendered `Live activity stopped`.
- Impact: the aggregate phone sequence cannot proceed because Session Detail requires one current replay-to-live SSE owner before later prompt, approval, recovery, and exact cleanup checks are valid.
- Route: high release blocker and bounded physical diagnosis inside `FE-V1-090` before selecting a browser, server, or harness root fix; no visual, API, authorization, ingress, or retry-contract change yet.
- Related requirements: `NFR-004`, `PR-005`, `MDH-05`, `MDH-06`, `MDH-07`, `MDH-15`, `MDH-20`, `MDH-21`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical Android target acquisition.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Ruled out: Tailscale transport, Chrome request dispatch, target naming, 44 CSS-pixel geometry, Chrome page containment, native clickability, tap dispatch, client navigation, detail routing, paired read authority, the detail GET, browser stream initiation, and subscriber cancellation are proven.
- Root cause: `PhysicalPromptHandoffService` copied dashboard replay events without the production contract's recursive immutability and always declared `truncated=false`. The dashboard replay begins with an intentional retention `replay_boundary`, which requires deep-frozen event data and `truncated=true`; the strict production subscriber rejected the contradictory fixture before admission and the selected SSE route correctly returned `500`.
- Fix: normalize every initial and published physical projection through the selected event schema, recursively freeze the normalized data, and derive `truncated` from whether the replay's first event is a boundary. Keep the strict production subscriber unchanged and retain the private-free lifecycle diagnostics.
- Validation: a direct regression opens all five dashboard boundary/complete/redacted/approval/runtime events through the real subscriber contract, drains replay in order, proves one live subscriber remains, then closes it exactly with no observed failure. The focused driver/fixture suite passes 28 with one intentional physical skip and CLI typecheck passes. The clean pushed `bcdcd1e` run established one live subscriber, drained replay, and opened the boundary and complete-event diagnostics exactly once before a separate occluded-control defect stopped the sequence.
- Closed by: replay normalization and handoff implementation `1ccf27e`, plus clean pushed physical proof on `bcdcd1e`.

### BUG-025 Failure Cleanup Races Lifecycle Observation

- Symptom: after the first `BUG-024` assertion, cleanup reported that selected-lifecycle remote disable failed. The ownership-safe fallback subsequently removed Serve, and independent inspection proved absent Serve plus fully restored device and process state.
- Impact: an expected assertion failure can add a second cleanup failure and rely on fallback ownership proof even though the selected lifecycle remains available, obscuring the primary defect and weakening exact cleanup evidence.
- Route: high release blocker and small physical-harness cleanup correction inside `FE-V1-090`; no remote-ingress, profile-selection, ownership, or product behavior change.
- Related requirements: `NFR-004`, `PR-005`, `PR-011`, `MDH-02`, `MDH-09`, `MDH-16`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical Android cleanup.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: the normal success path explicitly waits for one fresh lifecycle observation with zero active control operations before `remote disable`; the failure path restored the dedicated profile and immediately issued disable without the same settle gate.
- Fix: retain the exact selected lifecycle for cleanup and require one new idle observation after profile restoration before calling its CLI-owned disable operation. Keep the ownership-safe direct-manager fallback as a separately observable last resort.
- Validation: the pushed `824fbec` failure run reported only the primary Session Detail assertion, with no cleanup error. Independent post-run inspection proved Serve `{}`, restored captured Wi-Fi/mobile-data/stay-awake and accessibility settings, stopped Chrome, no ADB forward/reverse tunnel, no acceptance process, no partial evidence, and no worktree change.
- Closed by: pushed implementation `824fbec` plus its exact physical failure-cleanup inspection.

### BUG-026 Initial Detail Gate Assumes Current Viewport Visibility

- Symptom: the clean pushed `1ccf27e` run established one live Session Detail subscriber and rendered the writable composer, then timed out on an unscrolled replay-boundary label. The clean pushed `a3ad76f` run proved replay drain and then timed out on unscrolled top-context value `Current`.
- Impact: the aggregate sequence stops before its existing event-diagnostic workflow can scroll to, open, capture, and inspect the boundary row.
- Route: high release blocker and small physical-harness correction inside `FE-V1-090`; no product UI, event, SSE, authorization, ingress, or retry-contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-07`, `MDH-14`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical Session Detail evidence sequencing.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: `waitForAndroidUiText` observes only the current Chrome accessibility hierarchy and never scrolls. The docked composer remains visible when the route retains Mission Control's vertical offset, while top Session Context can be above the viewport and replay rows can be below it. The event diagnostic already owns bounded forward row reveal, but the top capture had no corresponding backward reveal owner.
- Fix: require one active subscriber and zero remaining server replay events, then perform a bounded backward reveal of the fully visible `Current` stream value before the top-of-detail capture. Keep the existing bounded forward row reveal, exact event read, limitation check, and screenshot as each replay row's physical evidence owner.
- Validation: direct five-event boundary replay and clean pushed physical live/writable/replay-drain transitions prove event production and stream ownership. The clean pushed `bcdcd1e` run backward-revealed fully visible top `Current` truth, captured the detail, then forward-revealed and opened the boundary and complete rows exactly once before a separate third-row tap defect stopped the sequence.
- Closed by: bounded initial-detail sequencing `a3ad76f`, viewport reveal `bcdcd1e`, and the clean pushed physical `bcdcd1e` run.

### BUG-027 Event Diagnostic Accepts An Occluded Action

- Symptom: the clean pushed `bcdcd1e` run passed top context plus the boundary and complete-event diagnostics, then found the third event's nearest `View event details` action near the bottom of Android's hierarchy. The action lay under the fixed session-control dock, so the one allowed tap produced neither an event read nor the `Turn event` dialog.
- Candidate result: the clean pushed `5d8b014` run rejected the occluded pair as designed, but its bounded reveal never admitted the third row. The generic timeout did not identify whether label movement, nearest-action distance, sticky-header geometry, one dock control, or the derived swipe lane differed on the real hierarchy.
- Diagnostic result: the clean pushed `351891b` run recorded one initial blocked state with valid page/content/gesture geometry, unique app controls, label below the dock, and its nearest clickable action within the bounded row distance. One derived swipe moved both nodes into the safe app band and preserved all app-control geometry. At that point only the Chrome toolbar-based page selector became invalid, so target admission and further swipes stopped. The app-header upward shift is consistent with Chrome collapsing its browser toolbar during page scroll.
- Compositor-fix result: the clean pushed `7027de6` run admitted the target after toolbar collapse and issued one tap. The admitted action was below the redaction notice and then disappeared, while `Turn event` never rendered. Production `SessionTimelineItem` order places `EventDiagnosticsAction` in the row header before its title, body, and content notice, so an action below that notice cannot own the target row and belongs to following activity.
- Impact: the aggregate sequence stops before redaction, approvals, writes, recovery, utility controls, profile switching, TalkBack, revocation, privacy, and cleanup evidence can execute.
- Route: high release blocker and task-local physical-driver correction inside `FE-V1-090`; no product UI, event, authorization, ingress, or retry-contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-07`, `MDH-08`, `MDH-14`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical event-row acquisition and one-tap integrity.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: three defects composed in sequence: hierarchy presence did not exclude the fixed dock; interaction geometry reused a screenshot selector invalidated by Chrome toolbar collapse; and geometric-nearest action selection could cross from a lower body/content notice to the following row's header action. The first two are corrected. The remaining association must preserve production hierarchy order as well as bounded geometry.
- Harsh success criteria: acquire one unique target label; select only the last clickable details action preceding that label in the same hierarchy snapshot; require the action center at or above the label center and within a bounded row distance; reject a closer following-row action and ambiguous/missing targets; require unique sticky back plus four dock controls; derive interaction bounds from one stable compositor and app overlays; require both pair members inside the 24-device-pixel-inset region; scroll forward no more than four times; keep screenshot cropping strict; issue one tap only after admission; and retain exact one-read plus expected-heading proof.
- Validation required: direct regressions must accept the bounded preceding header action even when a following-row action is geometrically closer, and reject top/bottom occlusion, incomplete overlays, ambiguous label, missing preceding action, and excessive preceding distance. Focused driver tests, CLI typecheck, static/planning checks, and a clean pushed no-retry physical run must pass with exact cleanup.
- Fix: compositor/app-overlay interaction geometry and strict screenshot separation remain. Action association now selects only the last clickable details action preceding the unique target label in the same hierarchy snapshot, requires its center at or above the label within 480 device pixels, and applies the same inset-content admission to both nodes. The private-free summary records the admitted owner separately from the geometrically nearest decoy.
- Validation: the direct matrix admits the bounded preceding action while rejecting a geometrically closer following-row decoy, missing preceding action, excessive distance, top/bottom occlusion, incomplete overlays, and ambiguous label. Focused 29/one intentional physical skip, CLI typecheck, lint across 829 files, package exports, and the planning graph pass. The clean pushed `c5925c2` run opened exact `Turn event`, verified `Content redacted`, and reached capture before a separate collapsed-toolbar screenshot defect stopped the sequence.
- Observability: on target-acquisition timeout, retain at most six changed summaries containing only known synthetic-node counts, bounded coordinates, clickability, Chrome page/content regions, gesture coordinates, and admission state. Never retain raw hierarchy XML, origin/profile/account data, pairing material, prompt/event payloads, or arbitrary labels.
- Closed by: ordered association implementation `c5925c2` and its clean pushed physical run.

### BUG-028 Screenshot Selector Rejects Collapsed Chrome Toolbar

- Symptom: after the clean pushed `c5925c2` run opened and verified the redacted Turn diagnostic, screenshot capture failed at `selectChromePageViewport` because the collapsed Chrome toolbar no longer appeared in Android's accessibility hierarchy.
- Impact: valid scrolled dialog and downstream physical states cannot be captured, so the aggregate evidence set cannot complete even though interaction and privacy preconditions remain available.
- Route: high release blocker and task-local screenshot-state correction inside `FE-V1-090`; no product UI, ingress, pairing, event, authorization, or retry-contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-07`, `MDH-14`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` private-free physical screenshot viewport selection.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: `selectChromePageViewport` requires exactly one toolbar and one compositor. Android omits the toolbar node after Chrome collapses it during a page scroll; the unique bounded compositor remains and the existing private-material scan can still validate that region.
- Harsh success criteria: accept exactly two explicit states: one valid expanded toolbar crops below its bottom, and zero toolbar nodes crops the unique bounded compositor; reject duplicate toolbar/compositor nodes and present-but-malformed toolbar geometry; keep dimension bounds; scan every intersecting semantic node for origin, hostname, forbidden device values, and registered secrets before writing any screenshot; preserve exact pixel cropping; and cover both states directly.
- Validation required: expanded, absent, malformed, duplicate, private-content, and exact-crop regressions; focused driver tests; CLI typecheck; lint/static/planning checks; and clean pushed no-retry physical proof with exact cleanup.
- Fix: page selection now first requires one bounded compositor. Zero toolbar nodes is the explicit collapsed state and returns that compositor; one toolbar must satisfy the existing expanded geometry and crops below its bottom; more than one toolbar or compositor and any present malformed toolbar fail closed. The existing intersecting-node private-material scan and exact pixel crop remain unchanged.
- Validation: direct tests prove expanded crop, absent-toolbar compositor crop, private origin rejected from the collapsed compositor, private toolbar text excluded from the expanded crop, malformed/duplicate toolbar rejection, duplicate compositor rejection, and exact output pixels. Focused 29/one intentional physical skip, CLI typecheck, lint across 829 files, package exports, and the planning graph pass. The clean pushed `ffd5a1d` run captured boundary, complete-message, and redacted-Turn dialogs through collapsed Chrome and reached approval control before a separate occluded-action defect stopped the sequence.
- Closed by: expanded/collapsed screenshot implementation `ffd5a1d` and its clean pushed physical run.

### BUG-029 Approval Review Is Occluded By Session Dock

- Symptom: the clean pushed `ffd5a1d` run reached pending approval, found `Review & approve` at the bottom beneath the fixed session-control dock, and issued one tap. The control remained present and `Approve elevated request?` never opened.
- Impact: approval decision, prompt writes, stream recovery, all utility controls, profile switching, TalkBack, revocation, privacy, and final cleanup evidence remain unreachable.
- Route: high release blocker and task-local physical-driver correction inside `FE-V1-090`; no product approval, UI layout, authorization, ingress, or retry-contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-08`, `MDH-14`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` inline approval control acquisition and one-tap integrity.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: the approval sequence reveals the row title, then reacquires `Review & approve` with presence-only semantics. The fixed session dock can cover the later action even while Android retains it in the hierarchy.
- Harsh success criteria: provide one reusable session-content selector/reveal over the existing bounded compositor, sticky back, and four-dock-control region; require a unique exact node fully inside that inset region; derive both forward and backward gestures entirely inside the same region; scroll at most four times; fail closed on absent/ambiguous overlays or target; preserve one tap with no retry; and prove the approval confirmation appears only after a safely admitted action.
- Validation required: direct safe/occluded/ambiguous/missing-overlay and forward/backward gesture checks; focused driver tests; CLI typecheck; lint/static/planning checks; and clean pushed no-retry physical proof with exact cleanup.
- Fix: add a reusable session-content selector and bounded reveal over the existing compositor/sticky-header/four-control-dock region. It admits one exact node only when fully inside that inset region and optionally clickable, drives at most four forward or backward gestures entirely inside the region, and fails closed otherwise. Pending approval now uses this owner for `Review & approve` before its unchanged one-tap transition.
- Validation: direct checks accept the safely positioned clickable review action and reject a dock-occluded action, duplicate target, and missing dock geometry. Forward and backward gestures use exact reversed coordinates inside the content region. Focused 29/one intentional physical skip, CLI typecheck, lint across 829 files, package exports, and the planning graph pass. The clean pushed `30f55f4` run opened exact elevated confirmation, verified its reason, and captured it before a separate confirmation-action identity defect stopped the sequence.
- Closed by: reusable safe-content reveal `30f55f4` and its clean pushed physical run.

### BUG-030 Approval Confirmation Requires Exact Action Identity

- Symptom: the clean pushed `30f55f4` run opened and captured `Approve elevated request?`, then timed out waiting for `Approve once` through Android's `text` field.
- Impact: the pending approval cannot be submitted, blocking every later write, recovery, utility, profile, accessibility, revocation, privacy, and final cleanup gate.
- Route: high release blocker and task-local Android semantic-acquisition correction inside `FE-V1-090`; no product approval, accessibility name, authorization, ingress, or retry-contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-08`, `MDH-14`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` approval confirmation action acquisition.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: the anonymous action fallback was based on a closed-sheet prompt-composer hierarchy, not a valid confirmation footer. Pushed `c8989d6` also proved the transition into confirmation still used a text-field-only title check and retained the review target across multiple hierarchy reads and a screenshot, despite the driver's semantic identity and current-geometry requirements.
- Candidate result: retain the explicit product label and `preventScroll` Cancel focus, require one exact semantic `Approve once` that is clickable and fully visible inside unique exact title/status/modal-reason context, and return no action for every anonymous or ambiguous hierarchy. Wait without swiping and tap once. No geometry-derived action identity, opaque-value read, nearest-node rule, coordinate constant, or retry is permitted.
- Harsh success criteria: admit one exact clickable semantic action fully inside the Chrome page only while the unique exact confirmation title, status, and one relational modal reason remain present; reject missing/duplicate context, anonymous footer controls, duplicate/non-clickable/out-of-page actions, containing-only labels, closed-modal background controls, and every ambiguity. Perform one tap with no retry and retain exact pending-request and responding-state assertions.
- Validation required: direct text-only, description-only, duplicate, missing, non-clickable, and out-of-page fixtures; focused driver tests; CLI typecheck; lint/static/planning checks; and clean pushed no-retry physical proof with exact cleanup.
- Fix: the dedicated selector keeps unique title/status plus one geometrically modal reason and returns only the exact semantic action. The disproven anonymous footer fallback is removed. Before the single review tap, the harness rechecks stable write authority, safely reacquires the row after capture, and recognizes one exact semantic confirmation title in either Android name field. Failure retains only private-free modal/stream diagnostics. The product keeps its explicit accessible label for browser/screen-reader semantics.
- Validation: direct fixtures cover text and accessibility-description title projection, duplicate-title rejection, exact semantic action success, anonymous/duplicate/missing/containing-only/non-clickable/out-of-page action rejection, relational modal context, and private-free context summaries. Component regression requires `{ preventScroll: true }` while preserving Cancel focus and origin restoration. Pushed `c8989d6` passed pairing, writable detail, stable write authority, safe row acquisition, and exact cleanup before stopping at the remaining transition check. Focused/static gates and corrected physical proof for the current candidate remain pending.
- Closed by: pending corrected physical evidence and implementation commit.

### BUG-031 Cleanup Can Retain Background Chrome

- Symptom: after the clean pushed `7481406` failure, launcher was foreground and Serve/tunnels/worktree were clean, but `pidof com.android.chrome` returned one background process. A manual force-stop removed it immediately.
- Impact: failure cleanup leaves phone process residue and cannot satisfy `MDH-22`/`MDH-24`, even when the primary assertion and other restoration checks are exact.
- Route: high release blocker and task-local physical cleanup correction inside `FE-V1-090`; no product, browser, pairing, ingress, or retained-deployment behavior change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-16`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical Android failure/success cleanup.
- Blocks: trustworthy no-retry physical evidence, retained phone deployment, and all downstream release tasks.
- Root cause: cleanup force-stops Chrome and then sends Home. On this Xiaomi launcher, that ordering can trigger or permit a delayed background Chrome restart after the immediate stopped observation.
- Harsh success criteria: send Home before force-stop; wait for exact stopped process state; require a second settled stopped observation; repeat the operation after profile/lifecycle/host/settings cleanup; preserve launcher foreground, no keyboard/tunnels, and all captured settings; report cleanup failure alongside the primary failure if absence cannot be proven; and reuse one helper for success and final paths.
- Validation required: direct cleanup ordering/settled-observation regression where practical, focused driver tests, CLI typecheck, lint/static/planning checks, and both failed-run plus successful-run physical absence proof.
- Fix: one frozen command plan now sends Home before force-stop. One helper waits for stopped state, waits a further second, requires stopped state again, and is reused in the success path, initial finalizer, and a final pass after profile/lifecycle/host/settings/tunnel cleanup.
- Validation: direct regression freezes and asserts Home-before-force-stop ordering. Focused/static gates passed before the clean pushed runs; `1d37ef1`, `306859b`, `d57c523`, `868b15a`, `0e1781a`, `43b27a5`, `de5b22b`, `6a1c8c9`, three separate `d11619a` executions, and `c8989d6` each ended with Serve absent, launcher foreground, Chrome stopped, dedicated profile selected, and clean worktree. Successful-run physical absence proof remains pending.
- Current evidence: the clean `cf3acba` Host-lock entry failure is the forty-seventh product-sequence post-mutation cleanup proof. One separate cellular preflight failure at `0fb3586` restored Wi-Fi, mobile data, Serve, profile, launcher, Chrome, tunnels, stay-awake, and worktree state before Chrome UI execution.
- Closed by: pending corrected physical evidence and implementation commit.

### BUG-032 Pairing Confirmation Timeout Lacks Bounded Evidence

- Symptom: the clean pushed `43b27a5` run passed the unrelated-network private-HTTPS preflight, entered and released the deliberate claim gate, consumed the pairing code, then timed out waiting for `Phone paired` before the approval candidate was exercised.
- Impact: the existing failure cannot distinguish a completed claim with failed CSRF bootstrap, a recognized product failure state, an unknown browser page, or a missing response; blind reruns would weaken the no-retry physical gate.
- Route: high release blocker and task-local physical observability correction inside `FE-V1-090`; no product, pairing, authorization, ingress, timeout, or retry behavior changes.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-03`, `MDH-16`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` production pairing entry and no-retry physical diagnosis.
- Blocks: diagnosis of the pairing stop, corrected aggregate physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: the confirmation waiter retained only a generic missing-label message. Request inspection counted claim and CSRF starts but not their response statuses, and the failure path retained no allowlisted startup-state projection.
- Harsh success criteria: retain claim and CSRF response statuses through the existing bounded status recorder; report only request/status counts, hardened-cookie presence, device/used-code counts, known proxy reason, and exact allowlisted pairing-state labels; classify all arbitrary hierarchy content as `unknown`; never emit the origin, fragment, token, device identifier, arbitrary UI text, or raw hierarchy; preserve the same single claim, timeouts, and no-retry behavior.
- Validation required: direct arbitrary-content privacy regression, focused physical-driver tests, CLI typecheck, lint/static/planning checks, and one clean pushed no-retry phone run that either advances or supplies enough bounded evidence for a root fix.
- Fix: the physical request inspector now records bounded claim/CSRF response statuses. Pairing-confirmation failure assembles a private-free diagnostic from those statuses, cookie issuance, database completion, proxy trust, and a frozen allowlist of product startup headings.
- Validation: direct arbitrary-content exclusion passes with exact diagnostic output; focused 31/one intentional physical skip, CLI typecheck, lint across 829 files, package exports, and the 220-task/84-requirement/683-dependency planning graph pass. The clean no-retry `de5b22b` run crossed the pairing confirmation and reached approval, while exact cleanup restored Serve, profile, Chrome, settings, launcher, and worktree state.
- Closed by: implementation `de5b22b` and its clean no-retry physical boundary proof.

### BUG-033 Approval Sequence Crosses Stream Reconnect

- Symptom: after the approval sheet opened, the action waiter observed no exact title, status, close control, or risk marker. A privacy-cropped screenshot showed Session Detail in reconnecting state, the sheet closed, and the disabled prompt composer occupying the geometry previously treated as a confirmation footer.
- Impact: the physical sequence cannot submit approval or reach later dashboard gates, and a geometry-only fallback could tap a non-approval control after authority loss.
- Route: high release blocker and task-local Android acceptance sequencing fix inside `FE-V1-090`; no product authorization, approval, reconnect, or UI contract changes.
- Related requirements: `NFR-002`, `NFR-004`, `PR-005`, `MDH-08`, `MDH-14`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` approval/prompt sequencing and fail-closed action acquisition.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: approval ran after three read-only event dialogs and reached the finite physical stream's reconnect boundary. The product correctly revoked write authority and closed the sheet, while the harness still attempted to identify an action from anonymous footer geometry.
- Harsh success criteria: run approval before read-only event diagnostics; before approval and prompt, require exactly one live subscriber, exactly one `Ready to send`, no reconnect/prompt-unavailable marker, and an unchanged stream-request count for at least two seconds; reset the stability window on any mismatch or new request; time out with private-free stream diagnostics; and never tap unless exact confirmation context plus one exact semantic action remain present.
- Validation required: direct ready, missing/duplicate-ready, zero/multiple-subscriber, both reconnect-marker, and prompt-unavailable cases; anonymous/closed-modal action rejection; focused driver tests; CLI typecheck; lint/static/planning checks; and one clean pushed no-retry physical run through approval, prompt, final cleanup, and accepted retained deployment.
- Fix: approval now runs immediately after writable Session Detail proof and before event diagnostics. A reusable two-second stable-write gate protects both approval and prompt, resets when the stream request count changes, and emits only the existing bounded private-free stream diagnostic on timeout. Approval action identity is exact-semantic only.
- Validation: direct boundary fixtures and focused component/driver 40/one intentional physical skip, CLI typecheck, lint across 829 files, package exports, and the 220-task/84-requirement/683-dependency planning graph pass. Pushed `c8989d6` crossed the new stable-write gate before the safely admitted review tap and restored all owned state after the subsequent transition-check failure. Corrected physical proof remains pending.
- Closed by: pending corrected physical evidence and implementation commit.

### BUG-037 Accepted Progress Masks Reconnecting Write Block

- Symptom: the clean pushed `0fb3586` run completed one prompt, disconnected the committed SSE stream, observed one retryable `source_failed`, held exactly one reconnect request, and blocked composer writes. Android never exposed `Session activity is reconnecting.` because the composer continued to render the earlier `Turn completed` result; the held request eventually reached the private Serve proxy timeout.
- Impact: users can see disabled write controls without the current reason, and the aggregate physical gate cannot prove or capture explicit reconnect truth before releasing the deterministic request.
- Route: high release blocker and local product-state correction inside `FE-V1-090`; the frozen prompt, connection, SSE, retry, authorization, architecture, and visual contracts remain unchanged.
- Related requirements: `NFR-002`, `NFR-004`, `PR-005`, `MDH-07`, `MDH-08`, `MDH-14`, `MDH-19`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical reconnect truth over the selected prompt composer.
- Blocks: corrected no-retry physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: `deriveStatus` gave every accepted-operation status priority over later availability failures. Admission correctly set `stream_reconnecting`, disabled the editor and send action, and retained the exact reason in `disabledReason`, but the rendered status surface uses `statusDetail`; the block therefore remained invisible.
- Harsh success criteria: after accepted or terminal turn progress, every non-turn availability failure must render its exact bounded reason and keep editor/send disabled; accepted waiting-for-input, waiting-for-approval, and unknown-turn states must retain their event-specific status; reconnect recovery must preserve and restore the accepted result without resend, retry, or operation loss; component output must expose the reason in the live status region.
- Fix: hidden and non-turn unavailable truth now precede accepted progress in status projection. Only accepted turn-owned input, approval, and unknown states retain their specialized event status. The accepted operation remains owned and reappears when current stream admission returns.
- Validation: direct projection covers completed-to-reconnecting priority and exact disabled truth; rendered component coverage proves accepted-to-reconnecting-to-current output, disabled controls, and accepted-result restoration. All 902 web cases, 24 projection server cases, 163 adjacent CLI/web cases with one intentional physical skip, CLI/web typechecks, and Biome pass. Corrected no-retry phone proof remains pending.
- Closed by: implementation `7858b61` and clean physical recovery proof at `b6c31f6`.

### BUG-038 Recovery Oracle Requires Offscreen Top Truth

- Symptom: the clean `f51ff84` run rendered and captured `Session activity is reconnecting.`, released the one held request, and reached `active=1`, `opened=2`, exact two stream requests, and no new stream failure. It then timed out waiting for `Current`, which is owned by top Session Context and was outside the current scrolled Android hierarchy.
- Impact: a valid same-request stream recovery cannot advance into stale/not-found, controls, profile, accessibility, revocation, privacy, and final cleanup checks even though server and browser reconnect ownership are correct.
- Route: high release blocker and local physical-oracle correction inside `FE-V1-090`; no product UI, SSE, retry, route, authorization, or architecture change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-07`, `MDH-08`, `MDH-14`, `MDH-19`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` post-reconnect physical UI proof.
- Blocks: corrected no-retry aggregate physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: `waitForAndroidUiText` observes only the current Chrome accessibility hierarchy. Earlier steps intentionally scroll Session Detail through event diagnostics while the prompt composer stays fixed; requiring the unrelated top `Current` label repeats the viewport-presence defect already eliminated from initial detail sequencing.
- Harsh success criteria: retain exact one-request/one-subscriber recovery counters; require one unique fixed-composer `Turn completed` status, no activity-reconnecting, composer-reconnecting, or prompt-unavailable marker, and one active subscriber; reject missing/duplicate status, zero/multiple subscriber, and every reconnect/unavailable marker; capture the restored composer state before advancing; never scroll, reload, reconnect, retry, or mutate to satisfy the oracle.
- Fix: replace the top-page text wait with one dedicated current-viewport completed-composer predicate and add a recovered-state screenshot. The server request/subscriber assertions remain unchanged and execute first.
- Validation: direct positive plus missing, duplicate, zero/multiple-subscriber, and three failure-marker cases pass with the full 32-driver matrix and one intentional physical skip; CLI typecheck and Biome pass. Corrected no-retry phone proof remains pending.
- Closed by: implementation `9b3d2c5` and clean physical recovered-composer proof at `b6c31f6`.

### BUG-039 Stale Oracle Requires Offscreen Notice

- Symptom: the clean `b6c31f6` run crossed recovered-composer capture, changed the deterministic detail fixture to stale, issued one fresh Session Detail request through Chrome refresh, then timed out waiting for `Showing stale session state`.
- Impact: valid stale, restored-current, not-found, controls, profile, accessibility, revocation, privacy, and final cleanup checks cannot advance even though stale response loading occurred.
- Route: high release blocker and local physical-oracle sequencing correction inside `FE-V1-090`; no product stale projection, refresh, route, SSE, retry, authorization, or architecture change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-07`, `MDH-19`, `MDH-21`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` stale Session Detail physical capture.
- Blocks: corrected no-retry aggregate physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: Chrome restores the previous Session Detail scroll position on full refresh. The stale notice renders below top Session Context, while the current hierarchy remains around the fixed composer and prior event position; a presence-only text wait cannot observe it.
- Harsh success criteria: after exactly one fresh stale-detail request, reveal one unique exact stale notice backward into the safe Session Detail content region using only the established compositor/sticky-back/four-control dock geometry; use at most four bounded swipes; reject missing, duplicate, occluded, or ambiguous notice and invalid overlays; capture only after safe admission; do not add reload, retry, request, mutation, or geometry fallback.
- Fix: replace the presence-only stale text wait with the existing bounded `revealPhysicalSessionContentNode` owner in the backward direction. Request-count and screenshot assertions remain unchanged.
- Validation: the existing direct selector matrix covers unique safe target, occlusion, duplication, missing overlay, and exact forward/backward gesture geometry. The full 32-driver matrix with one intentional physical skip, CLI typecheck, and Biome pass. Corrected no-retry phone proof remains pending.
- Closed by: implementation `d0d31f7` and clean physical stale/current/not-found proof at `c3fd9a4`.

### BUG-040 Android Intent Navigation Obscures Retained-Tab Authority

- Symptom: the clean `c3fd9a4` run safely revealed/captured stale state, refreshed back to current, navigated directly to a missing session, and captured `Session unavailable`. A second direct selected-session intent then rendered `Ready to send` without one provable selected-session subscriber. After the BFCache correction, the clean `8b3077b` run proved that opening the missing-session intent retained the original selected-session tab and its live subscriber. The clean `a59d0bc` run then proved that Android Back minimizes Chrome, closes the external tab, and drops the background stream instead of foregrounding the retained tab.
- Impact: the phone can present correct screens in multiple Chrome tabs while a same-document history oracle makes false assertions about stream closure or recovery. That ambiguity can hide zero, duplicate, or retained subscribers and cannot safely admit later controls or mutations.
- Route: high release blocker with one completed product browser-lifecycle correction and one local physical navigation/oracle correction inside `FE-V1-090`; the SSE retry, authorization, route, pairing, and architecture contracts remain unchanged.
- Related requirements: `NFR-002`, `NFR-004`, `PR-005`, `PR-011`, `MDH-02`, `MDH-07`, `MDH-19`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` direct Session Detail history/navigation recovery.
- Blocks: corrected no-retry aggregate physical acceptance, current package/browser refresh, retained phone deployment, and all downstream release tasks.
- Root cause: production `pagehide` closed startup and its coordinator exactly once, but a document restored from browser back-forward cache had no `pageshow.persisted` recovery owner. Separately, physical `openChromePath` uses an external Android `VIEW` intent, which opens an external-app Chrome tab while retaining the prior selected-session tab and stream. Chromium intentionally handles Back at the bottom of that external tab by moving Chrome to the background and closing the tab without showing the next tab; the former oracle incorrectly expected same-tab history or immediate retained-tab foregrounding.
- Harsh success criteria: retain the tested pagehide/BFCache lifecycle binding. Begin direct not-found proof with exactly one active selected-session subscriber and snapshot selected-detail reads, missing-detail reads, stream requests, opened subscribers, and active subscribers. Opening the missing-session intent must add exactly one missing-detail read and change no selected-session counter or subscriber. One Android Back action must close only the active stream while changing no request/open count; one normal Chrome launcher action must restore exact `Ready to send` truth only after one fresh selected-detail read, one fresh stream request/opened subscriber, and exactly one active subscriber. Reject every missing, lost, duplicate, or extra count, retry, or mutation; retain private-free diagnostics and exact cleanup.
- Fix: `6fa0579` adds the tested browser lifecycle owner. The corrected physical candidate separately counts missing-detail reads, freezes all selected-session authority counters, removes the second direct selected-session intent, proves exact background stream closure, relaunches Chrome through its normal launcher alias, and requires the BFCache reload's one fresh detail read plus one exact stream reopening.
- Validation: lifecycle coverage remains unit 2,922/29 intentional skips, web 904, adjacent web/CLI 142/one intentional physical skip, root static, and planning gates. The clean `27d84ec` run proved external-tab closure, normal launcher return, exactly one fresh detail read, one fresh stream/opened subscriber, and one active subscriber; it failed only because the prior oracle incorrectly rejected that required detail read. Updated direct/static and corrected no-retry phone proof remain pending.
- Closed by: product lifecycle implementation `6fa0579`, physical navigation corrections through `c2f834b`, and clean exact recovery proof in the `c2f834b` run; later aggregate stages remain pending under `BUG-041`.

### BUG-041 Plan Entry Oracle Requires Offscreen Terminal Status

- Symptom: the clean `c2f834b` run crossed the corrected not-found/BFCache sequence, completed Model and Goal controls, then found and measured one visible, clickable `/plan` trigger but timed out waiting for `Plan control ready`. The clean `5d2abfc` run used verified non-mutating entry and proved the trigger disappeared after the sheet opened; it could not reacquire the trigger because the terminal status remained outside the current Android hierarchy.
- Impact: the aggregate phone run cannot prove Plan current/staged/applied truth or advance into utilities, session actions, host/profile recovery, accessibility, revocation, privacy, cleanup-success, and retained deployment.
- Route: high release blocker and local physical non-mutating sheet-entry correction inside `FE-V1-090`; Plan contracts, product UI, reads, writes, retry policy, and architecture remain unchanged.
- Related requirements: `NFR-004`, `PR-005`, `MDH-11`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical `/plan` entry.
- Blocks: corrected no-retry aggregate physical acceptance, current package/browser refresh, retained phone deployment, and all downstream release tasks.
- Root cause: `Plan control ready` is rendered after the current/pending/execution rails, mode boundary, and two mode choices. The approved `ready-current-390x844.png` initial viewport shows `Default`, `No pending change`, and `No observed Plan execution` but not the terminal status below the fold. UIAutomator exposes only the current visible hierarchy, so the presence-only terminal wait misclassified an opened, current sheet as failed and the verified-tap owner correctly refused a second tap once the trigger was hidden.
- Harsh success criteria: use only the unique exact `/plan` description; retain target measurement and verified non-mutating entry. Admit current Plan truth only when the same hierarchy contains `Default`, `No pending change`, and `No observed Plan execution`; reject loading, any missing label, closed/reacquirable trigger after the transition deadline, duplicate request, or mutation. Require exactly one Plan GET after each initial/reopen entry; keep radio selection and protected submit strictly one-shot with one mutation and no automatic request retry. Do not scroll merely to satisfy the oracle.
- Fix: retain exact Plan GET counting and verified sheet entry, but replace the offscreen terminal-status predicate with the three approved initial-viewport current-state labels. Leave Plan choice, submit, release, apply, close, and later screenshot ownership unchanged.
- Validation: focused driver plus Plan state/component 105/one intentional physical skip cover positive, every missing label, loading, and the complete Plan matrix; CLI typecheck, Biome, and the 220-task/84-requirement/683-dependency planning graph pass. Corrected no-retry phone proof remains pending.

### BUG-049 Intentional Host Origin Blocks Private-Free Evidence

- Symptom: the clean `2b9680a` run opened Host from the approved Session Actions row, rendered current remote and permission truth, completed one read-only remote check, then rejected `fe090-32-host-access.png` because the app viewport contained the selected private origin.
- Impact: the aggregate phone run cannot retain Host, paired-device revocation, write lock, profile recovery, accessibility, privacy, cleanup, or deployment evidence even though the browser material is cropped and the product intentionally exposes the address as inert text.
- Route: high release blocker and task-local physical evidence correction inside `FE-V1-090`; no product UI, ingress, authorization, pairing, remote-control, or retry contract changes.
- Related requirements: `NFR-004`, `PR-005`, `PR-011`, `MDH-02`, `MDH-09`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` private-free physical Host evidence.
- Blocks: corrected no-retry aggregate physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: the screenshot gate scans the selected page viewport for the origin, hostname, and every registered device-private value, but has no representation for intentional product-owned origin text that must be removed from retained evidence. It therefore correctly fails closed but cannot produce a redacted Host capture.
- Harsh success criteria: preserve the unique bounded Chrome compositor and expanded/collapsed-toolbar crop rules; by default reject every origin, hostname, and registered private value. For explicitly marked Host captures only, require one exact `Host & access` identity landmark intersecting the selected page, one exact clickable `Back to session actions` control, and one exact visible `Read & write` permission value fully inside it; then admit at most two fully page-contained, non-clickable, resource-free Chrome web text nodes whose text equals the canonical full origin and whose description is empty. Reject hostname-only text, URL/edit controls, descriptions, links, unknown private values, missing/malformed/duplicate Host context, duplicate candidates, and invalid geometry. Expand each admitted text bound by a small bounded inset, merge overlaps, overwrite every admitted pixel with one opaque neutral color after crop and before an exclusive evidence write, and test that pixels outside each redaction remain unchanged.
- Validation required: direct exact-origin `View` and `TextView` cases under complete Host context; two-origin, default rejection, hostname-only, URL/edit, clickable, described, resource-owned, missing heading/back/permission, malformed back, duplicate context, unknown-private, excess-origin, out-of-page, malformed-redaction, exact-crop, exact-redaction, and unchanged-outside-pixel cases; focused physical-driver tests, CLI typecheck, Biome, planning checks, and one clean pushed no-retry phone run that advances beyond Host evidence with exact cleanup.
- Fix: private-free capture now has an explicit Host-only evidence option. It classifies every private node before capture, requires one exact intersecting Host identity plus unique structurally eligible back/permission context, admits no more than two exact canonical-origin Chrome web text nodes (`View` or `TextView`), expands and merges bounded redaction rectangles, crops the selected Chrome page, overwrites only translated redaction pixels with one opaque neutral color, then exclusively writes the PNG. All ordinary captures retain the original zero-private-node requirement.
- Validation: direct selection covers both Chrome web-text classes and two valid product origins under exact Host context, plus default, hostname-only, edit-control, clickable, described, resource-owned, missing/malformed/duplicate context, out-of-page, excess-origin, and registered unknown-private rejection. Exact PNG coverage proves crop translation, first/last redacted pixels, unchanged adjacent pixels, and malformed bounds rejection. Focused Android/Host/lock/device tests pass 59 with one intentional physical skip; CLI typecheck passes. The clean `4c5dbc5` phone run remained fail-closed before writing Host evidence and exact cleanup restored empty Serve, stopped Chrome, original phone settings, zero tunnels, and clean acceptance state. The clean `c142b9c` diagnostic then identified one exact canonical, non-interactive, description/resource-free, app-contained `TextView` and no other private node. The clean `710ef06` run matched that node and proved the decorative owner label is absent from Xiaomi UIAutomator. The clean `5f5f4fd` run then proved exact unique Host/back/permission context with the back and permission structurally eligible; only the heading's irrelevant text-node structure differed. Every run cleaned identically. The diagnostic regression proves arbitrary origin, hostname, class, resource, and registered values are absent from output. Corrected no-retry phone proof remains pending.
- Observability: rejected Host-origin admission reports at most four private-node structural records containing only canonical relation flags, registered-value presence, one allowlisted class category, click/description/resource/containment flags, bounded coordinates, and total/eligible counts for the three exact Host context landmarks. It never reports node text, descriptions, class/resource values, the origin, hostname, registered secrets, raw hierarchy, account, profile, device identifier, or pairing material; if a retained value collides with the bounded diagnostic, the entire detail is suppressed.

### BUG-050 Android Post-Revoke Host Lock Tap Is Inert

- Symptom: the clean `ac9b5d5` run crossed the redacted Host capture, Office-device confirmation, exact one-device revocation, and terminal capture, then acquired and measured one exact clickable `Lock writes` node. The clean `cf3acba` candidate then tapped the same fully visible, clickable, enabled bounds twice with zero lock audit; both taps left the Host state unchanged and never opened `Lock remote writes?`.
- Impact: aggregate acceptance cannot prove host-wide lock, local-only unlock recovery, profile switching, runtime compatibility, TalkBack, self-revocation, privacy, cleanup-success, or retained deployment.
- Route: high release blocker and task-local physical-driver sequencing correction inside `FE-V1-090`; no product host-lock, authorization, CSRF, confirmation, mutation, retry, or UI contract changes.
- Related requirements: `NFR-004`, `IR-004`, `PR-005`, `MDH-08`, `MDH-09`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical Host-lock entry and destructive confirmation selection.
- Blocks: corrected no-retry aggregate physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: Android reported the origin `Lock writes` node as fully inside Chrome's page viewport even though prior Host scrolling left its center beneath the fixed Session Actions header. A private-safe CDP observer proved the resulting trusted pointer/touch/mouse/click sequence targeted the sheet, not the button. The first occlusion-aware selector correctly rejected the target but also tied header admission to unstable Android presentation fields, so it suppressed the required reveal swipe.
- Harsh success criteria: initially acquire one exact fully page-visible clickable and enabled `Lock writes` target and measure it; require zero lock audit before entry. Tap once and accept only exact `Lock remote writes?` truth. If absent after the bounded transition window, permit one final tap only when the same hierarchy has no lock dialog, zero lock audit, and one exact fully page-visible clickable and enabled trigger; otherwise fail closed. A diagnostic run must establish whether the Android tap produces a trusted pointer/click on the topmost lock button without retaining URL, origin, account, device, pairing, hierarchy text, or request material. After the dialog opens, require one exact title, one exact visible clickable `Cancel`, and one exact visible clickable `Lock writes` action below the title and in the same bounded footer row as Cancel; reject the underlying action, missing/duplicate title/action/cancel, occlusion, disabled semantics, or excessive geometry. Require zero lock audit before destructive dispatch, tap the owned confirmation once, then require exact locked truth and later one accepted/terminal lock audit pair; never retry the mutation.
- Validation required: direct confirmation-action selector positive with an underlying decoy plus missing/duplicate/occluded/non-clickable/wrong-row/title cases; generic visible/clickable selector matrix; current Chromium post-revoke center-hit/touch regression; private-safe Android event/hit diagnosis; focused Android/Host-lock/app-shell tests; CLI/web typechecks, Biome, planning checks; and one clean pushed no-retry phone run through lock and later stages with exact cleanup.
- Fix: derive one strict Host content boundary from unique exact title/back/close accessible names, enabled/clickable state, bounded page-contained geometry, and coherent header layout without depending on Android's class, text/resource duplication, or generated resource IDs. Reject the obscured action, swipe only inside the exposed Host content region, then reacquire and tap only when the exact enabled target is fully below the complete header plus safety gap. Keep the strict dialog-owned destructive selector and one-shot mutation unchanged.
- Validation: the regression matrix proves the old page-only selector admits the obscured target, freezes the phone's observed `560..687` header and `625..751` action bounds, requires a bounded backward content swipe, admits the shifted target, and rejects missing/duplicate/disabled/incoherent/out-of-page variants. Focused Android-driver 43 passed/one intentional physical skip, CLI typecheck, Biome, and planning pass. Pushed `8754a63` then produced `fe090-35-lock-confirmation.png`, `fe090-36-host-locked.png`, locked Mission Control, local-only unlock, and profile-away truth with one accepted/terminal lock audit pair before the independent `BUG-053` evidence guard stopped the run; exact cleanup restored empty Serve, stopped Chrome, phone settings, tunnels, and acceptance state.
- Closed by: private-safe diagnosis and pushed physical correction `8754a63`.

### BUG-051 Archive Confirmation Action Is Not Dialog-Owned

- Symptom: after opening `Archive session?`, the physical acceptance driver ranked every `Archive session` node by vertical position and selected the lowest match. It did not require the confirmation title, Cancel control, enabled/clickable state, current Chrome page containment, one unique candidate, or shared footer geometry.
- Impact: an underlying, duplicated, disabled, displaced, or non-page control could be treated as the destructive confirmation. The aggregate run could dispatch an unowned tap or claim archive coverage without proving the approved confirmation path.
- Route: high release blocker and small validation-harness hardening inside `FE-V1-090`; no product archive, authorization, confirmation, mutation, retry, or UI contract changes.
- Related requirements: `NFR-004`, `IR-004`, `PR-005`, `MDH-01`, `MDH-02`, `MDH-16`, `MDH-20`, `MDH-21`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical managed-thread archive confirmation.
- Blocks: trustworthy aggregate physical archive evidence, retained phone deployment, and downstream release tasks.
- Root cause: a generic geometry fallback encoded "lowest matching text" as ownership. It ignored the confirmation structure even though the origin action and destructive footer action intentionally share the same visible label.
- Fix: replace the lowest-label fallback with a dedicated archive confirmation wait and a shared strict confirmation-footer selector. Admission now requires one exact page-contained `Archive session?` title, one exact page-contained enabled/clickable Cancel control below it, and one exact page-contained enabled/clickable `Archive session` action below the title in Cancel's bounded footer region. Missing, duplicate, disabled, non-clickable, wrong-row, and out-of-page candidates fail closed with bounded private-free state summaries.
- Harsh success criteria: never rank duplicate text by screen position alone; reject the underlying Session Actions trigger; admit exactly one destructive action owned by the current confirmation footer; tap it once; require terminal `Session archived` truth, a successful Back-to-sessions handoff, and zero retained Session Detail subscribers; do not retry the archive mutation.
- Validation: the direct fixture selects the owned action despite an underlying decoy and rejects missing/duplicate title, missing/disabled Cancel, disabled/non-clickable/wrong-row/duplicate/out-of-page confirmation candidates. The focused Android-driver suite passes 42 with one intentional physical skip; CLI typecheck and targeted Biome pass. One clean no-retry physical archive crossing remains required before closure.

### BUG-052 TalkBack Transcript Does Not Prove TalkBack Behavior

- Symptom: the physical path enables TalkBack, but each "traversal" step copies text and descriptions from every visible UIAutomator node before sending `KEYCODE_TAB`. It activates controls by tapping their known coordinates, sending Enter, and, on timeout, tapping the target twice. The retained sorted label set therefore does not identify accessibility focus, traversal order, spoken feedback, dialog focus, or TalkBack-owned activation.
- Impact: `MDH-20` can pass while TalkBack never focuses, reads, or activates a HostDeck control. The aggregate evidence could falsely claim real assistive-technology coverage and conceal focus-order, modal-containment, naming, state, or gesture failures.
- Route: high release blocker and validation-harness correction inside `FE-V1-090`; product changes are permitted only if genuine TalkBack evidence exposes a semantic or interaction defect.
- Related requirements: `NFR-004`, `PR-005`, `MDH-03`, `MDH-20`, `MDH-22`, `MDH-23`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical semantic-accessibility and TalkBack acceptance.
- Blocks: credible aggregate accessibility evidence, retained phone deployment, and downstream release tasks.
- Root cause: the harness conflates three independent facts: TalkBack is enabled, semantic nodes exist, and generic ADB input can change the UI. Full-tree inventory plus keyboard/direct-coordinate actions cannot establish the TalkBack focus path or its activation semantics. Sorting the collected labels further destroys traversal order.
- Harsh success criteria: prove the real TalkBack service is bound with touch exploration active; use one-finger next/previous gestures delivered through Android's accessibility input filter; record only bounded ordered evidence derived from actual accessibility-focus changes; activate the currently focused control using TalkBack's double-tap-anywhere gesture; prohibit target-coordinate activation, Enter, direct-tap, full-tree label collection, and fallback activation. Cover Mission Control route/status, selected-session navigation, primary control naming, dialog entry/initial focus/containment/exit, prior approval/result truth, and recovered remote status. Fail on unknown focus, missing/reordered required landmarks, focus escape, duplicate activation, service suppression, unbounded/raw event output, or any retained private value. Restore TalkBack and every accessibility setting on success and failure.
- Validation required: first run a bounded phone spike to determine which Xiaomi/Android surface truthfully exposes TalkBack accessibility focus without suppressing existing accessibility services. Then freeze a private-safe focus-event parser/allowlist, hostile malformed/private/unknown/reordered fixtures, exact one-finger traversal and double-tap activation, focus-containment assertions, service/touch-exploration preflight, cleanup failure tests, and one clean no-retry physical transcript. Generic UI-tree semantics remain supporting evidence only.
- Current state: the bounded phone spike and harness correction are complete, but physical closure is still pending. A no-permission instrumentation observer using `FLAG_DONT_SUPPRESS_ACCESSIBILITY_SERVICES` saw zero focus events from SDK `adb shell input`, proving that path bypasses TalkBack on this Xiaomi. The same observer recorded ordered accessibility-focus events and a click from a temporary internal `BUS_VIRTUAL` UHID touchscreen driven by one-finger swipes and a double-tap outside the focused control; the page changed only after that TalkBack-owned click. The implementation candidate now compiles the observer/touch helpers into a temporary DEX, observes only bounded private-safe focus/click records, rejects hostile malformed/private/unknown/reordered traces, proves modal containment and focus return, and restores accessibility settings plus TalkBack permission state. Typecheck, full Biome, focused CLI tests, hostile transcript tests, Java/D8 compilation, observer lifecycle, permission restoration, and device residue checks pass. One clean no-retry aggregate phone transcript remains required before BUG-052, FE-V1-090, retained deployment, and downstream release gates can close.

### BUG-053 Profile-Recovery Host Screenshot Omits Required Origin Redaction

- Symptom: clean candidate `535e713` physically passed Host lock, local-only unlock, the saved-profile-away boundary, and the recovered dedicated profile flow up to `fe090-39-profile-recovered.png`. That capture failed closed because the page hierarchy still contained private browser material.
- Impact: the aggregate run cannot retain the recovered-profile checkpoint or continue into runtime compatibility, real TalkBack replacement, archive, self-revocation, retained deployment, and downstream release gates.
- Route: high release blocker and small physical-evidence harness fix inside `FE-V1-090`; no product UI, origin display, profile, Tailscale, authorization, or privacy contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-09`, `MDH-16`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` private-free physical Host-access evidence.
- Blocks: corrected no-retry aggregate physical acceptance, retained phone deployment, and all downstream release tasks.
- Root cause: the capture already used the mandatory Host-evidence owner and requested exact-origin redaction. Its fail-closed selector modeled only the Session Actions Host sheet, requiring `Host & access`, `Back to session actions`, and `Read & write`. The recovered-profile checkpoint opens Mission Control's global Host sheet instead, whose equivalent ownership boundary is `Host & access` plus the enabled `Close Host and access` control. The exact inert origin node was therefore left unredacted by an incomplete context model; no private evidence was published.
- Fix: accept exactly one of two explicit, unique Host contexts: the session sheet (`Host & access`, `Back to session actions`, and inert `Read & write`) or the global sheet (`Host & access` and enabled `Close Host and access`). Both contexts require bounded page-contained geometry, exact inert origin text, and the existing private-free redaction region. Missing, duplicate, disabled, hostname-only, described, editable/clickable/resource-owned, ambiguous, or registered non-origin private nodes still fail closed. Added positive and hostile global-context regression coverage.
- Harsh success criteria: every screenshot taken while Host & access is open must use one mandatory Host-evidence capture owner; admit redaction only under one unique recognized Host context; keep the raw screenshot unpublished on rejection, retain no private diagnostic, and prove the next clean no-retry phone run saves the recovered-profile checkpoint before continuing.
- Validation required: direct redaction positive and hostile matrices, focused Android-driver suite, CLI typecheck, Biome, planning checks, one clean pushed physical crossing through `fe090-39-profile-recovered.png`, private scan, and exact success/failure cleanup.
- Closed by: selector correction plus fresh recovered-profile capture and continuation at `03103cd`; exact failure cleanup also passed.

### BUG-054 Incompatible Runtime Check Still Requires Ready Remote Copy

- Symptom: clean candidate `0ac7b05` accepted all dashboard checkpoints through `fe090-39-profile-recovered.png`, then the physical runtime-compatibility branch failed after the deliberate incompatible refresh because the bounded oracle could not find `Remote access ready` after four swipes.
- Impact: the aggregate run published no retained evidence and stopped before TalkBack, archive, and final cleanup assertions could close `FE-V1-090`.
- Route: high release blocker and small physical-acceptance harness correction inside `FE-V1-090`; no product route, runtime, remote access, or user-facing contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical runtime-compatibility state transition.
- Blocks: trustworthy incompatible/recovered runtime evidence, final TalkBack/archive/revocation acceptance, retained deployment, and downstream release tasks.
- Root cause: `runPhysicalRuntimeCompatibilityState()` intentionally changed the fixture's Codex collaboration modes so the refresh should replace ready compatibility with `Incompatible`, but `runOneProductionRemoteCheck()` unconditionally required the transient pre-refresh `Remote access ready` label after every status check. The failure was an impossible oracle requirement for the intentional incompatible branch, not a claimed remote-network failure.
- Fix: make the post-check ready assertion explicit. All ordinary remote checks retain the strict ready-truth requirement; only the deliberate incompatible transition skips that transient label and then requires the authoritative Mission Control `Incompatible` state after the Host sheet closes. The status-then-refresh request counts and single-action tap proof remain mandatory.
- Harsh success criteria: never infer compatibility from a request alone; require the exact status-then-refresh sequence, close the Host sheet through its owned control, and admit the incompatible and recovered Mission Control states exactly once without retry or remote mutation.
- Validation required: focused Android-driver regression, CLI typecheck, Biome, planning checks, one fresh clean no-retry physical run from the pushed correction through TalkBack/archive/revocation/cleanup, private scan, and retained evidence inspection.
- Closed by: implementation correction plus fresh incompatible-check continuation at `03103cd`; the newer BUG-056 correction restores independent remote-ready proof after exact response settlement.

### BUG-055 Xiaomi Plug Mask Does Not Match Stay-Awake Enforcement

- Symptom: clean candidate `b10ee11` passed 45 of 46 tests for 751 seconds, then the runtime-compatibility branch could not reacquire the production Host trigger. The bounded UI state contained no retained HostDeck landmark; after cleanup the phone reported `mPlugType=1`, while the harness had temporarily set `stay_on_while_plugged_in=2` and Android reported `mStayOn=false`. The first corrected candidate `6127392` then stopped before mutation because the phone's full `dumpsys power` output exceeded the harness's 512 KiB ADB bound and returned `ENOBUFS`. Pushed correction `ed31b7b` used the bounded `mPlugType` line; its fresh clean run passed 46 of 47 tests for 804 seconds with stay-awake setting `1`, proving the power-state fix before exposing the next runtime-oracle blocker.
- Impact: the Xiaomi can enter its screen timeout during the long physical sequence, turning a live UI checkpoint into an empty hierarchy. No retained private evidence was published; aggregate phone acceptance and all downstream release gates remain blocked.
- Route: high release blocker and small physical-acceptance harness correction inside `FE-V1-090`; no product UI, remote access, profile, authorization, or user-facing contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` Android device power-state preflight and long-run cleanup.
- Blocks: trustworthy no-retry aggregate physical acceptance, retained phone deployment, and downstream release tasks.
- Root cause: the harness hard-coded the USB stay-awake bit (`initialSetting | 2`). This Xiaomi exposes the connected USB-C power source as plug bit `1`, so the temporary setting did not activate stay-awake despite the device being ADB-visible.
- Harsh success criteria: read exactly one bounded active `mPlugType` mask before mutation; reject malformed, contradictory, or unplugged observations; set only `initialSetting | activePlugType`; require the setting to be observed before continuing; restore the original setting on every exit; and never use a power-setting shortcut that hides the baseline.
- Fix: extract only the exact `mPlugType` line on the Android side, parse and validate the bounded active plug mask, pass it into temporary stay-awake enforcement, and retain the existing exact restoration/cleanup path. The Xiaomi path now enables bit `1`; USB-reported devices continue to enable bit `2`.
- Validation: parser regression covers AC/USB/wireless, inactive, invalid, contradictory, and oversized observations. Pushed `ed31b7b`, the clean 804-second run, and fresh `03103cd` prove bounded observation, the active mask, 874 seconds of later physical work, and exact restoration.
- Closed by: implementation `ed31b7b` and fresh aggregate continuation at `03103cd`.

### BUG-056 Physical Runtime Compatibility Oracle Does Not Prove Settled Route Truth

- Symptom: fresh candidate `03103cd` crossed local unlock and recovered-profile evidence, completed the deliberate incompatible remote-check requests, closed Host access, then timed out waiting for `Codex interface incompatible`. The previous exact-label correction still polled only the current viewport. Its supported branch would later wait for `Codex compatible` after closing Host access even though supported Mission Control intentionally renders `Current` and no compatibility notice.
- Impact: request start can be mistaken for completed host truth, a valid top-of-route notice can be missed from a retained scroll position, and supported recovery can never satisfy the old route oracle. TalkBack, archive, self-revocation, retained deployment, and downstream release gates remain blocked. This does not establish a product runtime or network defect.
- Route: high release blocker and physical-acceptance harness correction inside `FE-V1-090`; no product runtime, remote-access, authorization, UI, or architecture contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical runtime-compatibility transition, recovery, and route assertion.
- Blocks: trustworthy incompatible/recovered runtime evidence, final TalkBack/archive/revocation acceptance, retained phone deployment, and downstream release tasks.
- Root cause: `runOneProductionRemoteCheck()` accepted host and remote request counters at request start without a completed host response. After closing the sheet, the harness did not restore Mission Control's scroll owner before polling the top notice. The prior correction also conflated the detailed Host-access title with the supported route contract: incompatible Mission Control renders `Codex interface incompatible` plus `Incompatible`, while supported Mission Control renders `Current` and deliberately hides `Codex compatible`.
- Fix: require exactly one browser remote request, one host request, and one new successful host response; then prove independent `Remote access ready` and the exact detailed compatibility title inside Host access. After closing, use a bounded backward route reveal and hold exact Mission Control, remote, permission, state, zero-subscriber, no-sheet, and no-pending truth for two stable seconds without coupling the top-of-route oracle to an offscreen session row. Supported recovery requires `Current` with both compatibility titles absent. Diagnostics retain at most six changed allowlisted count summaries.
- Harsh success criteria: no request-start success, non-200 response, duplicate request, raw substring, hidden sheet label, unsupported route title, unbounded scroll, retry-based state mutation, private hierarchy output, or transient observation may pass; incompatible and supported route truth must each remain stable for two seconds after one exact read-only check.
- Validation: Android-driver 48 passed with one intentional physical skip; Mission Control renderer 31 passed; CLI/web typechecks and targeted Biome pass. One fresh committed no-retry phone run must cross both runtime states, TalkBack, archive, self-revocation, cleanup, private scan, and retained evidence.
- Current state: implementation and hostile regressions are complete; physical closure remains pending on the next pushed candidate.

### BUG-057 Session Content Reveal Timeout Hides The Failed Invariant

- Symptom: after USB transport was restored, fresh clean candidate `fc9a7ef` paired, opened Session Detail, returned to the current context, retained stable write authority, then failed closed after four forward swipes because `Review & approve` never became admissible outside the fixed session dock. The timeout reported neither whether the target was absent, duplicate, disabled, clipped, or below the dock nor whether the back control, four dock controls, content region, or swipe lane was valid.
- Impact: weakening the safe-region selector, increasing swipes, or rerunning the same candidate would be guesswork. Aggregate approval, TalkBack, archive, self-revocation, retained deployment, and downstream release gates remain blocked. No partial/private evidence was retained, and cleanup restored phone settings, Chrome, tunnels, Serve, and the clean acceptance worktree.
- Route: high release blocker and task-local physical observability correction inside `FE-V1-090`; no product approval, authorization, mutation, retry, UI, or architecture contract changes are justified yet.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-07`, `MDH-13`, `MDH-20`, `MDH-22`, `MDH-23`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical inline-approval reveal and fixed-dock safety proof.
- Blocks: trustworthy root-cause correction, no-retry aggregate physical acceptance, retained phone deployment, and downstream release tasks.
- Root cause: the reveal owner reduces every non-admitted hierarchy to `null` and discards all bounded state transitions before its timeout. Existing event-diagnostic and Host-content reveal owners retain private-safe geometry observations, but the shared Session Detail content reveal does not; the exact physical target/scroll failure therefore cannot be determined from the accepted diagnostic surface.
- Harsh success criteria: retain at most six changed private-safe observations; distinguish absent, duplicate, non-clickable, disabled, invalid content boundary, above/below, edge-clipped, horizontal-outside, and admitted states; report only expected-match counts, enabled/clickable state, bounded geometry, exact back/dock landmark counts, content region, a six-node geometry-only scroll witness, and the selected swipe gesture; never report arbitrary text, descriptions, classes, resources, or hierarchy bytes; preserve the four-swipe limit, established scroll lane, full-region admission, one-shot tap ownership, and zero fallback coordinates or mutations; cover every hostile classification directly; require a new committed no-retry phone candidate before choosing any geometry or UI correction.
- Fix: pushed in `68d17ff`. `revealPhysicalSessionContentNode` now classifies every bounded state without changing selection or swipe behavior, retains only the last six changed observations at no more than 4 KiB each, includes a six-node geometry-only scroll witness, and reports direction plus exact swipe count on failure.
- Validation: focused Android driver 46 passed with one intentional physical skip, CLI typecheck, Biome, planning, clean diff, and push checks passed. Fresh candidate `7e6b06d` retained four changed private-safe states showing a valid dock/content/gesture boundary, changing scroll geometry, and an absent target at every step; exact cleanup passed. The trace identified `BUG-058` without another diagnostic run or any selector weakening.
- Closed by: implementation `68d17ff` and fresh physical diagnosis at `7e6b06d`.

### BUG-058 Physical Approval Fixture Expires At A Calendar Constant

- Symptom: fresh candidate `7e6b06d` paired, opened Session Detail, retained current write authority, and scrolled through four distinct valid content states, but `Review & approve` was absent in every hierarchy. The event fixture and approval-list fixture both set `expires_at` to `2026-08-01T12:00:00.000Z`; the run occurred after that instant, so the production UI correctly projected the request as expired and removed the action.
- Impact: a passing aggregate can become impossible solely because wall time crossed a source-code date. Approval, TalkBack, archive, self-revocation, retained deployment, and downstream release gates remain blocked even though product approval behavior is correct.
- Route: high release blocker and local physical-fixture correction inside `FE-V1-090`; no product approval, expiry, UI, authorization, mutation, retry, or architecture contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-07`, `MDH-13`, `MDH-22`, `MDH-23`, `MDH-24`.
- Affected / owning task: `FE-V1-090` deterministic physical approval event/list fixture.
- Blocks: no-retry aggregate physical acceptance, retained phone deployment, and downstream release tasks.
- Root cause: two independent fixture owners copied one absolute expiry date while the session/database clock intentionally follows real wall time. Once the date passed, strict event/list reconciliation remained coherent but the approval controller truthfully classified the request as non-actionable.
- Harsh success criteria: derive one approval creation time from the dashboard session fixture; derive one exact expiry 30 minutes later; pass the same immutable timing to both projected event and approval-list owners; assert exact event/list equality and lifetime; reject invalid creation times; retain strict pending/expiry product behavior and one-shot response ownership; use no far-future sentinel, calendar constant, hidden clock override, retry, or UI fallback; require one fresh committed physical crossing through approval confirmation, responding, and terminal approved truth with exact cleanup.
- Fix: the dashboard session fixture now constructs one frozen run-derived approval timing and passes that exact object to both the projected event and `createPhysicalDashboardControls`. Direct fixtures freeze the 30-minute delta, event/list identity, and invalid-time rejection.
- Validation: focused Android-driver suite, CLI typecheck, Biome, planning, clean diff/privacy review, commit, and push passed. Fresh no-retry candidate `9bc7679` ran for 917 seconds, crossed pending approval, confirmation, responding, and terminal approved truth, then reached the later Host-access local-unlock boundary. Failure cleanup restored the phone settings, Chrome, Serve, and clean acceptance worktree exactly.
- Closed by: implementation and fresh physical crossing at `9bc7679`.

### BUG-059 Local-Unlock Reopen Oracle Is Not Generation-Bound

- Symptom: fresh candidate `9bc7679` completed approval and the intervening dashboard controls, locked writes, rendered locked truth in Session Detail and Mission Control, and received a successful local-admin unlock response without mutating Serve. After Android reload and a selected-session tap, the harness timed out on `Ready to send` with `Physical Session Detail remained locked after local unlock`.
- Impact: the message attributes the failure to product lock state even though the boundary proves neither a settled unlocked Mission Control projection nor a new Session Detail route/stream generation. A stale subscriber can satisfy the tap completion before navigation, wasting a 15-minute physical candidate and blocking profile, runtime, TalkBack, archive, revocation, retained deployment, and release gates. No partial/private evidence was retained; exact cleanup passed.
- Route: high release blocker and task-local physical-oracle correction inside `FE-V1-090`; direct controller coverage must prove product local-unlock adoption before changing product lock, UI, authorization, or architecture behavior.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-08`, `MDH-13`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-23`, `MDH-24`.
- Affected / owning task: `FE-V1-090` local-only Host unlock, Mission Control reload, and Session Detail reopen proof.
- Blocks: the remaining aggregate checkpoints, retained phone deployment, and downstream release tasks.
- Root cause: the reload gate observed only that a session-list request started; it did not require new access/host reads, closed prior Session Detail transport, or settled unlocked UI truth. The following tap completed on the global condition `active_subscribers === 1`, which could already be true before the tap, without requiring a new detail request, stream request, or opened subscriber. A final raw-label wait then discarded the state needed to distinguish stale navigation from locked projection.
- Harsh success criteria: after local unlock, require one later access, host-status, and session-list request generation; require zero active Session Detail subscribers; hold exact current Mission Control `Remote ready`, `Write`, and selected-session truth for two stable seconds with no locked/stale/reconnecting label; reacquire one fully visible enabled selected-session target; require exactly one post-tap detail request, stream request, opened subscriber, and active subscriber; hold the existing strict writable Session Detail predicate for two stable seconds; retain at most six changed diagnostics containing only allowlisted label counts, request deltas, and bounded stream counters; use no retry, subscriber-only success, request-start-only success, fallback coordinate, product-state override, or arbitrary hierarchy text; require one fresh committed no-retry physical crossing through local unlock and all later aggregate stages.
- Fix: the controller regression now proves later local unlock is adopted across Mission Control refresh and Session Detail navigation. The physical boundary waits for all three reload owners and stable exact unlocked UI truth, then snapshots navigation and admits the tap only after exact one-generation detail/stream advancement; final write readiness uses the existing stable strict predicate and private-safe stream diagnostic.
- Validation: 97 focused controller/Android-driver tests pass with one intentional physical skip; CLI/web typechecks, targeted Biome, and planning pass. Fresh no-retry candidate `03103cd` held unlocked Mission Control stable, required exact one-generation detail/stream reopening, restored writable Session Detail, crossed profile-away/return recovery, and completed exact cleanup.
- Closed by: implementation and fresh physical crossing at `03103cd`.

### BUG-060 Dashboard Aggregate Remote-Check Count Is Stale

- Symptom: static review after `03103cd` found `assertPhysicalDashboardAudit()` requires `remoteBrowserStatusRequests === 2`, while the same frozen dashboard sequence performs four explicit browser checks: initial Host access, recovered-profile Host access, incompatible runtime, and supported runtime.
- Impact: even a phone run that passes every UI, TalkBack, archive, and self-revocation checkpoint would fail its final aggregate invariant. The stale expectation would waste a full no-retry candidate and block retained deployment without identifying a product defect.
- Route: high release blocker and small physical-acceptance invariant correction inside `FE-V1-090`; no product route, network, runtime, authorization, or UI contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-16`, `MDH-22`, `MDH-23`, `MDH-24`.
- Affected / owning task: `FE-V1-090` final request/authority aggregate and runtime transition accounting.
- Blocks: trustworthy aggregate closure, retained phone deployment, and downstream release tasks.
- Root cause: the final assertion retained the two-check count from the initial and profile-recovery stages after incompatible and supported runtime checks were added to the sequence in the same implementation commit.
- Fix: freeze the exact four-check aggregate and require the runtime branch itself to advance its baseline by exactly two with zero browser remote mutations. The shared remote-check boundary now also rejects request-only, duplicate, missing-response, and non-200 outcomes.
- Harsh success criteria: all four named checks execute exactly once; the runtime pair advances exactly twice; every accepted check completes one successful host refresh; zero browser remote mutation occurs; no loose range, retry, or skipped final invariant may pass.
- Validation: direct hostile remote-settlement tests, Android-driver 48 with one intentional physical skip, CLI typecheck, and Biome pass. One fresh full phone crossing through final aggregate validation remains required.
- Current state: implementation correction is complete; physical closure remains pending on the next pushed candidate.

### BUG-061 Archive Entry Is Not Viewport- Or Generation-Bound

- Symptom: static late-stage review found `runPhysicalArchiveControl()` waits for the selected session only in the current hierarchy after TalkBack intentionally returns focus to the top remote-status rail. Its tap then passes on global `active_subscribers === 1`, without proving TalkBack's prior subscriber closed or that archive entry opened a new detail and stream generation.
- Impact: a valid session row can be missed offscreen, or a retained subscriber can falsely pass navigation before Session Actions. Either outcome would waste a full no-retry candidate immediately before the already hardened archive confirmation, self-revocation, aggregate cleanup, and retained deployment.
- Route: high release blocker and task-local physical-acceptance correction inside `FE-V1-090`; no archive product, authorization, UI, or architecture contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-11`, `MDH-13`, `MDH-20`, `MDH-22`, `MDH-23`, `MDH-24`.
- Affected / owning task: `FE-V1-090` TalkBack-to-archive handoff and archive Session Detail admission.
- Blocks: trustworthy `BUG-051` physical archive proof, self-revocation, final aggregate cleanup, retained deployment, and downstream release tasks.
- Root cause: archive entry predates the generation-bound navigation invariant added for `BUG-059`. It neither normalizes the TalkBack-returned viewport nor snapshots detail, stream, opened-subscriber, active-subscriber, and missing-detail counters before tapping.
- Fix: require zero active subscribers after TalkBack, snapshot the complete navigation boundary, reveal one fully visible enabled selected-session control with bounded forward scrolling, admit the tap only after exactly one detail request, stream request, and opened/active subscriber with unchanged missing-detail count, then hold the existing strict writable Session Detail predicate before opening Session Actions.
- Harsh success criteria: no retained subscriber, current-viewport assumption, request-start-only signal, duplicate generation, missing detail, fallback coordinate, retry-based success, or unlocked-but-stale detail may pass; the existing strict dialog-owned archive selector and terminal/archive-back cleanup remain unchanged.
- Validation: the existing hostile exact-navigation matrix plus Android-driver 48/one intentional physical skip, CLI typecheck, and Biome pass. One fresh full phone crossing through TalkBack, archive, self-revocation, and final cleanup remains required.
- Current state: implementation correction is complete; physical closure remains pending on the next pushed candidate.

### BUG-062 Gated Pairing Evidence Can Exhaust Remote Admission

- Symptom: fresh clean candidate `61ebed2` paired the phone, then its first Mission Control access bootstrap was rejected before the route handler with `access=0/403` and `proxy=remote_generation_stale`. The candidate stopped after 135 seconds of physical work and was not retried.
- Impact: a valid product pairing can be followed by an artificial fail-closed route bootstrap when physical evidence timing lands on the admission deadline. No dashboard checkpoint, retained deployment, or release closure can be trusted from that candidate. This does not establish a product, Tailscale, pairing, or network defect.
- Route: high release blocker and physical-acceptance harness correction inside `FE-V1-090`; no product admission TTL, proxy trust, pairing, browser recovery, UI, or architecture contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-03`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` automated no-QR pairing claim and claiming-state evidence boundary.
- Blocks: trustworthy aggregate phone acceptance, retained deployment, and all downstream release tasks.
- Root cause: production remote admission is intentionally renewed on a 10-second observation lease. The physical harness holds the already admitted pairing request behind `PhysicalPairingClaimGate` while synchronous Android inspection and screenshot work records `Pairing this phone`, but it did not establish fresh lease runway around that hold. `evaluateRemote()` reads admission twice synchronously and correctly rejected the first access request when those reads straddled lease expiry; zero access-handler calls plus one `403` proves rejection occurred at the proxy trust boundary.
- Fix: while exactly one gated request lease is active, perform one authoritative read-only remote status observation immediately before and after the claiming screenshot. Each boundary requires open authority, the expected external origin, the same generation, exactly one active lease, and unchanged invalidation/signaled-lease counters before releasing the gate. Status failure, missing/duplicate lease, closed authority, origin or generation drift, invalidation, and signaling fail closed with bounded diagnostics; no request, pairing, or remote mutation is retried.
- Harsh success criteria: no admission-TTL increase, production polling change, gate removal, screenshot omission, sleep, retry, duplicate status observation per boundary, stale generation, changed origin, missing/duplicate active request, hidden invalidation, private diagnostic, or product fallback may pass; the next candidate must pair once and cross authenticated Mission Control route data before any later checkpoint.
- Validation: Android-driver 49 passed with one intentional physical skip; CLI typecheck and targeted Biome pass. One fresh committed no-retry phone candidate must cross pairing, Mission Control bootstrap, all remaining aggregate checkpoints, exact cleanup, private scan, and retained evidence.
- Current state: implementation and hostile regressions are complete; physical closure remains pending on the next pushed candidate.

### BUG-063 Transport Loss Is Misattributed And Cleanup Does Not Drain First

- Symptom: fresh clean candidate `59fe85e` crossed pairing, Mission Control, approval, prompt, failure states, model, goal, plan, usage, and compact, then rendered `Skills could not be loaded` with zero `read_skills` fixture calls after 1,085 seconds. It was not retried. During the same interval the host journal recorded repeated Tailscale relay receive/connect timeouts; selected lifecycle disable, fallback Serve removal, and host close then failed, leaving the exact HostDeck Serve mapping until emergency cleanup after transport recovery.
- Impact: the old Skills diagnostic could not distinguish a request that never reached Fastify from proxy rejection or route/backend failure. Cleanup could retain HostDeck-owned Serve state after an external outage, obscuring the candidate cause and violating exact physical cleanup. The run cannot close any remaining physical requirement or support retained deployment.
- Route: high release blocker and physical-acceptance harness hardening inside `FE-V1-090`; no product Skills, remote-access, Tailscale, UI, authorization, architecture, setup, or retry contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-23`, `MDH-24`.
- Affected / owning task: `FE-V1-090` aggregate transport attribution and exact failure cleanup.
- Blocks: trustworthy aggregate phone acceptance, exact cleanup, retained deployment, and all downstream release tasks.
- Root cause: the external failure was a host Tailscale relay-path outage: the browser entered its explicit read-failure UI while no Skills control call occurred, and journal timestamps align with relay unreachability. Independently, the harness recorded only fixture calls, so it could not prove whether Fastify saw the request. Its emergency manager also ran before explicitly draining the selected remote lifecycle and attempted only one immediate observe/disable sequence, allowing active lifecycle work and transient local Tailscale failure to cascade into host-close failure.
- Fix: count the exact Skills GET and bounded response statuses at Fastify, include those deltas plus the existing proxy-rejection category and fixture-call count in private-free UI failure diagnostics, and require exactly one `200` route response in the final aggregate. Cleanup now attempts selected lifecycle disable, explicitly begins remote drain, then runs up to 12 ownership-safe observations separated by five seconds. Transient reads and preflight failures may be re-observed; once any Serve command is attempted, reconciliation is observation-only and cannot issue a second mutation. Absent state succeeds; exact state permits the single mutation; foreign, colliding, drifted, public, stopped, unsupported, or wrong-profile truth fails without mutation.
- Harsh success criteria: no product retry, route retry, relay workaround, profile mutation, blind Serve reset, second actual cleanup command after uncertainty, foreign-state mutation, raw Tailscale output, private origin/profile/account retention, or failure reclassification without exact counters may pass. A fresh candidate requires sustained healthy host/phone Tailscale reachability, one no-retry aggregate, exact cleanup, private scan, and separately retained functional deployment.
- Validation: Android-driver 51 passed with one intentional physical skip; CLI typecheck, targeted Biome, and planning pass. Host emergency cleanup proved empty Serve, closed acceptance process/listener, clean committed acceptance worktree, stopped Chrome, zero ADB tunnels, TalkBack disabled, and exact phone setting restoration. Implementation `ecca780` is pushed; one fresh phone run remains pending.
- Current state: harness implementation and hostile reconciliation tests are pushed in `ecca780`; external transport has recovered, but physical closure requires its fresh no-retry candidate.

### BUG-064 Remote Check Reacquires While Its One Request Is In Flight

- Symptom: fresh clean candidate `17ce20d` crossed pairing, dashboard state coverage, profile-away recovery, and the corrected Skills boundary, then failed its first runtime-compatibility remote check. Exact counters were one remote browser request, one host request, and zero new host responses when the action was temporarily absent; after eight seconds the generic tap verifier tried to reacquire `Check again` instead of waiting for the in-flight response. The candidate was not retried.
- Impact: the physical driver can attempt a second read-only check or fail on the expected in-flight UI even though the first request was admitted exactly once. The run cannot prove the frozen no-retry remote-check contract or close any remaining physical requirement.
- Route: high release blocker and task-local physical-driver correction inside `FE-V1-090`; no product remote-check, timeout, retry, UI, runtime, Tailscale, authorization, or architecture contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-02`, `MDH-06`, `MDH-16`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` one-attempt physical remote status and runtime-compatibility transition.
- Blocks: trustworthy aggregate phone acceptance, retained deployment, and all downstream release tasks.
- Root cause: `runOneProductionRemoteCheck()` wrapped the exact response-settlement predicate in the generic two-tap UI verifier. That helper owns an eight-second first wait followed by action reacquisition, but `Check again` is intentionally unavailable while the single host status request is pending.
- Fix: the remote check now uses the existing one-tap transition boundary with a route-appropriate 45-second deadline. Settlement remains exact: one new browser request, one new host request, one new host response, and latest response status `200`; diagnostics name each delta and report no new status when only a stale prior response exists.
- Harsh success criteria: no action reacquisition, second tap, request-start success, non-`200` success, duplicate browser request, duplicate host request or response, generic retry helper, hidden timeout increase, product fallback, or private diagnostic may pass. Deterministic hostile coverage must prove in-flight request-only truth remains pending and exact one-response `200` truth settles after one tap.
- Validation: Android-driver 54 passed with one intentional physical skip; CLI typecheck, full 831-file Biome, and planning pass. Implementation `bde6968` is pushed; one fresh committed no-retry phone candidate and clean-commit proof remain pending.
- Current state: implementation and hostile regressions are complete; candidate `17ce20d` remains invalid and must not be rerun, and physical closure requires its fresh successor.

### BUG-065 Chrome Cleanup Does Not Prove Complete Settled Package Absence

- Symptom: after candidate `17ce20d` failed, both cleanup calls reported success, but Android Chrome was later found running and required explicit emergency force-stop. The cleanup oracle queried only the exact main process and checked it once after a one-second delay; Android event evidence also retained Chrome child-process activity beyond that window.
- Impact: a failed or successful physical run can leave browser authority and device resources active while reporting exact cleanup. That invalidates cleanup/privacy evidence and can contaminate the next candidate.
- Route: high release blocker and task-local physical cleanup hardening inside `FE-V1-090`; no product browser, pairing, remote-access, profile, authorization, or architecture contract change.
- Related requirements: `NFR-004`, `PR-005`, `MDH-03`, `MDH-20`, `MDH-22`, `MDH-23`, `MDH-24`.
- Affected / owning task: `FE-V1-090` physical Android browser cleanup and residue proof.
- Blocks: trustworthy aggregate phone acceptance, exact cleanup/privacy evidence, retained deployment, and all downstream release tasks.
- Root cause: the one-second exact-main-process oracle was insufficient to prove settled package absence. The retained evidence does not justify a narrower claim about which Android component caused the later running state.
- Fix: cleanup issues one explicit user-0 Chrome force-stop per call, observes the main process and every package child process, and requires continuous complete absence for 30 seconds within a 45-second deadline. Any reappearance resets settlement; malformed process output fails immediately; cleanup does not retry the force-stop.
- Harsh success criteria: no exact-main-only observation, one-sample success, child-process tolerance, second force-stop, swallowed observation error, process-name prefix collision, arbitrary sleep-only proof, browser relaunch, private process detail retention, or cleanup success before 30 continuous absent seconds may pass. Deterministic hostile coverage must prove stable absence, reset on reappearance, timeout, and immediate malformed-observation failure.
- Validation: Android-driver 54 passed with one intentional physical skip; CLI typecheck, full 831-file Biome, and planning pass. A bounded Xiaomi probe detected both actual main and child process classes, accepted the explicit user-0 force-stop, and proved 30 continuous absent seconds. Implementation `bde6968` is pushed. Failed candidate `38c2a48` then completed the same exact cleanup with empty Serve, the selected dedicated profile, restored phone settings, zero ADB tunnels, no acceptance process/listener, TalkBack off, and complete Chrome absence under direct post-run inspection.
- Current state: closed by `bde6968` plus the post-run cleanup of candidate `38c2a48`; successful-run cleanup remains the separate aggregate `FE-V1-090` and `BUG-031` gate.

### BUG-066 Chrome Launcher Return Depends On Retained Activity State

- Symptom: fresh clean candidate `36808cb` crossed pairing, approval, prompt, stream recovery, stale/current detail, and not-found capture, then failed after 383 seconds when the normal Chrome launcher produced zero selected-detail requests, stream opens, or active subscribers. It was not retried. Bounded Android activity evidence shows the missing-session VIEW entered the sole `ChromeTabbedActivity`, Back finished that activity, and the launcher alias created Chrome `Main` without reopening a HostDeck route.
- Impact: the physical driver can stall on Chrome task/session history even though HostDeck correctly closed the prior stream. This cannot prove deterministic not-found recovery and blocks every later dashboard, TalkBack, archive, revocation, cleanup, and retained-deployment checkpoint.
- Route: high release blocker and task-local physical navigation correction inside `FE-V1-090`; no product route, browser lifecycle, SSE, retry, UI, authorization, Tailscale, or architecture contract change.
- Related requirements: `NFR-002`, `NFR-004`, `PR-005`, `PR-011`, `MDH-02`, `MDH-07`, `MDH-19`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` direct Session Detail not-found recovery and selected-session reopening.
- Blocks: trustworthy aggregate phone acceptance, retained deployment, and all downstream release tasks.
- Root cause: the `BUG-040` correction proved one launcher-return outcome but still depended on Chrome retaining a launch-restorable tabbed activity. That state is not owned by HostDeck and was absent after the now-strict browser cleanup; a successful Android `MAIN` activity launch therefore did not imply any HostDeck route, request, or subscriber recovery.
- Fix: both not-found and clipboard external-page exits now use one shared exact recovery owner. It retains the one-Back proof that closes only the active selected-session stream, opens the canonical HostDeck root through one external VIEW, requires exactly one access, host-status, and session-list request plus stable current Mission Control truth, requires one unambiguous quiet-queue disclosure state and expands it once only when collapsed, and permits one selected-session row tap only. That tap must produce exactly one selected-detail request, one stream/open generation, and one active subscriber before later controls resume.
- Harsh success criteria: no Chrome launcher alias, retained-task/tab assumption, direct selected-session deep link, second Back, second VIEW, retry, request range, request-start success, hidden active subscriber, duplicate route bootstrap, duplicate session tap, BFCache-only success, private activity detail retention, or product fallback may pass. Existing exact navigation predicates must reject every missing, extra, duplicated, or pre-existing request/subscriber count.
- Validation: Android-driver 54 passed with one intentional physical skip; CLI typecheck, targeted Biome, and planning pass. Implementation `2017215` is pushed. Fresh candidate `38c2a48` crossed both corrected external-page returns with exact Back, canonical root bootstrap, selected-row request/subscriber generations, and no launcher alias; it continued until an unrelated Host-controls handoff failure at 687 seconds.
- Current state: closed by `2017215` plus candidate `38c2a48`. Candidates `36808cb` and `38c2a48` remain invalid aggregate evidence and must not be rerun.

### BUG-067 Session Actions Handoff Is Transient And Nondiagnostic

- Symptom: fresh clean candidate `38c2a48` crossed both corrected external-page returns, utilities, clipboard cleanup, and Interrupt. Tapping `Done` then accepted one hierarchy containing `Open session actions`, `Ready to send`, no Session Actions sheet, and no Interrupt result. The immediately following Host/access step spent 30 seconds in a second presence-only lookup and reported only that the trigger was unavailable. Chrome remained in the same tabbed activity until failure cleanup. The candidate was not retried.
- Impact: the physical driver cannot distinguish a genuinely absent trigger from duplicate, disabled, non-clickable, off-page, dialog-obscured, route-drifted, transport-drifted, or repeatedly unreadable hierarchy state. It therefore cannot prove the Session Detail-to-Host workflow or provide enough bounded evidence for the next root-cause decision.
- Route: high release blocker and backlog bugfix leaf `FE-V1-091`; no product route, UI, accessibility, runtime, network, authorization, architecture, or retry contract change is justified by current evidence.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-07`, `MDH-15`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-091` physical Session Actions admission and handoff diagnostics.
- Blocks: aggregate `FE-V1-090`, retained deployment, and all downstream release tasks.
- Root cause: the driver splits one handoff across two unrelated hierarchy polls. The `Done` completion accepts a single transient sample, while `waitForAndroidUiNodePresent()` later selects the largest match without requiring uniqueness, enabled/clickable state, Chrome-page containment, complete Session Detail shell, closed overlays, or stable request/subscriber generation. Its generic poller suppresses every hierarchy exception and its timeout retains no observation state.
- Planned fix: retain one shared pure Session Actions selector and bounded stable waiter, but correct the rejected candidate before use. The default clock is monotonic; authority is sampled against one immutable baseline on every poll even when hierarchy reads fail; Interrupt `Done` and Host/access share one admission owner/window without discarding a result or rebasing; and overlay closure is proven structurally/spatially rather than by globally excluding copy that may legitimately exist in the timeline. Admission still requires one valid Chrome page, exactly one `Back to Mission Control`, all four fixed dock controls exactly once, exactly one enabled/clickable/fully page-contained `Open session actions`, one active subscriber, and two continuous seconds of unchanged exact truth. Every entry taps only the final admitted node once.
- Harsh success criteria: no wall clock, largest-match selection, one-sample success, discarded admission, second waiter/baseline at the Interrupt-to-Host handoff, global terminal-copy exclusion, stale node reuse, silent generic exception swallowing, swipe/reveal, second tap, navigation, retry, authority rebasing, private hierarchy/error retention, product/UI change, or phone run may pass. Pure hostile coverage must reject absent, duplicate, disabled, non-clickable, clipped/off-page, malformed-page, incomplete-shell, structurally open overlays, zero/multiple subscribers, navigation drift, and transient sequences; admit legitimate identical timeline copy; prove that hierarchy-read errors reset continuity while authority drift still fails; and admit only two monotonic continuous seconds before returning the final observed node.
- Validation: candidate `d3da630` reported and independently reproduced 56 passing driver tests with 1 intentional physical skip, CLI typecheck, targeted Biome, and planning. Behavioral review still found the wall clock, discarded admission/fresh baseline, and global overlay-copy defects. No phone, aggregate, Tailscale, or Serve execution occurred; the failed physical candidate was not rerun.
- Current state: independent review accepts the `e2201fb` correction: each admission call receives a fresh bounded stability budget while retaining one immutable authority baseline, and the two-use deterministic test crosses a long intermediate interval. `FE-V1-091` remains done. `BUG-067` stays open only until the later no-retry phone crossing; no phone/environment execution or failed-candidate retry occurred.

### BUG-068 Physical Aggregate Actions Use Ambiguous Presence And Automatic Retap Helpers

- Symptom: the aggregate Android driver still routes 29 actionable acquisitions through `waitForAndroidUiNodePresent()`, which chooses the largest semantic match without proving uniqueness, enabled/clickable state, page containment, or current route ownership. Four aggregate boundaries use `performVerifiedAndroidTap()`, which may reacquire and tap a second time after an eight-second wait.
- Impact: a background, stale, duplicate, clipped, or wrong-sheet control can be measured or tapped; a slow transition can receive an unowned second tap. This is especially unsafe for confirmation, revoke, lock, structured-control, clipboard, and route actions and contradicts the no-retry aggregate claim.
- Route: high release blocker decomposed across `FE-V1-092`, `FE-V1-095`, `FE-V1-096`, `FE-V1-097`, and final guard `FE-V1-099`; no product/UI/architecture change.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `MDH-06`, `MDH-07`, `MDH-09` to `MDH-17`, `MDH-20`, `MDH-22`, `MDH-24`.
- Root cause: the physical sequence grew screen-by-screen around generic text-presence and bounded-retap helpers instead of an explicit inventory of current-route action owners and transition oracles.
- Fix: replace each aggregate action with a pure context-owned selector, bounded private-free diagnosis, and one tap tied to an exact route/state/counter transition. Destructive dialogs use structural title/footer ownership; shell/sheet controls use complete current headers and geometry; structured controls use task-specific pending/call counters; utility/clipboard actions preserve exact navigation authority. Remove unused broad helpers and add an executable action ledger/guard.
- Harsh success criteria: no largest-match action selection, unowned global label, disabled/clipped/background match, second tap, action reacquisition, arbitrary sleep as selection proof, text-only route completion, swallowed selector failure, optimistic mutation, private diagnosis, or unmapped aggregate action may pass. Hostile pure tests cover each rejection family and exact one-attempt terminal truth.
- Current state: corrected in `e70e19a` without a phone run. Self-revoke now requires the exact post-Office-revoke baseline; fixed-sheet, Mission, utility, Session Actions, and Resume selectors reject competing or mutable owners; structured terminal/applied truth is owner-local; utility/Resume paths retain immutable navigation; and copy outcomes are page-owned. Direct hostile coverage retains cause propagation, Back geometry, exact initial model/Plan selection, clipboard ownership, duplicate/background rejection, and authority drift. `FE-V1-092` and `FE-V1-095` to `FE-V1-097` are done pending independent `FE-V1-100` review.

### BUG-069 Physical Route And Authority Transitions Accept Loose Or Text-Only Success

- Symptom: initial Mission reload, Session Detail opening and stale/current reload, Interrupt refresh, route returns, and archived-result return still accept `>`, `>=`, a visible label, or subscriber closure in isolation instead of one immutable before/after route snapshot. Some return controls are selected by broad presence only.
- Impact: duplicate requests, an extra stream generation, retained or replaced route authority, a stale label, or a background Back control can pass and contaminate the remainder of a no-retry phone run.
- Route: high release blocker decomposed into `FE-V1-093`, `FE-V1-098`, and final guard `FE-V1-099`; no product route, browser, SSE, or UI contract change.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-05` to `MDH-08`, `MDH-14`, `MDH-15`, `MDH-17`, `MDH-20`, `MDH-22`, `MDH-24`.
- Root cause: exact snapshot predicates were added to later recovery paths but not applied consistently to earlier bootstrap/reload and route-return orchestration.
- Fix: capture immutable route/request/subscriber snapshots before each action, require the exact route-specific deltas and stable destination shell after one tap, and reject missing, extra, pre-existing, transient, or drifted generations. Add hostile pure coverage and a ledger guard against loose covered predicates.
- Harsh success criteria: no request-start success, `>`/`>=`/range success for covered counters, one-label destination, retained subscriber, duplicate request/stream/open generation, second Back/VIEW/tap, BFCache assumption, authority rebase, stale node, private diagnosis, or unmapped transition may pass.
- Current state: corrected in `e70e19a` without a phone run. Mission destination and all relevant returns require the complete shell, selected-session hierarchy, exact route/request/subscriber deltas, source-owner removal, and one continuous two-second stable window; hierarchy read errors reset stability. Fragment-free reload now uses that complete stable waiter, and hostile coverage exercises incomplete shells, zero/extra/prechanged generations, transient truth, and read failures. `FE-V1-093` and `FE-V1-098` are done pending independent `FE-V1-100` review.

### BUG-070 Profile Return Accepts Any Later Session Read Instead Of One Recovery Generation

- Symptom: profile-away and profile-return refresh controls use broad presence acquisition; return succeeds when `sessionListRequests` is merely greater than its prior value, without exact access/host/session generation or stable Mission authority. Away succeeds on one generic label without an explicit zero-admitted-route proof.
- Impact: partial, duplicate, or stale recovery can pass while profile/Serve noninterference and paired write authority are not jointly proven.
- Route: high release blocker and dedicated leaf `FE-V1-094`, followed by action-ledger guard `FE-V1-099`; no product, profile-selection, Tailscale, Serve, or architecture change.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-06`, `MDH-18`, `MDH-19`, `MDH-22`, `MDH-24`.
- Root cause: the profile flow predates the exact Mission Control request snapshot/stability owner and retained its earlier label-plus-range completion.
- Fix: select one current Mission refresh action, tap once, prove closed-admission generic unreachable truth with zero admitted route/mutation delta while away, then prove exactly one access/host/session generation and stable current writable Mission truth after observed admission returns. Preserve claim, browser-mutation, manager, and foreign Serve counters exactly.
- Harsh success criteria: no duplicate/disabled/off-page refresh, request range, partial/extra generation, stale unreachable/ready truth, app-owned profile or Serve mutation, re-pair, retry, private identity diagnosis, or success before stable authority may pass. Hostile tests cover each boundary.
- Current state: corrected in `e70e19a` without profile, phone, Tailscale, or Serve execution. Away and return snapshots now include foreign Serve bytes/hash and manager attempts, and every settlement poll rereads them alongside closed/open admission and exact zero/one request generations. External mutation, stale truth, admission drift, and partial/extra generation hostile cases reset or fail settlement. `FE-V1-094` is done pending independent `FE-V1-100` review.

### BUG-071 Aggregate Action Ledger Is Not Executable

- Symptom: `physicalAggregateActionLedger` has exactly one row per canonical interaction, but `runPhysicalDashboardSequence` reaches many additional physical opens, choices, closes, confirmations, returns, and repeated event actions. Aggregate tap call sites do not accept an action ID or consume a ledger row.
- Impact: a new unowned tap, wrong maximum tap count, stale selector name, or nonexistent counter oracle still passes the guard. The ledger therefore cannot prevent another no-retry phone candidate from containing an unreviewed action boundary.
- Route: high release blocker in reopened `FE-V1-099`; no product, UI, architecture, phone, Tailscale, or Serve change.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-01`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-099` executable aggregate physical-action registry and call-graph guard.
- Root cause: the implementation equates the 39 canonical interaction taxonomy with the larger physical action call graph. Its validator checks length, order, uniqueness, nonempty metadata strings, frozen rows, and a `0|1` value; selector, waiter, oracle, diagnostic, and task fields are not executable references or checked against aggregate use.
- Fix: maintain separate exact canonical-interaction and reachable-action maps. Route every aggregate physical action through a typed registry-backed executor with a unique action ID, real selector/waiter and transition/counter ownership, and an enforced tap maximum. Reject direct/unregistered tap paths and swallowed invariant failures; prove exact multiplicity for interactions with multiple actions or repeated invocations.
- Harsh success criteria: adding, deleting, duplicating, reordering, bypassing, or miscounting any reachable action fails compile-time or deterministic validation; arbitrary nonempty strings cannot satisfy ownership; all 39 interactions map exactly to one or more registered actions; no direct aggregate tap bypass remains; every hostile mutation of identity, owner, executable oracle, maximum taps, or call-site registration fails.
- Validation: targeted hostile driver tests, CLI/root typechecks, full relevant Biome, planning, privacy/static, diff, and exact protected-worktree checks. No physical/environment mutation is permitted.
- Current state: corrected in `e70e19a` without physical/environment mutation. The registry invokes each action's executable selector source/waiter, counter source/owner, diagnostic source/owner, and guarded transition oracle; exact set/order/multiplicity checks reject missing, duplicate, reordered, and weakened actions; and the source guard rejects direct `tapAndroidUiNode` and direct helper bypasses. Hostile registry-call-site, unguarded completion, source-owner, reordering, and direct-tap tests pass. `FE-V1-099` is done pending independent `FE-V1-100` review.

### BUG-072 Aggregate Publisher Uses A Stale Package Generation

- Symptom: clean host-only preflight at `cc73a34` built a 619-source/1,245-output/6,231-entry schema-4 package, while the supported-browser manifest names a different package and `readPhysicalDashboardPackageIdentity()` reads an older 6,466-entry `IFC-V1-091` package. The publisher requires those historical and browser hashes to match, which is impossible.
- Impact: a fully successful 35-minute no-retry phone traversal would fail only during final evidence publication, discard aggregate acceptance, and consume a candidate for a deterministic host-side defect.
- Route: high release blocker and backlog leaf `FE-V1-101`; no product behavior, UI, architecture, phone, profile, Tailscale, or Serve change.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `MDH-02`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-101` current package/browser identity binding.
- Blocks: aggregate `FE-V1-090`, retained deployment, and all downstream release tasks.
- Root cause: package/browser evidence was repinned after product corrections, but the physical publisher remained hard-coded to a historical task artifact and fixed package counts. No deterministic test exercised publication against a freshly built current package.
- Planned fix: build and verify the current package before device mutation, bind its complete manifest identity to the exact supported-browser manifest, repin and rerun all 76 browser cases, pass the verified identity into publication, and add hostile stale/malformed/count/hash mismatch regressions.
- Harsh success criteria: no historical artifact read, hard-coded stale package identity, browser-only hash claim, post-phone package build, partial identity, count/hash mismatch, unverified package, stale browser report, swallowed publication failure, or phone/environment mutation may pass.
- Current state: closed in pushed `7b67563`. The exact current package is built and verified before device mutation, its complete schema/source/output/content/manifest/web identity must equal the supported-browser manifest, all 76 browser cases were republished against it, and publication consumes the validated identity directly. Driver 72/1, focused identity/evidence 83/1, package 42 plus deterministic double build, full unit 2,981/29, CLI/root typecheck, lint, planning/runtime-boundary, privacy/diff, and protected-worktree gates pass. The prepared `cc73a34` worktree was never executed and remains ineligible; no phone, profile, Tailscale, Serve, or aggregate execution occurred.

### BUG-073 Physical Approval Responding Owner Is Structurally Impossible

- Symptom: clean pushed candidate `43b93fc` failed once after 286 seconds with `Physical approval did not render owner-local responding truth.` The submit tap had already proven one pending `respond_approval` call.
- Impact: aggregate physical acceptance cannot pass the elevated approval checkpoint, and the pending fixture is never released, so all later interactions and retained deployment remain blocked.
- Route: high release blocker and backlog leaf `FE-V1-102`; no product UI or API behavior change is required.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-13`, `MDH-20`, `MDH-22`.
- Affected / owning task: `FE-V1-102` physical approval responding owner.
- Blocks: aggregate `FE-V1-090`, retained deployment, and all downstream release tasks.
- Root cause: production intentionally keeps `Approve elevated request?` open during submission, renders `Confirming decision` inside it, and disables Approve, Cancel, close, and Escape. The physical waiter requires the enabled confirmation-action selector to be null and then calls `selectPhysicalSessionContentNode()`, whose content owner is unavailable while the dialog overlays Session Detail. The conjunction is structurally unsatisfiable and existing hostile tests cover only the pre-submit confirmation owner.
- Planned fix: add a pure exact responding-dialog selector and waiter over the real title/reason/risk/status/footer geometry and disabled controls; bind it to unchanged navigation, one pending call, and bounded private-free diagnostics; release only after that state; then require dialog removal plus exact terminal row ownership.
- Harsh success criteria: text-only, background, duplicate, malformed, clipped, enabled-action, wrong-status, navigation-drift, counter-drift, premature terminal, and stale-dialog states fail; the exact production pending dialog passes; no retry, arbitrary delay, owner disappearance assumption, private hierarchy output, or phone/environment execution occurs during correction.
- Current state: closed in pushed `e82873e`. The pending waiter admits one exact title/risk/action/scope/reason/status owner with disabled close/Cancel/Approve controls, unchanged selected-session navigation, and one pending call; only then can the fixture release. Terminal truth requires no dialog residue, no second call, and one ordered Session Detail status/detail/action/scope owner. Missing, duplicate, background, enabled, clipped, malformed, premature, stale-dialog, navigation-drift, and counter-drift cases fail; diagnostics retain at most six bounded private-free states. Driver 73/1, focused approval state/component 63, focused browser 13, full unit 2,982/29, CLI/root typecheck, lint/exports, planning/runtime-boundary, privacy/diff, and protected-worktree checks pass. No phone, profile, Tailscale Serve, or aggregate execution occurred, and `43b93fc` remains ineligible.

### BUG-074 Aggregate Process Inherits Bounded Command Lifetime

- Symptom: pushed, clean, remote-matched candidate `39aed8a` passed frozen install, package, driver, typecheck, and lint review, then the aggregate process received SIGTERM at about 120 seconds and exited 143 without a Vitest assertion or harness result.
- Impact: process termination bypassed the harness `finally` cleanup and invalidated the candidate before physical acceptance could complete.
- Route: high release blocker owned by active `FE-V1-090`; no product, package, UI, or physical-oracle behavior change is required.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-090` aggregate physical mobile-dashboard hardening.
- Blocks: accepted aggregate phone evidence, retained deployment, and downstream release tasks.
- Root cause: the long-running physical command was a descendant of the API-managed PTY, whose bounded lifetime sends SIGTERM independently of the test harness.
- Planned fix: invalidate `39aed8a`, create one distinct pushed candidate, and launch it once under a detached monitor with a mode-600 runtime directory, environment-only saved-profile ids, private log/result files, one recorded PID and commit, no inherited PTY, duplicate-launch rejection, and read-only polling.
- Harsh success criteria: the monitor is detached before the initiating command exits; no saved-profile id enters arguments, output, repository files, or retained evidence; exactly one candidate/process/run exists; the process survives beyond 120 seconds; completion comes only from its exit marker and harness result; failure still receives exact cleanup inspection; and neither `39aed8a` nor any prior candidate is rerun.
- Current state: closed by candidate `87a6216`. A user-manager service loaded profile ids from a mode-600 environment file deleted at startup, exposed no id in arguments or retained output, survived beyond 120 seconds with one PID, and wrote its own result after 439 seconds. The later harness assertion is independently routed as `BUG-075`; exact automatic cleanup passed.

### BUG-075 Nested Prompt Uses Standalone Registry Assertion

- Symptom: candidate `87a6216` completed elevated approval, three event-diagnostic flows, and prompt send/running/completed behavior, then failed at `runProductionPromptUiSequence()` with `Physical aggregate action registry consumption was invalid.`
- Impact: every aggregate run that reaches prompt completion fails before stream recovery and all later dashboard systems, despite valid product behavior and action consumption.
- Route: high release blocker and backlog leaf `FE-V1-103`; no product UI, API, package, profile, or transport change is required.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-01`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-103` nested prompt aggregate-registry scope.
- Blocks: aggregate `FE-V1-090`, retained deployment, and downstream release tasks.
- Root cause: one registry instance intentionally records the whole dashboard run. The nested prompt call begins with Session Detail already open, so it does not consume standalone `prompt-open-session`, and it retains preceding dashboard bootstrap, approval, and event actions. Its unconditional `physicalAggregatePromptExpectedActionIds` assertion therefore requires one absent action while rejecting every valid preceding action.
- Planned fix: define one typed prompt registry scope. Standalone prompt acceptance keeps its exact standalone set; dashboard embedding must assert the exact cumulative ordered prefix from pairing through `prompt-send`, then the final dashboard assertion still owns the complete dynamic set. Aggregate scope is legal only for the existing session-open/no-Mission/no-cleanup/no-capture call shape. Fix the adjacent unawaited async rejection assertion found in the completed run log.
- Harsh success criteria: no skipped/deferred assertion, subset-only check, set-only check, reordered prefix, missing prior action, extra action, synthetic `prompt-open-session`, duplicate action, broad boolean escape hatch, swallowed registry failure, product change, or phone/environment execution passes. Direct tests execute standalone and cumulative registries and reject cross-scope, missing, extra, and reordered history. Focused driver, full unit, CLI/root typecheck, lint/exports, planning/runtime-boundary, privacy/diff, and protected-worktree gates pass.
- Current state: closed in pushed `01983b9`. Standalone prompt acceptance preserves its five-action set. Dashboard embedding is legal only for the already-open/no-Mission/no-cleanup/no-capture call shape and asserts all 14 actions from pairing through `prompt-send`; the final dynamic dashboard assertion remains unchanged. Cross-scope, four invalid embedding shapes, missing, extra, duplicate, and reordered histories fail directly, and the adjacent async rejection is awaited. Driver 74/1, full unit 2,983/29, CLI/root typecheck, 831-file/eight-package lint, planning 233/84/697, runtime boundary 619/22, privacy/diff, and protected-worktree checks pass without phone/environment mutation. Candidate `87a6216` remains ineligible and its exact cleanup is unchanged.

### BUG-076 Event Diagnostic Loses Its One-Tap Handoff

- Symptom: candidate `84af3ab` acquired and registry-revalidated one exact Replay boundary `View event details` node, issued its only tap, then timed out with the original target absent and no admitted completion.
- Impact: aggregate acceptance stops before event diagnostics, prompt, stream recovery, and every later dashboard system; the failed candidate cannot be retried.
- Route: high release blocker and backlog leaf `FE-V1-104`; no product contract, API, package, profile, or transport change is currently justified.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-01`, `MDH-14`, `MDH-20`, `MDH-22`, `MDH-24`.
- Affected / owning task: `FE-V1-104` stable event-diagnostic one-tap handoff.
- Blocks: aggregate `FE-V1-090`, retained deployment, and downstream release tasks.
- Root cause: the current reveal helper returns the first admitted target after a swipe and 350 ms settle. The registry proves the same node still exists once immediately before tapping, but neither owner proves continuous target geometry plus immutable selected-session/request authority. Its fallback summary reports only the original semantic target and a small generic label set, so an absent target after the tap cannot identify a moved/missed target versus exact request or sheet state.
- Planned fix: admit the exact label/action pair only after two continuous seconds of unchanged geometry, navigation, and event-read baseline; reset on hierarchy error, target/owner/geometry/counter/navigation drift, or duplicate/occluded state. Retain exactly one registry-owned tap. On completion or failure, classify the exact request delta and owner-local Event details title/close/heading/limitation/status state with at most six bounded private-free observations.
- Harsh success criteria: no retry, second tap, stale-node reuse, rebaseline, sleep-only fix, loose presence, subset/set-only registry weakening, swallowed hierarchy/request failure, broad boolean escape hatch, product change, or phone/environment execution passes. Direct hostile tests prove slow stable admission, transient target, geometry shift, disappearance, hierarchy error, navigation drift, request drift, duplicate/occluded owner, exact opened/loading/current/failure sheet outcomes, and bounded diagnostics. Focused driver, full unit, CLI/root typecheck, lint/exports, planning/runtime-boundary, privacy/diff, and protected-worktree gates pass.
- Current state: closed in pushed `440b0bf`. Each event target now requires two continuous seconds of exact action/label geometry and immutable selected-session/event-read authority, followed by a synchronous registry-owned final guard and one tap. Completion requires unchanged navigation, exactly one new event read, and one exact current Event details owner; failure distinguishes same/moved/duplicate/absent targets and all sheet states in at most six bounded private-free observations. Direct hostile coverage includes transient, shifted, disappearing, duplicate, disabled, occluded, hierarchy-error, authority-drift, pre-tap-drift, request-drift, all terminal-sheet, and diagnostic-bound cases. Driver 77/1, event UI/state 56, serial full unit 2,986/29, root/CLI typecheck, 831-file/eight-package lint, planning/runtime-boundary, privacy/diff, and protected-worktree gates pass; one unrelated ordinary-aggregate laptop-resume timeout passed 10/10 in isolation. No phone/profile/Tailscale/Serve mutation occurred. Candidate `84af3ab` remains ineligible and will not be retried.

### BUG-077 Async ADB Failure Erases Its Operation And Probe State

- Symptom: candidate `2cccd35` passed every host gate and read-only phone/Tailscale preflight, then its physical test failed after 90 seconds with only `Physical asynchronous ADB command failed.`
- Impact: the no-retry candidate is consumed before app interaction, while retained output cannot distinguish tailnet ping, private HTTPS probe, or UI-hierarchy failure and cannot support the next root-cause decision.
- Route: high release blocker and backlog leaf `FE-V1-105`; no product, UI, API, package, profile, transport, or retry contract change is justified.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-03`, `MDH-04`, `MDH-22` to `MDH-24`.
- Affected / owning task: `FE-V1-105` typed asynchronous ADB outcomes.
- Blocks: aggregate `FE-V1-090`, retained deployment, and downstream release tasks.
- Root cause: all three asynchronous ADB operations share one wrapper that rejects every nonzero remote-shell exit before the enclosing bounded ping/HTTPS wait can interpret it as a false observation, and the rejection discards operation, exit, signal, stdout, and stderr classification. UI hierarchy uses the same wrapper, so the final generic stack cannot identify which boundary failed.
- Planned fix: give each call one closed typed operation id; classify a completed bounded remote-shell exit into a private-free numeric outcome; let only ping/HTTPS callers interpret nonzero as false within their existing bounded preflight windows; make UI-hierarchy nonzero fail immediately under its exact operation; and classify spawn/timeout/signal/ADB-transport failures without retaining command arguments or output.
- Harsh success criteria: no raw argument, hostname/origin, device id, stdout, stderr, exception cause, or arbitrary exit text reaches diagnostics; no product action, pairing claim, mutation, UI hierarchy failure, transport failure, or prior failed candidate is retried. Direct injected tests cover zero/nonzero exits, spawn error, timeout/signal, protected stdout/stderr, all three operation ids, bounded network false observations, and exact UI-read failure. Focused driver, full unit, CLI/root typecheck, lint/exports, planning/runtime-boundary, privacy/diff, and protected-worktree gates pass without phone/profile/Tailscale/Serve mutation.
- Current state: closed in pushed `c8bf276`. One frozen three-operation catalog and classifier validate bounded output without retaining arguments, stdout, stderr, private values, or causes; completed remote-shell exits carry only operation/status/stdout internally. Ping and HTTPS nonzero exits become false observations only inside their existing bounded preflight waits. UI nonzero, ADB transport, spawn, timeout/signal, malformed, oversized, and protected-output outcomes fail immediately with exact operation-owned private-free errors. Hostile tests cover all operations and outcomes, including a private device id inside transport stderr. Driver 78/1, full unit 2,987/29, root typecheck, 831-file/eight-package lint, planning 235/84/699, runtime boundary 619/22, privacy/diff, and exact protected-worktree gates pass without further phone/profile/Tailscale/Serve mutation. Candidate `2cccd35` remains ineligible and will not be retried. Evidence retained in pushed `c8bf276`.

### BUG-078 Stream-Recovery Waiter Erases Every Android Hierarchy Failure

- Symptom: clean pushed candidate `6124f5c` passed 78 host driver checks, typecheck, lint, current package build/verification, read-only device/Tailscale preflight, pairing, event diagnostics, and prompt completion. After 364 seconds the server proved one `source_failed` stream, one held reconnect request, zero active subscribers, and one opened stream, but `waitForStablePhysicalPromptRecoveryTruth()` timed out with `states=none` while the request path ended at `504`.
- Impact: aggregate acceptance cannot distinguish missing product reconnect truth from repeated UI-hierarchy exit, timeout, transport, invalid/private output, or parser failure. The failed candidate produced no evidence and cannot support a product change or another no-retry candidate.
- Route: high release blocker and backlog leaf `FE-V1-106`; no product UI, SSE, retry, route, authorization, package, profile, or transport change is justified until the observation boundary is exact.
- Related requirements: `NFR-003`, `NFR-004`, `PR-005`, `PR-011`, `MDH-07`, `MDH-14`, `MDH-20`, `MDH-22` to `MDH-24`.
- Affected / owning task: `FE-V1-106` typed physical stream-recovery observation.
- Blocks: aggregate `FE-V1-090`, retained deployment, and downstream release tasks.
- Root cause: the stable recovery waiter catches every `readAndroidUiNodes()` failure, clears stability, and returns `false` without classifying or retaining even a private-free failure category. Two command-level timeouts can consume the full 30-second state window, leaving `states=none`; existing tests inject parsed node arrays and do not execute the async hierarchy-read failure path.
- Planned fix: isolate one typed recovery observation helper that classifies each hierarchy attempt before evaluating exact nodes, navigation, subscriber, recovery-gate, stream-failure, and request-generation truth. Bound command and overall budgets so a failed read cannot erase the state window; preserve only a closed private-free category and at most six immutable observations. Use direct injected async tests to prove every read/process/parser outcome, exact reconnect and completion stability, reset semantics, timeout accounting, and no retry of app actions or failed candidates. Change product behavior only if the corrected oracle proves a product defect.
- Harsh success criteria: `states=none`, swallowed hierarchy error, raw argument/output/cause, private value, arbitrary timeout extension, unbounded read overlap, stale-node reuse, stability rebase, loose text presence, fake success, product-action retry, and failed-candidate reuse cannot pass. UI read exit, spawn, termination, transport, malformed/private/oversized output, parser failure, valid mismatch, authority drift, transient exact state, and stable exact reconnect/completion each produce deterministic bounded outcomes. Focused driver, full unit, CLI/root typecheck, lint/exports, planning/runtime-boundary, privacy/diff, protected-worktree, and clean candidate host gates pass before another phone mutation.
- Current state: closed through `32e5b8c` and `7292927`. Every attempt now yields a typed bounded private-free read/process/parser or exact node-state observation; command and overall budgets are independent, reads do not overlap, and stability cannot rebase on failed or stale observations. Hostile direct tests pass, and later distinct candidates crossed the real held stream-recovery checkpoint. Candidate `6124f5c` remains ineligible and was not retried.

### BUG-079 Production Route Ports Lose Their Service Receiver

- Symptom: the installed V1 package is healthy, paired, remote-ready, and mutation-ready, but `codexdeck start` returns HTTP 500. Its audit trail records accepted then incomplete `internal_error`, while both selected session and start-recovery tables remain empty.
- Impact: users can pair and open Mission Control but cannot create the first managed session; all receiver-dependent session and control mutations are at risk.
- Route: critical release blocker against completed `IFC-V1-082` and `IFC-V1-040`; expected product and API behavior are unchanged.
- Root cause: production composition copied prototype methods into null-prototype route-port views without binding their owning service. Routes intentionally invoke ports with an undefined receiver, so `DefaultManagedCodexThreadService.start()` throws before cwd validation or reservation.
- Fix: bind every selected production function port to its source owner at composition time; retain frozen exact-key views and fail fast for non-callable ports.
- Harsh success criteria: detached invocation preserves the original owner; non-callable ports reject; focused tests, server typecheck, and production package verification pass; an upgraded installed service creates exactly one managed session without recovery residue; the paired phone can open that session after fragment-free reload.
- Current state: closed in pushed `793e69b` and `08c210f`. Focused 94 tests, server typecheck, planning/runtime gates, deterministic 633-source/1,273-output/6,293-entry build, and standalone package verification pass. The live preserved-state upgrade starts cleanly; one managed session has accepted/succeeded audit truth with zero recovery rows, and the paired phone retains write authority, opens that session, connects its current stream, and renders `Ready to send`.

### BUG-080 Accepted Prompt Falsely Fails And Delegated Turn Poisons Projection

- Symptom: one paired-phone prompt reached Codex and started a real SideCue turn, but the API returned terminal `incomplete/internal_error`, the composer reported that the prompt could not be sent, projection stopped after the first agent message, and Mission Control later reported unavailable while Codex continued through internal workers to `task_complete`.
- Impact: the primary remote-control flow gives contradictory write truth, hides sustained activity and completion, leaves the selected session falsely running, and closes host mutation admission.
- Route: critical release blocker against `IFC-V1-041`, `INT-V1-017`, `INT-V1-018`, `FE-V1-011`, and `FE-V1-020`; expected product and API behavior are unchanged.
- Harsh success criteria: one phone tap performs exactly one dispatch and returns one exact accepted `202` receipt; direct, staged-model, staged-Plan, and steer receipts contain only the seven contract fields; parent-turn projection remains current through supported activity and terminal events when Codex spawns internal workers; unrelated child-thread activity cannot fail or contaminate the managed parent; host health remains ready; no HostDeck or managed Codex path launches a desktop GUI; focused hostile tests, clean package verification, live preserved-state deployment, and one real phone prompt prove accepted-to-terminal truth without retry.
- Fix: construct public prompt receipts field by field, accept exact completed-only `subAgentActivity` items while preserving paired-item ordering, and keep reconnect event observation independent of the not-yet-readable live interrupt client. The consumed SideCue prompt was neither resent nor interrupted.
- Current state: closed on installed `0.0.3`. One physical phone tap sent `PHONE_OK` exactly once; its one dashboard operation has accepted and succeeded audit phases, the same live page rendered user and agent `PHONE_OK`, then reached `Quiet`, `Current`, and `Turn completed` without prompt/stream failure or refresh. The preserved Codex PID, both managed sessions, remote readiness, all seven health components, and open mutation admission survived the upgrade.

### BUG-081 Forward Replay Boundary Rejected By Browser

- Symptom: the paired phone opens a current writable session, then Session Detail reports `Stream stopped` and disables prompts while all ordinary API reads remain healthy.
- Impact: retained sessions cannot receive live updates or phone prompts after a host restart or retention gap.
- Route: critical release blocker against completed `FE-V1-023` and `FE-V1-012`; the selected continuity contract is unchanged.
- Root cause: both browser continuity layers required replay-boundary `after` to equal the browser's stale requested cursor. The server correctly returns its newer durable retention floor when unavailable history lies between them.
- Fix: accept a first boundary whose `after` is at or ahead of the committed browser cursor; continue rejecting null, backward, repeated, or mid-attempt boundaries and ordinary unmarked gaps.
- Current state: closed in pushed `8230f5b` and installed `0.0.3` from source commit `acb27b0`. Exact paired-browser capture reproduced request `after=15` and valid boundary `after=29`, cursor `30`. Focused 40 tests, all 909 web tests, web typecheck, deterministic 6,293-entry package build and independent verification pass. After preserved-state upgrade, the paired phone displayed `Current`, retained the restart boundary, enabled the composer, streamed one exact phone prompt through completion, and showed no prompt or stream failure.

### BUG-082 Initial Adoption Boundary Rejected By Public Reads

- Symptom: adopting a native Codex thread succeeds, but `/api/v1/sessions` and Session Detail fail closed, so the phone reports Mission Control unavailable.
- Impact: an adopted existing session cannot be used from HostDeck even though its mapping and projected history are valid.
- Route: critical backlog bugfix owned by `INT-V1-109`; no contract or UI change.
- Root cause: adoption correctly stores a replay boundary at cursor 1 with no retention floor, while the public read repository incorrectly required every null-floor window to contain zero replay boundaries.
- Fix: accept exactly one initial boundary only as the first event, preserve strict cursor/count/byte checks, admit a valid boundary-only retained window, and continue rejecting non-first or duplicate boundaries.
- Current state: closed in `76f5016`. Direct list/detail regressions and the exact 0.144.0 adoption aggregate prove authenticated phone-facing list, detail, event page, and SSE reads before and after HostDeck restart.

### BUG-083 Shared Codex History Breaks Startup And Existing-Session Continuity

- Symptom: with the user's real shared `~/.codex`, HostDeck could report Mission Control unavailable, leave managed sessions unavailable after an app-server restart, and hide ordinary Codex sessions created by nearby CLI versions.
- Impact: phone control and the required normal-Codex-to-HostDeck handoff were unreliable on the actual laptop even though isolated interoperability tests passed.
- Route: critical completed-task bugfix owned by `INT-V1-109`; no public API, UI, setup, or security contract change.
- Root cause: startup strictly parsed the entire foreign history inventory instead of only managed ids; app-server `notLoaded` state was treated as a contradiction instead of resumable idle state; discovery required a history's creation version to equal the controlling runtime; and Linux uninstall waited forever on a stopped `failed` unit with PID zero.
- Fix: reconcile from bounded state-database inventory and strictly read only managed ids, exact-resume managed `notLoaded` threads, preserve the source history version while recording the current controlling runtime on adoption, and accept a failed/PID-zero unit as stopped during uninstall.
- Current state: closed in `495fde2` and `dbe641e`. Focused 70, unit 3,220/32 opt-in skipped, contract 287, integration 36, typecheck, 897-file lint, exact model-backed interoperability, deterministic package build/verification, preserved-state install, API readiness, and live shared-home discovery pass. The installed service retained both managed sessions and paired write device; existing normal sessions from Codex 0.130.0 through 0.146.0 are discoverable, and a 0.130.0 full adoption snapshot parses through the pinned 0.144.0 runtime without mutation.

### BUG-084 Mobile Startup Transfers Uncompressed Assets And Eager Device State

- Symptom: Mission Control felt slow on the physical phone; a cache-disabled load transferred 1.11 MB of JavaScript and fetched the paired-device list before Host/access was opened.
- Impact: the primary phone route paid avoidable network, parse, and API work on every cold load.
- Route: high completed-task bugfix owned by `FE-V1-090`; no product, security, or setup contract change.
- Root cause: validated JS/CSS assets had no content-encoding negotiation, and the always-mounted Host/access controller eagerly loaded device state.
- Fix: stream Brotli or gzip only for validated JS/CSS assets with exact `Accept-Encoding` handling and `Vary`; defer the device read until the user first opens Host/access, then retain activation across routes.
- Current state: closed in `ee68dcc` and installed as `0.0.5`. On the Xiaomi phone, cache-disabled DOM ready/FCP improved from 724/776 ms to 369/400 ms; JavaScript transfer fell from 1,105,233 bytes to 270,710 bytes, CSS to 15,443 bytes, and startup made zero device-list requests. Opening Host/access issued exactly one device read. Static decompression identity tests, all 910 web tests, typecheck, lint, contract 287, integration 36, package 43, package verification, and manual phone inspection pass without fallback or browser errors.

### BUG-085 Adopted Session Start Boundary Stops Browser Streaming

- Symptom: an adopted native session appeared on the phone with retained history, then Session Detail changed to `Stream stopped` and disabled prompting while direct API reads remained healthy.
- Impact: existing Codex sessions could be adopted but not reliably controlled from the phone.
- Route: critical completed-task bugfix owned by `INT-V1-109`; no public contract or adoption-policy change.
- Root cause: the browser requested recent history with `after=0`, but rejected the valid initial adoption boundary whose durable predecessor is represented as `after=null`; the client canceled the otherwise healthy SSE response.
- Fix: treat `null` as the cursor-zero floor only when validating the first boundary, while preserving rejection of backward, duplicate, repeated, and mid-attempt boundaries.
- Current state: closed in `07f7355` and installed as `0.0.5`. A real Codex 0.144.0 thread was created normally, adopted unchanged, opened on the phone with a stable SSE connection, accepted one `202` prompt, streamed exact `ADOPTED_PHONE_OK` in 5.2 seconds, reopened through `codexdeck resume`, unmanaged, and reopened in normal Codex with both original and phone turns intact. The regression test, all 910 web tests, package verification, service restart continuity, Wi-Fi-off cellular/Tailscale load, and browser request/error inspection pass; no active user session was taken over.

### BUG-086 Ordinary Top-Level Codex Fork Cannot Be Adopted

- Symptom: after closing the standalone SideCue Codex client, `codexdeck adopt 019fc8bd-25ef-74c3-a3bf-c6e59e4122a4 --name sidecue_sol --confirm-handoff` returned `operation_conflict`; `codexdeck resume` could not help because it correctly accepts only managed `sess_...` ids.
- Impact: an ordinary resumable existing Codex conversation cannot enter HostDeck, blocking the primary laptop-to-phone continuity flow.
- Route: critical completed-task release blocker owned by `INT-V1-109`; `FR-019`, `PR-006`, and `DEC-030` now state the intended top-level user-facing eligibility precisely.
- Root cause: exact `thread/read` reports SideCue as top-level and user-facing but retains `forkedFromId`; HostDeck rejected every fork, trusted list metadata that omitted this provenance, rejected interrupted turns without completion timestamps, and failed an entire mature turn instead of retaining an explicit bounded suffix.
- Fix: accept and revalidate top-level fork provenance while continuing to reject parent/subagent identity; exact-read discovery candidates; admit honest interrupted timestamps; inspect at most 1,024 items per turn; and retain at most 64 complete recent user/agent messages with explicit truncation truth.
- Current state: closed in pushed `2a63fd5` and installed through `0.0.7`. The exact SideCue thread was adopted unchanged as managed session `sess_f3ce8660795f47a23dc3`, survived preserved-state upgrade, remained current on the phone, and opened through `codexdeck resume` by that managed id. Focused adapter/contracts/service regressions, full workspace gates, exact live bounded-history parsing, and the shared-TUI handoff pass without transcript output or mutation.

### BUG-087 Adoption Boundary Crashes Session Detail

- Symptom: after SideCue adoption, authenticated detail, approval, CSRF, and SSE requests returned `200`, but Session Detail rendered blank and stopped updating.
- Impact: the adopted session existed and was writable but could not be used from the phone.
- Route: critical completed-task bugfix owned by `FE-V1-036` and `INT-V1-109`; no product or security contract change.
- Root cause: interrupt control duplicated the replay-boundary reason list and omitted the valid `adoption` reason, so its strict parser threw during React rendering.
- Fix: export the replay-boundary reasons and schema from the shared contract, consume that schema in both strict UI boundary parsers, and cover adoption boundaries before idle and active turn evidence.
- Current state: closed in pushed `f923874` and installed as `0.0.7`. All 911 web tests, 3,227 unit tests/32 intentional skips, 287 contract tests, 36 integration tests, typecheck, lint, planning, runtime-boundary, package verification, and a cache-disabled Xiaomi phone trace pass. The exact SideCue page renders at 384 by 736 with current SSE, enabled composer, no failure copy, and no current exception, console error, or HostDeck request failure.

### BUG-088 Mature Native History Rejects Valid Private Payloads

- Symptom: confirmed adoption of the closed MarketPilot thread returned `protocol_error` with HTTP 502 before creating a managed session.
- Impact: valid mature Codex sessions containing large file changes could not enter the laptop-to-phone continuity flow.
- Route: critical completed-task bugfix owned by `INT-V1-109`; no public product, security, setup, or UI contract change.
- Root cause: adoption reused the live-event item parser for native history and applied projection text limits to recognized private `fileChange` payloads even though native adoption deliberately retains only bounded user and agent messages.
- Fix: validate every history item's bounded id and generated item type, reject unknown types and duplicate ids, fully parse retained user/agent messages, and omit recognized non-message payload bodies without reading or copying them.
- Current state: closed in `5b0d6e1` and installed as `0.0.8`. Focused 37, unit 3,228/32 intentional skips, contract 287, integration 36, typecheck, lint, planning, runtime-boundary, deterministic package build, and independent package verification pass. Exact live read/identity/resume probes now accept MarketPilot; confirmed adoption succeeded unchanged as `sess_04148572a7253dc60de5`, the failed attempt left no membership, and the managed session is current, idle, deduplicated from discovery, and retains its branch and paused goal.

### BUG-089 Native Adoption Requests Unbounded Omitted History

- Symptom: after the `BUG-088` fix, confirmed adoption of ScandyControl still returned `protocol_error` with HTTP 502; MicroForge's much larger history closed the Codex transport during the same read.
- Impact: valid existing sessions with legacy image metadata or large persisted tool history could not reliably enter HostDeck.
- Route: critical completed-task bugfix owned by `INT-V1-109`; no public product, security, setup, or UI contract change.
- Root cause: adoption still requested Codex's full turn-item surface and fully validated private user-input metadata that HostDeck deliberately omits. Legacy `localImage.detail: null` failed the current live schema, while very large full histories could exceed the transport before projection.
- Fix: request the bounded Codex summary surface, require that exact response mode, strictly validate retained user/agent text and recognized type envelopes, omit recognized private bodies without parsing or copying them, and continue rejecting unknown types, malformed retained text, duplicate ids, and identity races.
- Current state: closed in `053edd8` and installed as `0.0.9`. Focused 16, unit 3,229/32 intentional skips, contract 287, integration 36, typecheck, lint, planning, runtime-boundary, deterministic package build, and independent verification pass. Live bounded reads pass for ScandyControl, MarketPilot, SideCue, and the 665 MB MicroForge history. ScandyControl was adopted unchanged as `sess_2582903113e11fd1a362`, appears exactly once as current/idle, and is excluded from discovery.

### BUG-090 Optional Reconciliation Timeout Blocks Startup

- Symptom: HostDeck could remain unavailable when an optional managed-thread detail read timed out during startup reconciliation even though the shared broker and durable session were recoverable.
- Impact: Mission Control and phone control stayed unavailable instead of exposing the affected session as explicitly stale.
- Route: critical completed-task bugfix owned by `INT-V1-114`; no public contract, UI, setup, or security change.
- Root cause: the reconciliation boundary converted Codex `remote_error` into unavailable state but let the equally non-authoritative `request_timeout` escape and abort runtime admission.
- Fix: classify optional read timeouts as unavailable, retain explicit stale/unknown projection truth, and continue startup without resuming or mutating the thread.
- Current state: closed in pushed `410e966`. Direct read/goal/turn timeout coverage proves ready runtime admission with three stale/unavailable sessions and no hidden retry or thread mutation.

### BUG-091 Resumed Historical Thread Cannot Rejoin HostDeck

- Symptom: an ordinary `codex resume NATIVE_UUID` session ran on the standard shared broker while HostDeck kept its retained mapping recovery-required, omitted live updates, or failed startup during automatic enrollment.
- Impact: normal laptop Codex and phone HostDeck were not reliably interchangeable for existing conversations.
- Route: critical completed-task release blocker owned by `INT-V1-113`, `INT-V1-114`, and `IFC-V1-113`; the selected shared-session contract is unchanged.
- Root cause: retained recovery mappings could not refresh atomically; eligibility compared historical thread-creation `cliVersion` with the active broker; `thread/started` bypassed existing stale mappings; and the reconnect reconcile lease rejected enrollment's read-class `thread/resume` subscription.
- Fix: refresh retained mappings without changing native identity, derive eligibility from the admitted active app-server, route stale existing mappings through enrollment, and permit only the required read-class resume subscription during reconcile while keeping mutations blocked.
- Current state: closed in pushed `acb23a8`, `d1d6a6a`, and `89d9eca`, installed as `0.0.7`. Focused 57, unit 3,282/32 intentional skips, contract 309, integration 36, web 960, typecheck, lint, package 44/two deterministic builds, packaged Chromium, and supply-chain 8 pass. A real historical SideCue thread retained its native/internal identity across preserved-state upgrade, projected an ordinary-TUI turn into HostDeck, and displayed one HostDeck-submitted turn in that same TUI without broker replacement.
