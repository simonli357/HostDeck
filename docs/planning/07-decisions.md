# Decisions

Resolved decisions only. Keep open questions in the PRD.

| ID | Date | Decision | Why | Affects |
| --- | --- | --- | --- | --- |
| DEC-001 | 2026-07-08 | Use the drafted HostDeck end goal as the planning target. | Human approved the end-goal draft after the brainstorming conflict about Android-only V1 was removed. | `docs/planning/00-end-goal.md`, roadmap planning |
| DEC-002 | 2026-07-08 | Use the proposed V1/V2/V3 roadmap scope. The tmux implementation detail was later superseded by `DEC-018`; the product outcome remains. | Human approved a narrow V1 focused on an Ubuntu host agent and mobile-responsive local dashboard, with relay, native apps, voice, push, and bulk operations deferred. | `docs/planning/00-roadmap.md`, active-version PRD |
| DEC-003 | 2026-07-08 | Make `/model`, `/goal`, and `/plan` primary V1 slash-command controls, with `/usage`, `/compact`, and `/skills` also available as utility commands. | Human identified `/model`, `/goal`, and `/plan` as the main slash commands for the product workflow. | `docs/planning/01-prd.md`, requirements planning |
| DEC-004 | 2026-07-08 | Use a one-user local pairing/token flow as the V1 write trust gate. | Human approved the PRD recommended defaults; this keeps V1 secure enough without committing to full QR pairing before architecture. | `docs/planning/01-prd.md`, security requirements, architecture |
| DEC-005 | 2026-07-08 | Bind localhost by default and require explicit opt-in for LAN access in V1. | Human approved the PRD recommended defaults; local-first access reduces accidental exposure while still allowing phone browser use when enabled. | `docs/planning/01-prd.md`, platform requirements, architecture |
| DEC-006 | 2026-07-08 | Support new HostDeck-managed `codex` sessions first and defer arbitrary import. `DEC-018` later adds exact resume of HostDeck-managed app-server threads. | Starting with managed sessions avoids brittle arbitrary terminal/session import. | `docs/planning/01-prd.md`, functional requirements, backlog |
| DEC-007 | 2026-07-08 | Approve the hardened V1 requirements for architecture planning. | Human approved the requirements after an explicit hardening pass for CLI/API contracts, persistence, output, audit, fixtures, service lifecycle, and safety behavior. | `docs/planning/02-requirements.md`, architecture planning |
| DEC-008 | 2026-07-08 | Approve the initial TypeScript/Node/Fastify/React, pnpm, SQLite, local-trust, localhost-default architecture. Its tmux backend and plaintext-LAN assumptions were superseded by `DEC-018` and `DEC-020`. | The initial pass established package, storage, trust, and service boundaries but predated current Codex rich-client interfaces and the security audit. | `docs/planning/04-technical-plan.md`, implementation blueprint |
| DEC-009 | 2026-07-08 | Approve the V1 UX/interface contract while deferring generated visual mockups until detailed design, state coverage, and validation planning make them useful implementation targets. | Human corrected that image-generated mockups are later than the current UX contract step, then asked to proceed to the next workflow step. | `docs/planning/03-ux-spec.md`, implementation blueprint, later UI concept generation |
| DEC-010 | 2026-07-08 | Approve the hardened V1 implementation blueprint as the input to test-plan planning. | Human asked to proceed after the blueprint was hardened with approval criteria, public interfaces, storage model, critical sequences, failure matrix, requirement coverage, candidate blocks, delivery slices, and spike exit criteria. | `docs/planning/04a-implementation-blueprint.md`, `docs/planning/04b-test-plan.md`, capability block planning |
| DEC-011 | 2026-07-08 | Approve the V1 test plan as the input to capability block planning. | Human asked to proceed after the test plan was drafted with planned commands, requirement coverage, block coverage, spike validation, regression matrices, manual inspection, and evidence policy. | `docs/planning/04b-test-plan.md`, `docs/planning/05-blocks/` |
| DEC-012 | 2026-07-08 | Approve the V1 capability block map as the input to backlog decomposition. | Human asked to proceed after the block index and six required V1 block specs were created for contracts/core/fixtures, local state/auth/audit, tmux/output, API/CLI, web dashboard, and hardening/release readiness. | `docs/planning/05-blocks/`, `docs/tracking/backlog/`, `docs/tracking/06-tasks.md` |
| DEC-013 | 2026-07-08 | Require exact per-requirement task traceability and split broad backlog tasks before implementation authorization. | Human challenged whether the decomposed backlog was rigorous enough to cover V1 completely. | `docs/planning/02-requirements.md`, `docs/tracking/backlog/`, `docs/tracking/06-tasks.md` |
| DEC-014 | 2026-07-08 | Use `better-sqlite3` plus a first-party migration runner for V1 local SQLite storage. | `node:sqlite` works in pinned Node `22.22.2` but still emits an experimental warning there; `node-sqlite3` is deprecated/archived; `sqlite` wraps that driver; `better-sqlite3` is current, MIT licensed, supports Node 22, and provides the synchronous transaction model HostDeck needs. | `docs/planning/04-technical-plan.md`, `docs/planning/04a-implementation-blueprint.md`, `docs/planning/05-blocks/BLK-V1-02-local-state-auth-audit.md`, `docs/tracking/backlog/local-state-auth-audit.md` |
| DEC-015 | 2026-07-08 | Use a host-only `HttpOnly` cookie for dashboard device-token transport, plus a server-bound CSRF token required in same-origin write headers. | Browser writes need a revocable token without durable JavaScript-readable storage; SameSite/CORS are useful but not enough alone, so V1 requires an explicit CSRF header and no wildcard credentialed CORS. | `docs/planning/04-technical-plan.md`, `docs/planning/04a-implementation-blueprint.md`, `docs/planning/05-blocks/BLK-V1-02-local-state-auth-audit.md`, `docs/tracking/backlog/local-state-auth-audit.md`, `packages/contracts/src/api.ts` |
| DEC-016 | 2026-07-08 | Use V1 retention defaults of 10,000 output events or 10 MB output payload per session, plus 5,000 audit events or 30 audit days globally. | The hybrid caps bound noisy sessions without tuning to today's small fixtures; replay/audit boundaries make pruning visible instead of pretending old output or audit history is still contiguous. | `packages/contracts/src/storage.ts`, `docs/planning/04-technical-plan.md`, `docs/planning/04a-implementation-blueprint.md`, `docs/planning/04b-test-plan.md`, `docs/planning/05-blocks/`, `docs/tracking/backlog/local-state-auth-audit.md` |
| DEC-017 | 2026-07-08 | Use tmux `pipe-pane` for live output ingestion plus bounded `capture-pane` snapshots on startup and reader restart. This decision is now historical under `DEC-018`, not the selected V1 runtime. | The spike proved tmux capture mechanics with a fake producer; the 2026-07-09 audit found that it did not prove the real Codex product path. | `docs/planning/05-blocks/BLK-V1-03-codex-runtime-events.md`, `docs/tracking/backlog/tmux-output.md`, `artifacts/int-v1-001-tmux-capture-spike.md` |
| DEC-018 | 2026-07-09 | Rebase the primary V1 Codex integration from tmux/TUI scraping to a version-gated Codex app-server adapter over a user-private Unix socket. Keep existing tmux code only until the structured vertical slice passes. | Codex now documents app-server as the rich-client interface. Local 0.144.0 evidence proved schema generation, structured model/thread/goal operations, and normal TUI attachment over the same runtime. TUI text parsing duplicates structured state and the existing smoke used a fake Codex producer. | `docs/planning/04-technical-plan.md`, `docs/planning/04a-implementation-blueprint.md`, `docs/planning/05-blocks/BLK-V1-03-codex-runtime-events.md`, `docs/tracking/backlog/tmux-output.md`, `artifacts/int-v1-002-codex-integration-reassessment.md` |
| DEC-019 | 2026-07-09 | Make phone Mission Control and conversation-first Session Detail the V1 primary UI. Support structured inline approvals, but defer phone raw-shell input; full control resumes the same thread in the laptop TUI. | The rejected mockups were desktop-led, and phone raw terminal control weakens both usability and the trust boundary. Structured thread, turn, approval, model, and goal state supports the actual mobile workflow without building a terminal emulator. | `docs/planning/00-roadmap.md`, `docs/planning/01-prd.md`, `docs/planning/03-ux-spec.md`, `docs/tracking/backlog/web-dashboard.md` |
| DEC-020 | 2026-07-09 | Permit HTTP only on loopback. Explicit LAN mode must use HTTPS, paired read/write authorization, a configured host allowlist, and Secure/HttpOnly/SameSite cookies; app-server is never exposed directly to LAN. | Session output, project paths, approvals, and write credentials are sensitive. Plaintext LAN and unauthenticated reads cannot satisfy the product's shell-control security boundary. | `docs/planning/02-requirements.md`, `docs/planning/04-technical-plan.md`, `docs/tracking/backlog/api-cli-control-plane.md`, `docs/tracking/backlog/hardening-release.md` |
| DEC-021 | 2026-07-09 | Pin V1 to exact `codex-cli 0.144.0` and its reviewed experimental app-server binding; require `experimentalApi` because primary `/plan` semantics are absent from the default generated surface. | Exact generation is reproducible, and a live no-model handshake returns `Plan` and `Default`. A broad version range or stable-only binding would claim unsupported required behavior. Upgrades require a new generated identity and compatibility review. | `packages/codex-adapter/`, `docs/planning/04-technical-plan.md`, `artifacts/int-v1-003-codex-binding-compatibility.md` |
